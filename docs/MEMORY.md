# MEMORY.md — Babel

Memoria de arquitectura y estado del proyecto. Actualizar al cierre de cada sesión de trabajo relevante.

---

## 1. Estado actual

| Campo | Valor |
|---|---|
| Versión | En producción desde 03/08/2026 |
| URL producción | https://babel.letiende.co (en producción desde 03/08/2026, dominio personalizado PR #81) |
| URL staging (real) | `https://oyzau0c910.execute-api.us-east-1.amazonaws.com` (API Gateway HTTP API, stage `staging`) |
| Rama principal | `main` |
| Rama de trabajo actual | `main` (sin trabajo en curso; próximo lote diseñado en `docs/plan-duplicados-catalogacion.md`) |
| Última sesión | 2026-08-19 — Tarea 2 del lote de catalogación (palabras clave que invalidan una portada de scraping) completa: PR #90, validado en `staging` ("Funciona bien"), incluye un bug real encontrado y corregido en el mismo PR (`palabrasClaveInvalidas` sin normalizar para sitios ya guardados — ver §2 y §7). Tarea 3 (proceso asíncrono "Validar libros") promovida como siguiente tarea, sin detallar todavía. Ver `TODO.md` |

---

## 2. Funcionalidades completadas vs. pendientes

- [x] Scaffold del proyecto Angular 22 + SSR + Tailwind CSS 4 (PR #2, rama `feature/scaffold-angular-ssr`) — ver detalle en §9
- [x] Esqueleto de Serverless Framework + tablas DynamoDB, desplegado a `staging` (PR #3, rama `feature/serverless-skeleton-dynamodb`) — ver detalle en §5 y §9
- [x] Autenticación con Google (Firebase Auth) — SDK cliente, `AuthService` (Signals), `AuthGuard` (PR #5) — ver detalle en §9.
- [x] `LoginComponent` + `NoAuthGuard` — probado con deploy real a `staging`, identidad visual igualada a Comandante por medición de píxeles (sin Ionic, PR #6/#9 fusionados) — ver detalle en §7 y §9.
- [x] Deploy automático a `staging` en cada PR (CI, PR #8 fusionado) — ver §5 y §9
- [x] Íconos PWA + `manifest.webmanifest` (PR #10 fusionado) — ver §7 y §9. La fuente Angellya NO se integrará como `@font-face`: decisión del usuario 2026-07-18, es tipografía exclusiva del logo y los logos ya existen como SVG (ver §7).
- [x] Modelos de datos compartidos + cliente DynamoDB base (PR #12, rama `feature/modelos-datos-dynamodb-client`) — ver detalle en §9.
- [x] Verificación real del ID Token de Firebase en el backend + `GET /api/usuarios/me` (PR #14 fusionado) — 401/403/200 verificados en vivo contra `staging` con un ID Token real. Ver detalle en §9.
- [x] Endpoint público `GET /api/libros` — catálogo de consulta (PR fusionado) — verificado en vivo contra `staging` (`200` con `[]`, sin auth). Backend solamente, sin consumo desde el frontend todavía. Ver detalle en §9.
- [x] `RoleGuard` en el frontend usando `GET /api/usuarios/me` (PR #16 fusionado) — `UsuariosService` (Signal de solo lectura, nunca lanza) + `RoleGuard` (`CanActivateFn` parametrizado por rol). 3 tests unitarios (sin sesión, rol incorrecto/403, rol correcto). No se agregó ninguna ruta real de administración todavía (a propósito, ver `TODO.md`). Verificación "en vivo" satisfecha combinando el 401/403/200 ya probado en vivo de `GET /api/usuarios/me` (tarea anterior) con los tests unitarios del guard, en vez de desplegar una ruta de prueba desechable — decisión del usuario, ver §9.
- [x] `CatalogoPublicoComponent` — consumo real de `GET /api/libros` desde el frontend, SSR, ruta `/` (PR #17 fusionado) — `LibrosService` (Signal) + `CatalogoPublicoComponent` + `PvpPipe` (formato `$45.000`). Corrige de paso un bug real de SSR (URLs relativas de `HttpClient` no resuelven en el Lambda `ssr`) con `absoluteUrlInterceptor`. Ver detalle en §7 y §9.
- [x] `POST /api/libros` — catalogar un libro, backend protegido con rol `vendedor`/`administrador` (PR #18 fusionado) — nueva función Lambda `catalogarLibro` (ADR-008), valida el body (`pvp`/`porcentajeDescuentoEditorial`/`cantidadTotal`), genera `bookId`/`costo`/`utilidadCatalogo`/`creadoPor` en el backend. Primer endpoint del proyecto cubierto con tests unitarios reales (`npm run test:api`, Vitest, 11 tests). Verificado en vivo contra `staging`: `401`/`403`/`201` confirmados con `curl` + un ID Token real (operación `probar-catalogar-libro`), datos de prueba limpiados. **Incidente real en el mismo PR:** un deploy posterior dejó `GET /api/libros` respondiendo `500` (`package.patterns` incompleto — ver gotcha crítico en §7); detectado y corregido antes de fusionar. Ver detalle en §7 y §9.
- [x] CRUD `/api/estantes` (administrador) (PR #19 fusionado) — un solo handler para los 4 verbos, exige rol `administrador` exclusivamente (a diferencia de `POST /api/libros`, `vendedor` no basta). `PUT`/`DELETE` verifican que el `estanteId` exista (`404` si no). Nueva función `escanearTodo<T>` en `dynamodb.ts`. 13 tests unitarios. Verificado en vivo: `401` sin token, `403` con `vendedor`, ciclo completo `GET`(200)→`POST`(201)→`PUT`(200)→`GET`(200, aparece actualizado)→`DELETE`(204) con `administrador`, datos de prueba limpiados (operación `probar-estantes`, mismo mecanismo de ID Token real). Aplicada de entrada la lección del incidente de `POST /api/libros`: `verificar-token.js` incluido en `package.patterns` desde el primer commit. Ver detalle en §7 y §9.
- [x] `POST /api/ventas` — registrar una venta, backend protegido (PR #21) — nueva función Lambda `registrarVenta` (ADR-008), exige rol `vendedor`/`administrador`, nunca confía en `pvp`/`costoLibro` del cliente (lee el `Libro` real, `CLAUDE.md` A08), decrementa `cantidadDisponible` de forma condicional (`decrementarSiPositivo`, nueva función en `dynamodb.ts` con `ConditionExpression`) para evitar sobrevender. Snapshot de `precioFinal`/`utilidad` calculado y persistido siempre en el backend. 11 tests unitarios. Verificado en vivo de punta a punta: `401` sin token, `403` con un correo sin fila en `babel-usuarios-staging` (detectado ya en el primer `POST /api/libros` del script de prueba, sin llegar a intentar la venta — cero efectos secundarios), y con un usuario `vendedor` sembrado: se catalogó un libro de prueba (`201`), se vendió (`201`, snapshot correcto: `pvp` 50000, `costoLibro` 32500, `precioFinal` 45000 con 10% de descuento, `utilidad` 12500), `cantidadDisponible` confirmado en 0 tras la venta (`cantidadTotal` era 1), y los tres registros de prueba (libro, venta, usuario de Firebase Auth) se limpiaron solos. Ver detalle en §9.
- [x] CRUD `/api/usuarios` (administrador) (PR #22) — un solo handler para los 4 verbos, exige rol `administrador` exclusivamente (mismo patrón que `estantes.ts`). `POST` responde `409` si el email ya existe en vez de sobrescribir (ADR-009). **Salvaguarda de auto-degradación/auto-eliminación (ADR-009):** un administrador nunca puede cambiar su propio rol ni eliminarse a sí mismo vía este endpoint — `400` explicativo en ambos casos. 22 tests unitarios. Verificado en vivo: `401` sin token, `403` con un correo sin fila en `babel-usuarios-staging` (script se detiene ahí), y con un `administrador` sembrado: `PUT`/`DELETE` sobre el propio email → `400` (ambas salvaguardas confirmadas), ciclo completo `GET`(200)→`POST`(201)→`PUT`(200)→`GET`(200, aparece actualizado)→`DELETE`(204) sobre un usuario objetivo de prueba distinto, limpiado solo. Ver detalle en §9.
- [x] Catalogación manual en el frontend (PR #23) — `EstantesService` (cliente autenticado de `GET /api/estantes`) + `CatalogarLibroComponent` (formulario reactivo, todos los campos de `POST /api/libros`, limpia tras guardar conservando `porcentajeDescuentoEditorial`), `RoleGuard` extendido para aceptar un rol o una lista de roles válidos. **Verificado en vivo por el usuario con un login real de Google en su propio navegador** (primera vez en el proyecto — toda verificación anterior de rutas protegidas usó `curl` o tests unitarios, nunca un login interactivo real, ver §9): confirmó que un libro queda correctamente catalogado de punta a punta. La verificación destapó y permitió corregir, dentro de la misma tarea, dos bugs reales que ningún `curl`/test unitario anterior podía detectar — ver §7 (gotcha crítico de SSR/Firebase Auth) y §9 para el detalle completo de ambos.
- [x] CRUD `/api/editoriales-descuentos` (administrador) (PR #24) — un solo handler para los 4 verbos (mismo patrón que `estantes.ts`/`usuarios.ts`), exige rol `administrador` exclusivamente. `editorial` es la clave primaria de `babel-editoriales-descuentos`, no se genera. `POST` responde `409` si la editorial ya existe en vez de sobrescribir en silencio (misma decisión que ADR-009, por consistencia). 19 tests unitarios nuevos (78 totales en `test:api`, antes 59). Verificado en vivo: `403` con un correo sin fila en `babel-usuarios-staging` (script se detiene ahí), y con ese correo sembrado como `administrador`: ciclo completo `GET`(200 `[]`)→`POST`(201)→`PUT`(200)→`GET`(200, `porcentajePorDefecto` actualizado a 40)→`DELETE`(204), datos de prueba limpiados. Ver §7 y §9.
- [x] `PATCH /api/libros/:bookId/estante` — cambio de estante de un libro catalogado, backend (PR #25) — `handlerCambiarEstante` agregado a `server/api/handlers/libros.ts` (mismo archivo que `handler`/`handlerCrear`), exige rol `vendedor` o `administrador`, valida que el `bookId` exista (`404`) y que el `estanteId` exista en `babel-estantes` (`400`) antes de actualizar. Corrige de paso el path desactualizado de `tech-specs.md` (`:isbn` → `:bookId`, la clave primaria real de `babel-libros`). Nueva función Lambda `cambiarEstanteLibro` (ADR-008) con rol IAM propio. 6 tests unitarios nuevos (84 totales en `test:api`, antes 78). Verificado en vivo: `403` con un correo sin fila en `babel-usuarios-staging` (los dos estantes desechables del script se limpiaron igual, confirmando que la limpieza funciona aunque el ciclo se corte temprano), y con ese correo sembrado como `vendedor`: `POST`(201, libro creado en el estante origen)→`PATCH`(200, estanteId actualizado al destino)→`GET`(200, confirma el estanteId nuevo), datos de prueba limpiados. Ver §9.
- [x] `GET /api/ventas` (administrador) — listar/filtrar ventas para reportes, backend (PR #26) — `handlerListar` agregado a `server/api/handlers/ventas.ts` (mismo archivo que `handler` de `POST /api/ventas`), exige rol `administrador` exclusivamente. Filtros opcionales `desde`/`hasta` (sobre `vendidoEn`), `formaDePago`, `editorial` (resuelto contra `Libro.editorial` por `bookId`, solo `GetItem` puntuales). **Hallazgo de infraestructura:** el GSI `vendidoEn-index` de `babel-ventas` solo tiene `vendidoEn` como partición (sin sort key), así que no admite `Query` por rango pese a que `tech-specs.md` lo describe para eso — se usa `Scan` + filtrado en memoria (`escanearTodo`), mismo criterio ya aceptado para tablas pequeñas. Nueva función Lambda `listarVentas` (ADR-008) con rol IAM de solo lectura. 13 tests unitarios nuevos (97 totales en `test:api`, antes 84). Verificado en vivo: `403` con un correo sin fila en `babel-usuarios-staging`, y con ese correo sembrado como `administrador`: ciclo completo con 2 libros/ventas de prueba (editoriales y formas de pago distintas), confirmando el listado sin filtro y cada filtro por separado, datos de prueba limpiados. Ver §7 y §9.
- [x] `CambiarEstanteComponent` — frontend de consumo de `PATCH /api/libros/:bookId/estante` (PR #28) — `ListaLibrosCatalogadosComponent` (antes placeholder del scaffold) ahora lista el catálogo real con un enlace "Cambiar estante" por libro; `CambiarEstanteComponent` (`src/app/features/libros/`, ruta `/libros/:bookId/estante`, guardada solo con `AuthGuard`) resuelve el libro actual reutilizando `LibrosService.libros()` (el catálogo público ya cargado, vía `computed()`) en vez de agregar un nuevo endpoint de detalle por `bookId` — deliberadamente, ya que `GET /api/libros` no tiene endpoint de detalle y no hacía falta uno solo para esta pantalla. El `<select>` de estante se pobla con `EstantesService` (ya existente); el `PATCH` autenticado se hace inline en el componente (mismo patrón que `CatalogarLibroComponent`, sin crear un método nuevo en `LibrosService` para no mezclar cliente autenticado/no-autenticado en el mismo servicio). `RenderMode.Client` en `app.routes.server.ts` para la ruta nueva (mismo motivo que `/libros`/`/catalogar`, ver §7). 10 tests unitarios nuevos (`CambiarEstanteComponent` 6, `ListaLibrosCatalogadosComponent` 4). **Verificación:** el usuario eligió explícitamente "Combinar evidencia existente" — el backend `PATCH /api/libros/:bookId/estante` ya estaba verificado en vivo contra `staging` de punta a punta (PR #25), combinado con los 10 tests unitarios nuevos del frontend, en vez de requerir un login real de Google en este entorno sandbox. Ver §9.
- [x] Sección `/admin` — placeholder de navegación + `RoleGuard('administrador')` (PR #29) — `AdminInicioComponent` (`src/app/features/admin/`), índice con las 4 secciones futuras (Estantes, Usuarios, Editoriales, Reportes), ninguna implementada todavía — enlaces sin `routerLink` a propósito. Ruta `/admin` en `app.routes.ts` guardada con `RoleGuard('administrador')` (ya existente, sin cambios de lógica) y `RenderMode.Client` en `app.routes.server.ts` (mismo motivo que `/libros`/`/catalogar`). Header (`App`) extendido: nuevo `computed()` `esAdministrador` que depende tanto de `authService.usuario()` como de `usuariosService.usuarioActual()?.rol`, para que el enlace "Administración" desaparezca de inmediato al cerrar sesión sin depender de limpiar el Signal de `UsuariosService` (que no se resetea solo). Un `effect()` nuevo en el constructor de `App` llama `usuariosService.obtenerUsuarioActual()` cada vez que `authService.usuario()` deja de ser `null` (login), para resolver el rol sin esperar a que el usuario navegue a una ruta guardada por `RoleGuard`. 7 tests unitarios nuevos (`App` 5, `AdminInicioComponent` 2 — 51 tests totales de frontend, antes 45). **De paso, se sembró la primera cuenta `administrador` real y persistente del proyecto** (`letiende.co@gmail.com`, vía la operación `sembrar-usuario-prueba` de `operaciones-staging.yml`, sin ejecutar la limpieza — a diferencia de todo uso anterior de esa operación, que siempre fue desechable). **Verificación:** el usuario eligió explícitamente "Verificación manual tuya" (a diferencia de la tarea anterior) — probó en vivo contra `staging` con esa cuenta real: confirmó el enlace "Administración" y el acceso a `/admin` como administrador, y la ausencia del enlace + bloqueo de acceso directo como `vendedor`. Ver §9.
- [x] Escaneo de ISBN con cámara en `CatalogarLibroComponent` (PR #30) — botón "Escanear ISBN" activa la cámara trasera (`@zxing/browser`, `facingMode: 'environment'`) solo con gesto explícito del usuario (`getUserMedia`, iOS Safari — `CLAUDE.md` §7); al detectar un EAN-13 completa el campo `isbn` del formulario reactivo y libera la cámara (`stop()`, también en `ngOnDestroy` si el usuario navega a mitad de un escaneo). Sin permiso/cámara: mensaje de error visible, sin bloquear la entrada manual del ISBN. 4 tests unitarios nuevos con `BrowserMultiFormatReader` mockeado como función constructora (55 tests totales de frontend, antes 51). **Incidente real de infraestructura en el mismo PR (no del componente):** el deploy a `staging` falló (`EstantesLambdaFunction`, límite de AWS de 262.144.000 bytes descomprimidos excedido) — `@zxing/browser` (30MB) fue la gota que rebasó un `node_modules` de ~570MB que ya arrastraba `@angular`, el SDK cliente de `firebase` (no `firebase-admin`), `rxjs` y herramientas de desarrollo en las 9 funciones Lambda que empaquetan `node_modules/**` completo. Corregido con exclusiones verificadas una por una con `npm ls <paquete>` contra el árbol real de dependencias (`serverless.yml`) — cada función bajó a ~103MB; el caso delicado fue `@firebase/**` (`firebase-admin` sí necesita 9 subpaquetes suyos vía `@firebase/database-compat`, Realtime Database, aunque Babel no lo use). Verificado con `serverless package` + `require()` real del paquete podado (mismo tipo de riesgo que el incidente de `Runtime.ImportModuleError` ya documentado, ver §7) antes de hacer push. **Verificación:** el usuario desplegó y probó en vivo desde su celular con cámara real — confirmó que el escaneo funciona — y fusionó el PR directamente sin pedir revisión previa (a diferencia de todas las tareas anteriores). Ver §7.
- [x] `GestionEstantesComponent` — CRUD real de estantes en `/admin/estantes` (PR #32 fusionado) — primera pantalla real de administración: `EstantesService` extendido con `crearEstante`/`actualizarEstante`/`eliminarEstante` (autenticados; el backend `CRUD /api/estantes` ya existía), componente standalone con un único formulario reactivo reutilizado para crear/editar (`estanteEditandoId`), borrado con `confirm`, mensajes de éxito/error; ruta `/admin/estantes` (`RoleGuard('administrador')`, `RenderMode.Client`); card "Estantes" activada en `AdminInicioComponent`. 19 tests unitarios nuevos (74 totales de frontend). Verificado en vivo por el usuario contra `staging` con la cuenta `administrador` real: "todo funciona perfecto".
- [x] Autocompletar metadatos del libro (título/autor/editorial/portada) por ISBN vía `api.letiende.co` (PR #33 fusionado) — `GET /api/metadatos/:isbn` (función Lambda `metadatos`, exige `vendedor`/`administrador`) que consume el contrato **real** de la API externa, verificado en vivo inspeccionando su código Lambda y probándola: `GET api.letiende.co/libros?barcode=<isbn>` (pass-through de Google Books, no `/metadatos/:isbn` como decía `tech-specs.md`). La API externa es intermitentemente inestable (`503` aun con ISBNs válidos) → el cliente `server/api/services/api-letiende.ts` **nunca lanza** y **reintenta una vez** (800ms) antes de devolver "no encontrado" (ver §7). `CatalogarLibroComponent` dispara la búsqueda al escanear o al perder el foco el campo ISBN, y pre-carga solo los campos vacíos (nunca pisa lo que el vendedor ya escribió, `CLAUDE.md` A08). 28 tests nuevos (109 backend / 81 frontend). Verificado en vivo por el usuario; el PVP sigue siendo **manual** (su automatización es la iniciativa siguiente).
- [x] CRUD de sitios de scraping en `/admin/sitios` — lista única `info`/`pvp` (PR #35, iniciativa "obtención automatizada de info de libros", Task A / ADR-010) — tabla `babel-sitios-scraping` (PK `dominio`, suministrado por el administrador), handler con el mismo patrón que `editoriales-descuentos.ts` (`409` en duplicado, `404` en PUT/DELETE inexistente), `GestionSitiosScrapingComponent` (formulario único crear/editar, `dominio` no editable tras crear, checkboxes `info`/`pvp`). Solo datos + administración: nunca hace peticiones salientes (eso es la Tarea 1 siguiente, el motor de scraping). Los 4 sitios semilla (Librería Lerner, Tornamesa, Librería Nacional, Busca Libre) se sembraron directamente vía `aws dynamodb put-item` en vez de la UI, por los incidentes de infraestructura del mismo día (ver abajo y §7).
- [x] Fix del selector de cuentas de Google en Chrome de escritorio (PR #36) — `GoogleAuthProvider.setCustomParameters({ prompt: 'select_account' })` en `AuthService.iniciarSesionConGoogle()`; sin ese parámetro, Chrome de escritorio auto-seleccionaba la sesión de Google "activa" sin mostrar el selector (Chrome Android sí lo mostraba). Reportado por el usuario, que necesitaba elegir `letiende.co@gmail.com` entre varias cuentas para probar funciones de administrador.
- [x] Guarda de concurrencia en el deploy a producción + redeploy de `staging` (PR #38) — corrige dos incidentes reales de infraestructura del mismo día (ver detalle completo en §7): (1) el stack compartido de `staging` perdió la tabla/función/rol de `sitios-scraping` DOS veces por deploys de ramas que no incluían esos cambios aún sin fusionar; (2) fusionar 3 PRs en <1 minuto disparó 3 deploys a producción en paralelo sin `concurrency`, 2 de 3 fallaron por choque de CloudFormation (producción sobrevivió por timing, no por diseño). Se agregó `concurrency` (grupo `desplegar-produccion`) al job de producción en `deploy.yml`, mismo patrón que ya protegía a `staging`.
- [x] Motor de scraping por ISBN + guardia SSRF fija (PR #39, iniciativa "obtención automatizada de info de libros", Task B / ADR-011) — `server/api/services/scraping.ts`: `esUrlSegura`/`fetchSeguro` (exige `https:`, resuelve DNS real, rechaza IPs privadas/loopback/link-local incluido `169.254.169.254`, revalida cada redirección antes de seguirla) + 4 adaptadores con contrato real verificado en vivo (Librería Lerner y Librería Nacional vía la API pública VTEX; Tornamesa con búsqueda HTML + JSON-LD `Book`; Busca Libre con JSON-LD `Product` y redirección). `scrapearSitio()` nunca lanza; un dominio sin adaptador se omite (ADR-010). 158 tests (`test:api`). Aún **sin ningún consumidor** — no está integrado en `/api/metadatos/:isbn` (eso es Task C, sigue pendiente). El usuario reordenó las prioridades de los 4 sitios desde `/admin/sitios` en `staging` y confirmó que el panel funciona bien.
- [x] Reordenamiento arrastrable en `/admin/sitios` — el campo `Prioridad` manual desaparece de los formularios de crear/editar sitios de scraping; el listado "Sitios registrados" (ahora ordenado por `prioridad` ascendente, antes sin orden garantizado) se reordena arrastrando filas (`@angular/cdk` `DragDropModule`, `cdkDropList`/`cdkDrag`), que renumera y persiste solo los sitios cuyo orden cambió (`Promise.all` sobre `actualizarSitio`, en paralelo). Al crear un sitio se le asigna `prioridad = máxima existente + 1` automáticamente; al editar, se preserva sin cambios — el backend sigue exigiendo el campo (`server/api/handlers/sitios-scraping.ts`, sin tocar), solo se ocultó el control del formulario. El formulario ahora está oculto por defecto (se abre con "Editar" o el nuevo botón "Agregar") y el listado aparece antes que el formulario en el layout — pedido explícito del usuario tras probar el panel en vivo. 113 tests de frontend. `@angular/cdk` agregado como dependencia; se subió el presupuesto de bundle inicial en `angular.json` (500kB→600kB warning, 1MB→1.2MB error) porque el bundle ya rondaba los límites previos incluso antes de este cambio.
- [x] **Iniciativa "obtención automatizada de info de libros" — COMPLETA en su alcance acordado** (`plan-obtencion-info-libros.md`, ADR-010/ADR-011). `GET /api/metadatos/:isbn` ahora orquesta `api.letiende.co` (siempre primero) + el fallback de scraping (`scrapearSitio`, PR #39) para lo que siga faltando — incluido `pvp`, que `api.letiende.co` nunca resuelve (Google Books no maneja precios). Los sitios aplicables (filtrados por sus banderas `info`/`pvp`) se consultan **en paralelo** (`Promise.all`) y se fusionan respetando `prioridad` ascendente como desempate — el orden de llegada de red nunca decide (verificado con un test que invierte deliberadamente el orden de resolución respecto a la prioridad). `timeout` de la función subido a 25s (el peor caso en paralelo es ~16s, el sitio más lento, no la suma de todos — con esto ya no aplica el riesgo de +30s que tendría una resolución secuencial). `CatalogarLibroComponent` pre-carga `pvp` en el campo ya existente, mismo criterio "nunca pisa lo ya escrito" que el resto de campos (para un número, "vacío" es el valor por defecto `0`). 163 tests backend / 116 frontend. Con esto, las 3 tareas de la iniciativa (Task A: CRUD de sitios; Task B: motor de scraping; Task C: integración) están completas y verificadas — queda diferida, a propósito, la búsqueda por título/autor + UI de selección de candidato (fuera del alcance acordado con el usuario).
- [x] `GestionUsuariosComponent` — CRUD real de usuarios en `/admin/usuarios` — `UsuariosService` extendido con `cargarUsuarios`/`crearUsuario`/`actualizarUsuario`/`eliminarUsuario` (patrón `SitiosScrapingService`, `email` como clave primaria no editable tras crear); `obtenerUsuarioActual()` (usado por `RoleGuard`) intacto. **Salvaguarda visual de ADR-009:** compara cada fila contra `authService.usuario()?.email` — en la fila propia deshabilita "Eliminar" y, al editarla, deshabilita el `<select>` de rol (queda fijo en `administrador`), anticipando el `400` del backend en vez de que el usuario lo descubra por un error. 144 tests de frontend.
- [x] `DescuentosEditorialesComponent` — CRUD real de descuentos por editorial en `/admin/editoriales` — `EditorialesDescuentosService` nuevo (patrón `UsuariosService`, `editorial` como clave primaria no editable tras crear). `porcentajesDisponibles` (`number[]`) se edita como input de texto con números separados por coma (único patrón del proyecto para "array editable en un formulario reactivo" — sin precedente previo, documentado en el componente), validado declarativamente (segmento no numérico o fuera de 0-100 invalida el control) y parseado a `number[]` solo al guardar. Con esto, los 4 CRUD de administración con backend ya listo quedan completos: `GestionEstantesComponent` (PR #32), `GestionSitiosScrapingComponent` (PR #35), `GestionUsuariosComponent`, `DescuentosEditorialesComponent`. 172 tests de frontend.
- [x] `GET /api/ventas/exportar` (XLSX) + `ReportesVentasComponent` — `handlerExportar` nuevo en `ventas.ts` (mismo archivo que `handler`/`handlerListar`, ADR-008), reusa una función compartida nueva (`consultarVentasFiltradas`) que resuelve título/editorial por `bookId` SIEMPRE (antes solo cuando se filtraba por editorial) — `handlerListar` despoja esos 2 campos antes de responder para no romper su contrato JSON ya existente. Genera el `.xlsx` en memoria con la librería `xlsx` (nueva dependencia de producción, ADR-004) vía `XLSX.utils.json_to_sheet` + `XLSX.write(..., {type:'base64'})`, columnas fecha/título/editorial/ISBN/PVP/costo/utilidad/forma de pago; un `Libro` faltante para un `bookId` histórico usa `'—'` de respaldo en vez de romper el reporte (CLAUDE.md A08). `VentasService` (frontend, nuevo) pide el archivo como `blob` autenticado y dispara la descarga con un `<a>` temporal. `ReportesVentasComponent` (nuevo, `/admin/reportes`): formulario de filtros + botón "Exportar a Excel", sin listar ventas en pantalla. Función Lambda `exportarVentas` en 131MB. 182 tests frontend / 170 backend.
- [x] `DESIGN.md` propio de Babel (PR #46 fusionado) — documenta, sin inventar patrones nuevos, la identidad de marca ya vigente (colores/tipografía/formato de precio, fuente de verdad `src/styles.css`), estructura de página, componentes reutilizables (tarjetas, botones, inputs, mensajes de éxito/error, estados vacío/carga) y el patrón de formulario único crear/editar — cada uno citando `archivo:línea` real de los 10 componentes ya construidos. Documenta explícitamente los desvíos existentes entre componentes (tarjeta de login con `p-8`/sombra más intensa, `GestionEstantesComponent` sin el patrón formulario oculto, mensajes de éxito/error centrados vs. alineados a la izquierda) sin corregirlos. `CLAUDE.md` §4 actualizado para referenciarlo en vez de "se recomienda crear".
- [x] Búsqueda por título/autor + UI de selección de candidato al catalogar (PR #48) — última pieza, antes diferida a propósito, de la iniciativa de obtención automatizada de info de libros. Contrato confirmado en vivo antes de implementar: `api.letiende.co/libros?titulo=&autor=` devuelve `{items:[...]}` con múltiples `volumeInfo` (la mayoría SIN `industryIdentifiers` ISBN_13); sin resultados responde `200` con `null` (no `{}`). Lerner/Nacional (VTEX `ft=`) soportan texto libre multi-palabra — 2 gotchas reales encontrados y manejados: un `+` en la query dispara el WAF del sitio (`400`, hay que usar `%20`/`encodeURIComponent`, nunca `URLSearchParams` contra estos 2 dominios) y el campo `ean` no es un ISBN limpio (trae sufijo de SKU, se valida a 13 dígitos). Tornamesa/Busca Libre quedan fuera de la lista de candidatos a propósito (sus páginas de búsqueda no traen los datos en la misma respuesta, costo de N+1 no justificado), pero Tornamesa sí se usa como fallback de PVP (ver abajo). Backend: `GET /api/metadatos/buscar?titulo=&autor=` (función Lambda `metadatosBuscar`, rol IAM propio) orquesta api.letiende.co + Lerner + Nacional en paralelo, hasta 20 candidatos `{titulo, autor, editorial, portadaUrl, isbn}`, sin `pvp`. **Ajuste pedido tras la primera versión:** para un candidato SIN ISBN, el PVP se resuelve en un segundo paso — `GET /api/metadatos/buscar-pvp?titulo=&autor=` (función Lambda propia `metadatosBuscarPvp`) busca en Lerner y Nacional en paralelo y, si ninguno encuentra precio, intenta Tornamesa como último recurso (reutiliza `scrapearTornamesa` refactorizado para aceptar cualquier texto, no solo ISBN). Frontend: `CatalogarLibroComponent` gana un botón "Buscar por título y autor" (visible sin ISBN) con lista de candidatos — muestra portada, título, autor, editorial e ISBN (los 2 últimos agregados tras la primera versión, para distinguir ediciones similares). **Al seleccionar un candidato se SOBRESCRIBEN todos los campos del formulario** (no solo los vacíos, ajuste pedido tras la primera versión: es una elección explícita del vendedor sobre ese libro exacto) — si el candidato trae ISBN, además dispara el flujo ya existente de `/api/metadatos/:isbn` (también con sobrescritura) para refinar los datos con la ficha confirmada por ISBN, más precisa que el resultado de texto libre. El escaneo/entrada manual de ISBN NO cambia de comportamiento (sigue sin pisar campos ya escritos, ahí la búsqueda es automática sin confirmación del vendedor) — `buscarYPrecargarMetadatos` ganó un parámetro `sobrescribir` para distinguir ambos casos sin duplicar lógica. 722+ líneas de tests nuevos entre backend/frontend. Verificado por el usuario en sandbox ("ya funciona bien"); pendiente verificación en vivo contra `staging` con un libro real sin ISBN a mano.
- [x] Búsqueda por título/autor/ISBN en el catálogo público (PR #49) — filtro resuelto en el CLIENTE, sobre el `libros()` signal que `LibrosService` ya carga completo (decisión documentada en `catalogo-publico.component.ts`): DynamoDB no soporta bien texto libre, así que filtrar en el backend no reduciría el costo del `Scan` de `GET /api/libros`, solo el tamaño de la respuesta; y el catálogo ya viaja completo al cliente hoy sin paginación, por lo que filtrar en el cliente no agrega ninguna petición nueva. "Tema" (mencionado en `PRD.md` §5.7) confirmado que NO existe como campo del modelo `Libro` — alcance real título/autor/ISBN. Búsqueda sin distinguir mayúsculas ni tildes (`normalize('NFD')` + strip de diacríticos). Mensaje neutral distinto para "catálogo vacío" vs. "sin resultados de búsqueda". El volumen del payload completo (3.000+ libros sin paginar) queda documentado como fuera de este alcance — característica previa de `GET /api/libros`, no introducida por esta tarea.
- [x] Ficha de libro pública en `/libro/:bookId` (PR #51) — cerraba el roadmap Alta/Media original completo. Nueva función Lambda `librosDetalle` (rol IAM propio de solo lectura), pública sin autenticación, resuelve la ubicación física con un `GetItem` puntual a `babel-estantes` (en ese momento; reemplazado por Espacio/Mueble/Ubicación en la Tarea C del nuevo backlog, ver abajo). A diferencia del listado, no filtra por `cantidadDisponible` (decisión deliberada, confirmada de nuevo al revisar `ajustes-finales.md`: un enlace directo no debe romperse aunque el libro esté agotado). **Bug real detectado después:** `Title` (Angular) es singleton — la ficha lo sobreescribe con el título del libro y `CatalogoPublicoComponent` nunca lo resetea, así que el título de pestaña queda pegado al último libro visitado; corrección es la Tarea 1 del backlog nuevo.

**Repriorización mayor (2026-07-23):** con el roadmap original completo, el usuario entregó `ajustes-finales.md` — un conjunto grande de ajustes de producto (detalle completo en ese archivo y en `PRD.md`, ya actualizado): ubicación física pasa de una sola entidad "Estante" a una jerarquía de 3 (Espacio→Mueble→Ubicación) con CRUD independiente; los libros catalogados se pueden **editar** por completo (no solo cambiar ubicación) y **eliminar** (admin); la venta se registra desde la ficha del libro vía un botón "Vender" (primera UI real para `POST /api/ventas`, hasta ahora sin frontend); nueva área "Gestionar" (vendedor+admin) con pestañas Catalogar/Editar, reemplazando `/libros` y `/catalogar` como destinos separados; el catálogo público suma filtro por ubicación (acumulativo, navegable por URL, listo para QR); nuevo reporte de Inventario XLSX. **Decisión explícita del usuario: modo offline y despliegue a producción quedan bloqueados hasta cerrar TODO este conjunto** — se rompió en 7 tareas atómicas (Tarea A–G) documentadas en `ajustes-finales.md` §"Backlog ordenado de implementación", con `TODO.md` manteniendo las 2 primeras activas. 5 ambigüedades reales del documento se resolvieron con el usuario antes de tocar el PRD (forma de pago faltante en el diálogo Vender, cantidad>1 por venta, comportamiento de la ficha agotada, datos de prueba de staging, alcance de "QR por mueble") — ver `ajustes-finales.md` §"Decisiones técnicas confirmadas".

- [x] Tarea A del backlog — fixes rápidos del catálogo público (PR #53) — corrige el bug real de `Title` (singleton, quedaba pegado al título del último libro visitado), cambia el texto tras el logo a "Catálogo Librería", agrega vínculo "Volver al catálogo" en `/login`. Primer spec de `LoginComponent` (no tenía cobertura previa).
- [x] Tarea B del backlog — modelo jerárquico Espacio→Mueble→Ubicación (PR #54) — reemplaza el CRUD de "Estante" (una sola entidad de texto libre) por 3 entidades independientes con FK reales: `server/api/handlers/ubicacion-fisica.ts` (3 handlers, `GET` público sin token — a diferencia de `GET /api/estantes` — `POST`/`PUT`/`DELETE` exigen administrador), 3 tablas DynamoDB + 3 roles IAM de mínimo privilegio (cada uno con acceso cruzado de solo lectura al padre/hijo necesario para validar existencia y reglas de borrado sin huérfanos). Pantalla nueva `/admin/ubicaciones` (ruta renombrada, no se mantuvo `/admin/estantes` — nombre más preciso dado que el contenido ya no es sobre "estantes"), 3 pestañas con cascada Espacio→Mueble en Ubicaciones. **Deliberadamente NO se tocaron** `babel-estantes`/`EstantesService`/`estantes.ts`/`Libro.estanteId`: `CatalogarLibroComponent`/`CambiarEstanteComponent` siguen dependiendo de ellos hasta la Tarea C (siguiente, `TODO.md`) — evita romper el build a mitad de la migración. Solo se borró `GestionEstantesComponent` (la pantalla), no el servicio compartido. **Incidente post-merge:** la descripción de la función `espacios` en `serverless.yml` excedía el límite de 256 caracteres de CloudFormation (`AWS::Lambda::Function.Description`) y hizo fallar el deploy a `staging`; corregido acortándola (ninguna otra función del archivo estaba cerca del límite, verificado).
- [x] Tarea C del backlog — migra `Libro.estanteId` → `Libro.ubicacionId` (PR #56) — `POST /api/libros` valida `ubicacionId` contra `babel-ubicaciones` antes de guardar (mismo criterio que `handlerMuebles`/`handlerUbicaciones` con su padre); la ficha (`handlerDetalle`) resuelve Espacio→Mueble→Ubicación con 3 `GetItem` puntuales (`resolverUbicacion`, nunca `Scan`) y la muestra como 3 campos independientes (antes concatenados con `—` contra `babel-estantes`) — cierra el hallazgo de `ajustes-finales.md`. `EstantesService`/`server/api/handlers/estantes.ts`/CRUD `/api/estantes`/tabla `babel-estantes`/modelo `Estante` eliminados por completo, junto con sus roles IAM. **`handlerCambiarEstante`/`CambiarEstanteComponent`/ruta `/libros/:bookId/estante` no se eliminaron, se renombraron** a `handlerCambiarUbicacion`/`CambiarUbicacionComponent`/`/libros/:bookId/ubicacion` (mismo flujo, apuntando al modelo nuevo) — placeholder mínimo decidido en la propia tarea para no dejar al vendedor sin forma de reubicar un libro hasta que la Tarea E lo reemplace de verdad. **Bug real encontrado después de fusionar (mismo PR, commit adicional):** libros catalogados antes de esta migración no tienen `ubicacionId` — `resolverUbicacion(undefined)` intentaba un `GetItem` con clave `undefined` y DynamoDB lanzaba, devolviendo `500` en `GET /api/libros/:bookId` (confirmado consultando `babel-libros-staging` directamente); corregido devolviendo `null` de inmediato cuando `ubicacionId` no existe, mismo criterio ya usado para cualquier eslabón roto de la cadena. **Hallazgo de proceso (no de código):** con 2 PRs abiertos casi al mismo tiempo, el PR de documentación (#58, rama creada antes de que este PR se fusionara) re-desplegó `staging` con código viejo y pisó el deploy correcto — ver `babel-staging-compartido-race-condition` en memoria del agente; causó una falsa alarma de "el fix no funciona" que se resolvió re-disparando el deploy del PR correcto. El usuario confirmó después que ya funciona correctamente y sembró varios Espacio/Mueble/Ubicación de prueba en `staging`.
- [x] Tarea D del backlog — vender desde la ficha del libro (PR #57) — primera UI real de `POST /api/ventas`. Backend: acepta `cantidad` (entero ≥ 1, requerido, sin default); `decrementarSiPositivo` → `decrementarPorCantidadSiSuficiente` en `dynamodb.ts` (`ConditionExpression` por `cantidad`, rechazo atómico sin condición de carrera); `Venta.cantidad` nuevo; `precioFinal`/`utilidad` pasan a representar el TOTAL de la transacción (`pvp`/`costoLibro` siguen siendo snapshot unitario) — 1 solo registro de `Venta` representa N ejemplares, no N registros. Frontend: botón "Vender" en `LibroDetalleComponent`, mismo patrón de guard visual que `App.esAdministrador` (`authService.usuario() !== null && rol vendedor/administrador`), oculto si `cantidadDisponible = 0`; diálogo con cantidad (máximo = disponible)/% descuento/forma de pago (obligatoria, mismo enum de 5 formas ya usado en Reportes); tras confirmar, refresca la ficha sin ocultarla (evita parpadeo). Nuevo `VentaService.registrarVenta(...)` (`src/app/core/api/venta.service.ts`) — servicio separado de `VentasService` (que solo cubre reportes/exportación), mismo criterio de un servicio por endpoint ya usado en el resto de `core/api/`. Verificado en vivo por el usuario tras sembrar la primera Ubicación en `staging` (el botón no aparecía antes por falta de esa data, no por un bug de código).
- [x] Tarea E del backlog — área "Gestionar" (PR #60) — nueva ruta `/gestionar` (`GestionarComponent`, `RoleGuard(['vendedor','administrador'])`), 2 pestañas reemplazando `/libros` y `/catalogar` como destinos separados. **Catalogar** reutiliza `CatalogarLibroComponent` (movido de `features/catalogar/` a `features/gestionar/`) con el panel "Ubicación del libro" (cascada Espacio→Mueble→Ubicación) ahora persistente entre catalogaciones seguidas (ya no se limpia al guardar, solo el formulario del libro) y autocompletado de `porcentajeDescuentoEditorial` por coincidencia de nombre de editorial. **Editar** (`EditarLibroComponent`) lista el inventario completo vía `GET /api/libros/inventario` (nuevo `handlerInventario`, ruta estática sin conflicto con `/api/libros/{bookId}` — mismo patrón que `/api/usuarios/me`; a diferencia del catálogo público, incluye libros agotados) con filtro por título/autor/ISBN (reutiliza el lector de código de barras), edición de Espacio/Mueble/Ubicación/cantidad/PVP/descuento y "ELIMINAR LIBRO" (guard visual + backend revalida, solo administrador). Backend: `PUT /api/libros/:bookId` (`handlerEditar`, vendedor/administrador — recalcula `costo`/`utilidadCatalogo`, ajusta `cantidadDisponible` por delta recortado a `[0, nuevaCantidadTotal]` para preservar lo ya vendido) y `DELETE /api/libros/:bookId` (`handlerEliminar`, administrador exclusivo) reemplazan por completo a `handlerCambiarUbicacion`. `GET /api/editoriales-descuentos` se abrió también a `vendedor` (antes solo `administrador`) para que el autocompletado funcione desde Catalogar. Header: "Mi cuenta" → "Gestionar" apuntando a `/gestionar`. Eliminados: `ListaLibrosCatalogadosComponent`/`/libros`, `CambiarUbicacionComponent`/`/libros/:bookId/ubicacion`, y `AuthGuard` (sin ninguna ruta que lo usara tras retirar `/libros`). 262 tests de backend, 288 de frontend, todo en verde antes del PR. **Nota de proceso:** esta tarea la implementó y fusionó otra sesión (posiblemente en otro dispositivo) mientras esta sesión, con el `TODO.md` desactualizado en su contexto, reimplementaba lo mismo desde cero sin saberlo — al descubrir el PR #60 ya fusionado (aviso del usuario), el trabajo duplicado (sin commitear) se descartó y el repositorio local se resincronizó con `origin/main` desde cero, incluida la limpieza de 10 ramas locales obsoletas ya fusionadas. Lección: con varias sesiones trabajando el mismo repo en paralelo, `git fetch`/`pull` de `main` y una relectura de `TODO.md` deberían preceder cualquier trabajo nuevo, no solo confiar en el estado ya cargado en el contexto de la conversación. La verificación en vivo contra `staging` quedó marcada "pendiente" en el cuerpo del PR #60 al momento de fusionarlo — el usuario fusionó de todas formas; no hay confirmación explícita post-merge registrada en este documento.

- [x] Tarea F del backlog — filtrado público por ubicación (PR #62) — `CatalogoPublicoComponent` agrega 2 `<select>` (Espacio, Mueble dependiente del Espacio elegido), acumulativos con la búsqueda de texto ya existente. Resuelve `Libro.ubicacionId → Ubicacion.muebleId → Mueble.espacioId` con 2 `Map` en memoria (sin llamada nueva por libro), reutilizando `UbicacionFisicaService` (endpoints públicos ya existentes) sin tocar `GET /api/libros`. Soporta query params `?espacio=<espacioId>&mueble=<muebleId>` en ambas direcciones: pre-filtra al entrar (caso de uso QR) y cambiar un `<select>` actualiza la URL (`router.navigate` con `replaceUrl: true`). 8 tests unitarios nuevos (filtro por Espacio, por Mueble, acumulativo con texto, cascada Mueble↔Espacio con reseteo, pre-selección por query params, actualización de la URL). **Bug real de Angular encontrado y corregido en la misma tarea:** el binding `[value]` en un `<select>` nativo sin Reactive Forms (`ngModel`/`formControlName`) no selecciona la opción correcta si las `<option>` generadas por `@for` todavía no existían en el DOM en el momento en que se aplica el binding — comportamiento nativo del navegador (fijar `.value` en un `<select>` sin una `<option>` coincidente ya presente falla en silencio y no se autocorrige después), no un bug de Angular en sí. Detectado al escribir el test de pre-selección por query params (el valor esperado quedaba en `''`). Corregido reemplazando el binding en el `<select>` por `[selected]` en cada `<option>` — el patrón correcto para selects sin `ngModel`/`formControlName`. **Incidente de proceso resuelto en la misma tarea:** al abrir el PR, GitHub reportó un conflicto de fusión real contra `main` — el PR #61 (revalidación del motor JIT, fusionado mientras esta rama ya existía) y esta rama habían agregado, cada uno, una fila distinta al final de `Hitos desarrollo - detail.csv`. Resuelto con `git merge origin/main` (no `git rebase`, para no reescribir historial ni requerir `--force`, prohibido por `CLAUDE.md`) conservando ambas filas en orden cronológico; build/tests reverificados en verde tras el merge antes de reenviar. Verificado en vivo por el usuario en `staging`.

- [x] Tarea G del backlog — Reporte de Inventario XLSX (PR #63) — nuevo `GET /api/libros/exportar` (`handlerExportarInventario`, `server/api/handlers/libros.ts`, administrador exclusivo, ruta estática sin conflicto con `/api/libros/{bookId}`). Resuelve Espacio/Mueble/Ubicación por libro con 3 `escanearTodo` en paralelo (`Promise.all`, una tabla cada uno) + `Map` en memoria, en vez de repetir `resolverUbicacion` (3 `GetItem`) por cada libro — más barato a este volumen (miles de libros, decenas de ubicaciones distintas). Columnas: ISBN, Título, Autor, Editorial, PVP, Descuento editorial (%), Cantidad, Espacio, Mueble, Ubicación; `Cantidad` reporta `cantidadDisponible` (lo que debería quedar físicamente en el estante), no `cantidadTotal` (decisión documentada en el propio código: un reporte de inventario existe para conciliar contra el conteo físico). Nueva función Lambda `exportarInventario` (ADR-008) con rol IAM de solo lectura. Frontend: `LibrosService.exportarInventario()` (mismo patrón de descarga de blob que `VentasService.exportarVentas`) y una segunda tarjeta "Reporte de inventario" en `ReportesVentasComponent` (`/admin/reportes`), con su propio estado de carga/error/éxito independiente del reporte de ventas. 3 tests de backend + 7 de frontend nuevos (265/302 tests totales). Verificado en vivo por el usuario en `staging` — descarga real del `.xlsx` confirmada. **Cierra el backlog completo de `ajustes-finales.md` (Tareas A–G).**

Estado real del repositorio: el scaffold de Angular, el backend completo de negocio (catálogo público con ficha, búsqueda y filtro por ubicación, catalogación —incluida la obtención automatizada de info de libros por ISBN y por título/autor, PVP incluido—, ventas + reportes XLSX de ventas e inventario, venta desde la ficha del libro, edición/eliminación de libros desde `/gestionar`, los 4 CRUD de administración originales, y el modelo jerárquico Espacio→Mueble→Ubicación con `Libro.ubicacionId` ya migrado) y su infraestructura (múltiples funciones Lambda por endpoint, ADR-008; 8 tablas DynamoDB) están implementados y desplegados en `staging`. Todo el roadmap Alta y Media del PRD está cubierto, y las Tareas A–G del backlog de `ajustes-finales.md` están cerradas por completo. `babel-estantes`/`EstantesService`/`AuthGuard`/`/libros` ya no existen. El usuario ya sembró varios Espacio/Mueble/Ubicación de prueba en `staging`. **Pausa deliberada del motor JIT (`TODO.md` sin tareas activas):** el usuario va a correr una ronda de pruebas manuales de flujos completos en `staging` antes de detallar/arrancar las 2 últimas piezas del roadmap (modo offline/cola de sincronización, primer despliegue a producción — ambas "Baja" en `PRD.md` §6, sin desglose atómico todavía).

- [x] **Generador de QR para muebles — COMPLETO (2026-07-28/29, PR #76 fusionado)** — pista independiente y deliberadamente fuera de Babel (`tools/qr-muebles/`, paquete npm propio, excluido del build/deploy de Angular/Lambda). Lee `babel-espacios-{stage}`/`babel-muebles-{stage}` directo de DynamoDB (perfil AWS `default`, `us-east-1`, sin pasar por la API) y genera un PDF tamaño carta con una estampilla circular de QR por mueble. Dos rondas de trabajo: **(1)** versión inicial con geometría calculada en código (SVG a mano + `@resvg/resvg-js` + `pdf-lib`, ver `plan-herramienta-qr-muebles.md`); **(2)**, tras feedback del usuario, se reescribió sobre un **sistema de plantilla SVG editable** (`plantillas/estampilla.svg`, convención `id="campo:*"` para los 3 placeholders — nombre espacio, nombre mueble, QR — parseado/rellenado con `@xmldom/xmldom`, escalable a cualquier `--diametro` incluso con `viewBox` de origen no-cero como el que produce Inkscape), se agregó el **monograma de Le Tiende embebido en el centro del QR** (`errorCorrectionLevel: 'H'`, ~20% del lado + fondo blanco) y **tipografía Poppins real** embebida (`.ttf` Black/Bold, sin depender de fuentes del sistema). **Hallazgo real de escaneabilidad, no solo cosmético:** las 3 "marcas decorativas" de la primera versión (mal interpretadas de la imagen de referencia del usuario — nunca debieron existir) causaban fallos reproducibles de decodificación con `@zxing/library`; se eliminaron por completo, y de paso se corrigieron dos causas raíz más (DPI 300→600, `margin` del QR 1→4 — ambas insuficientes para decodificadores reales a diámetros pequeños con URLs largas). Se agregó una **verificación automática de escaneo** (`jsqr`, corre en cada `npm run generar`, avisa sin bloquear la corrida) — `@zxing/library` se descartó como dependencia permanente por producir falsos negativos reproducibles en contenido QR válido (limitación propia de esa librería, verificado aislando el PNG crudo del QR sin ningún renderizado de por medio). Un `architect` revisó el código en cada ronda (2 en total) — el único bug real encontrado (escalado de plantilla asumiendo `viewBox` con origen `(0,0)`) se corrigió con test de regresión. **Pendiente, no bloqueante:** el usuario debe escanear al menos una estampilla impresa con un celular real antes de imprimir el lote completo — ningún decodificador de software reemplaza esa prueba.

- [x] **Orden alfabético en Espacio/Mueble/Ubicación — COMPLETO (2026-07-30, PR #78 fusionado)** — fix centralizado en `UbicacionFisicaService.cargarEspacios()`/`cargarMuebles()`/`cargarUbicaciones()`, ordenando por `nombre` (`localeCompare('es', { sensitivity: 'base' })`) antes de exponer los Signals — alcanza sin cambios propios las 4 pantallas que consumen el servicio (`GestionUbicacionFisicaComponent`, `CatalogarLibroComponent`, `EditarLibroComponent`, `CatalogoPublicoComponent`). **Hotfix agregado a la misma PR (pedido del usuario):** en el catálogo público, sin un Espacio elegido, el `<select>` de Mueble mostraba todos los muebles de todos los espacios — problemático porque el nombre de un Mueble no es único entre espacios (ej. "Biblioteca 1" en más de una sala); ahora ese `<select>` queda vacío y deshabilitado hasta elegir un Espacio específico.

- [x] **Mejoras a Catalogar: cámara, thumbnail, duplicados — COMPLETO (2026-07-30/31, PR #79 fusionado)** — 3 ajustes a `CatalogarLibroComponent`: **(1)** recuadro de escaneo horizontal (`aspect-[2/1] object-cover` en el `<video>` — el usuario ajustó manualmente de 3:1 a 2:1 tras probarlo — más `aspectRatio: { ideal: 3 }` best-effort en `getUserMedia`, sin actualizar tras el ajuste manual, inconsistencia menor sin impacto funcional); **(2)** thumbnail de portada junto al campo de URL; **(3)** detección de duplicados por ISBN — nuevo `GET /api/libros/por-isbn/:isbn` (función Lambda dedicada, ADR-008, GSI `isbn-index` ya existente) precarga el formulario si el ISBN ya está catalogado (1 coincidencia automática, varias con lista para elegir, con opción de ignorar y catalogar como nuevo); el campo de cantidad pasa a significar siempre "ejemplares nuevos". **Hallazgo real de una revisión de `architect` (no solo estilo):** el diseño inicial de fusión de duplicados (`PUT` reutilizado, total calculado en el cliente) tenía una condición de carrera real — dos vendedores fusionando el mismo duplicado casi al mismo tiempo podían perder ejemplares de inventario silenciosamente (matemática trazada y confirmada). Corregido con un endpoint dedicado y atómico, `POST /api/libros/:bookId/fusionar-duplicado` (`fusionarLibroDuplicado` en `dynamodb.ts`, `UpdateItem` con `SET` + `ADD`, nunca lee el libro antes de escribir — la atomicidad de `ADD` en DynamoDB resuelve la carrera de raíz, verificado con tests que confirman que el handler nunca lee `babel-libros` antes de escribir). La misma revisión encontró una ventana de estado obsoleto entre cambiar el ISBN y limpiar el duplicado seleccionado — corregida con un reset síncrono (antes de cualquier `await`) y deshabilitando el botón de guardar mientras cualquier búsqueda esté en curso. **Incidente de deploy post-merge (mismo día):** las descripciones de las 2 funciones Lambda nuevas excedían el límite de 256 caracteres de CloudFormation — reincidencia del mismo gotcha ya documentado el 2026-07-18 (función `usuariosMe`), no detectada esta vez por `npx serverless print` (no corre la misma validación de schema que `serverless deploy`); corregido acortando las descripciones y verificando con conteo directo de caracteres — ver §7, entrada reforzada con el método de verificación concreto para evitar una tercera reincidencia.

**Nota de proceso:** entre este punto (2026-07-31) y la siguiente entrada (2026-08-18) hay 8 PRs fusionados (#80–#88: cierre de `ajustes-2026-07-27.md`, dominio personalizado, incidente de costos DynamoDB, generador de QR, reforma del header) sin bitácora propia en esta sección — ver `TODO.md` y los números de PR en el historial de GitHub para el detalle de cada uno; no se reconstruye retroactivamente aquí por no ser parte de la tarea que motivó esta entrada.

- [x] **Fix: catalogar/editar/fusionar un libro sin ISBN (2026-08-18, PR #89 fusionado)** — primera tarea del lote de 3 ajustes traído por el usuario ese mismo día (portadas placeholder de scraping, PVPs sospechosos sin forma de re-verificar, y este bug — ver `TODO.md` para el detalle completo del lote y las 2 tareas siguientes). **Causa raíz real:** el GSI `isbn-index` de `babel-libros` tipa el atributo `isbn` como `S` estricto; para que un ítem quede fuera de ese índice disperso, DynamoDB exige que el atributo esté **ausente**, no presente con valor `null` — `guardar()` (`dynamodb.ts`) hacía un `PutCommand` directo sin limpiar `null`s, así que `POST`/`PUT /api/libros` con `isbn: null` siempre fallaba con `ValidationException` (100% reproducible, invisible en tests porque mockean `guardar`). Corregido con `omitirCamposNulos()` nueva en `dynamodb.ts`, aplicada antes de `guardar()` en `handlerCrear`/`handlerEditar` — la respuesta HTTP sigue devolviendo `isbn: null` explícito, sin cambiar el contrato con el frontend. **Segunda instancia del mismo bug, encontrada por el agente durante la implementación (no anticipada en el plan):** `fusionarLibroDuplicado` (`dynamodb.ts`) tenía el mismo problema por una ruta distinta — usa `UpdateCommand`, no `PutCommand` — corregido con `REMOVE #isbn` condicional en la `UpdateExpression` en vez de `SET #isbn = :isbn` cuando el isbn fusionado queda en `null` (DynamoDB permite mezclar cláusulas `SET`/`ADD`/`REMOVE` en una sola expresión). Sin migración de datos: como el `PutCommand` siempre fallaba, nunca llegó a existir un libro guardado con `isbn: null` en producción. 2 tests nuevos en `libros.spec.ts` (291 tests de backend en verde). Verificado por el usuario en `staging` ("Funciona bien").

- [x] **Palabras clave que invalidan una portada de scraping (2026-08-19, PR #90 fusionado)** — segunda tarea del lote de catalogación del 2026-08-18. Nuevo campo `palabrasClaveInvalidas: string[]` en `SitioScraping` (propagado a las 3 copias locales del tipo: `sitio-scraping.model.ts`, `scraping.ts`, `sitios-scraping.ts`), default `[]`. Nueva `portadaEsInvalida(portadaUrl, palabrasClaveInvalidas)` en `scraping.ts` — substring simple, sin distinguir mayúsculas — usada por `resolverMetadatosCompletos` (`metadatos.ts`) para descartar una portada scrapeada que coincide con alguna palabra clave del sitio de origen y probar el siguiente sitio de la cola de `prioridad`, en vez de aceptarla como válida; solo aplica a portadas de scraping, no a la que ya trae `api.letiende.co` (esa no tiene sitio de origen con palabras clave asociadas). `GestionSitiosScrapingComponent` gana un control de texto separado por comas (`parsearPalabrasClaveInvalidas`), convertido a arreglo solo al guardar y reconstruido con `.join(', ')` al precargar el formulario en modo edición.

  **Bug real encontrado por el usuario probando el PR en `staging` (mismo PR, commit adicional):** `/admin/sitios` aparecía incompleta — sin botones "Eliminar" en ninguna fila, solo el primer sitio con su info visible aunque las cards de todos estaban presentes. **Causa raíz:** las filas de `babel-sitios-scraping` guardadas ANTES de esta tarea no tienen el atributo `palabrasClaveInvalidas` en absoluto — el `Scan` de DynamoDB devuelve el ítem tal cual quedó guardado, sin aplicar ningún default; el default `[]` solo se aplicaba en el path de escritura (`validarDatosSitioScraping`), nunca al leer. La plantilla evaluaba `sitio.palabrasClaveInvalidas.length > 0` sobre `undefined` para esas filas y lanzaba al renderizarlas, rompiendo esa fila y las siguientes del `@for`. **El mismo hueco era más grave en un segundo lugar, no reportado por el usuario pero encontrado durante la investigación:** `resolverMetadatosCompletos` (`metadatos.ts`) pasaba `sitio.palabrasClaveInvalidas` (también `undefined` para sitios viejos) directo a `portadaEsInvalida`, que hace `.length` sin guardas — cualquier sitio viejo con `info`/`pvp` en `true` que lograra scrapear una portada real habría devuelto `500` en `GET /api/metadatos/:isbn`, la ruta crítica de catalogación (`CLAUDE.md` §1). Corregido normalizando `palabrasClaveInvalidas ?? []` en los 2 puntos de lectura (`sitios-scraping.ts` GET, `metadatos.ts` tras el `Scan`) — el contrato de `SitioScraping` (arreglo siempre presente) queda garantizado también para datos anteriores a la migración. Se agregaron además guardas `?? []` en el propio componente (`editar()`, `alSoltar()`, plantilla) para no depender silenciosamente de la normalización del backend — defensa en profundidad en el punto exacto donde el bug fue visible para el usuario. 2 tests de regresión de backend + 1 de frontend, reproduciendo exactamente el escenario de "fila sin el atributo". 306 tests backend / 328 frontend. Verificado por el usuario en `staging` tras el fix ("Funciona bien"). Ver gotcha nuevo en §7.
- [x] **Diseño del proceso asíncrono "Validar libros" — PR #91 (docs-only) fusionado (2026-08-19)** — tercera tarea del lote de catalogación del 2026-08-18, la más grande. Resuelve el detalle fino que no estaba persistido en ningún documento: modelo de `babel-validaciones-libros`, contrato de las 3 Lambdas, IAM (incluida la auto-invocación), tamaño de lote, forma del polling. Ver `docs/plan-validar-libros-async.md` y ADR-012 (§3). Sin código todavía — ver la entrada siguiente para el backend.
- [x] **Backend de "Validar libros" — COMPLETO (2026-08-19, PR #92, validado por el usuario en `staging`: "Funciona bien")** — implementa `docs/plan-validar-libros-async.md` tal como quedó diseñado, sin desviaciones de producto. Tabla `babel-validaciones-libros` (PK `validacionId`, sin GSI). `server/api/handlers/validaciones-libros.ts`: `handlerIniciar` (`POST /api/validaciones-libros`, agrupa la cola por mueble con `construirColaPorMueble` — pura, exportada para tests — y detecta corridas `en_progreso` abandonadas por más de 10 minutos vía `escanearProyeccion`, nueva en `dynamodb.ts`, para no traer `colaBookIds` completo de cada corrida histórica solo para ese chequeo); `handlerWorker` (Lambda interna sin ruta HTTP, procesa lotes de 20 libros — consenso de PVP como el más alto scrapeado, chequeo global de portada inválida con reemplazo automático o `portadasPendientes` si no hay reemplazo, libros sin ISBN se saltan solo PVP — y se auto-invoca hasta agotar la cola); `handlerConsultar` (`GET /api/validaciones-libros/:validacionId`, omite `colaBookIds`/`limitesMueble` de la respuesta). 3 roles IAM de mínimo privilegio nuevos, incluida la auto-invocación con el ARN construido por convención (`${self:custom.nombreFuncionValidarLibrosWorker}`, no `!GetAtt`, para evitar la dependencia circular ya anticipada en el ADR-012). Nueva dependencia `@aws-sdk/client-lambda`. **Gotcha reincidente evitado a tiempo:** las descripciones de 2 de las 3 funciones nuevas excedían los 256 caracteres de Lambda (mismo límite documentado en §7 tras el incidente de `usuariosMe`/PR #79) — detectado esta vez ANTES de desplegar con `npx serverless print --stage dev` (que sí valida ese esquema, a diferencia de lo que se creía en el incidente de PR #79), corregido acortando las descripciones. 28 tests nuevos (334 backend en verde). `npm run build:api`, `npm run build` y `npx serverless package --stage dev` (packaging real de los 3 zips, verificando que cada uno trae `dynamodb.js`/`scraping.js`/`verificar-token.js` y las dependencias de `node_modules` necesarias) verificados sin errores. Desplegado y validado en `staging` por el usuario — sin bloqueos para arrancar el frontend (`ValidarLibrosComponent`, `docs/plan-validar-libros-async.md` §7).

  **Bug real encontrado por el usuario probando el botón "Iniciar validación" ya con el frontend desplegado (PR #93, commit adicional a esa misma rama):** `POST /api/validaciones-libros` respondía `500 {"error":"Error interno del servidor."}`. La validación "Funciona bien" del backend (arriba) solo cubrió que el deploy terminara sin errores — no había frontend todavía para ejercitar el botón real. Causa raíz: `IniciarValidacionLambdaRole` le daba a `babel-usuarios` únicamente `dynamodb:Scan`, pero el código resuelve el rol del administrador con `obtenerPorClave` (`GetItem`) — un desajuste de acción IAM invisible para `tsc`/tests (todos mockean `obtenerPorClave`) y para `serverless print`/`package` (ninguno valida que las acciones IAM declaradas coincidan con las que el código ejecuta). Corregido separando la política de `babel-usuarios` a su propio statement con `dynamodb:GetItem` (en vez de agrupada con el `Scan` de `babel-libros`/`babel-ubicaciones`/`babel-muebles`). Se revisaron los otros 2 roles nuevos (`ValidarLibrosWorkerLambdaRole`, `ConsultarValidacionLambdaRole`) línea por línea contra el código real — ambos correctos, sin el mismo bug. Ver gotcha nuevo en §7.
- [x] **Frontend de "Validar libros" — COMPLETO (2026-08-19)** — cierra por completo la Tarea 3 y el lote de 3 ajustes de catalogación del 2026-08-18. `ValidacionesLibrosService` (`src/app/core/api/`, mismo patrón de autenticación que `SitiosScrapingService`): `iniciarValidacion()` (`POST /api/validaciones-libros`) maneja el `409` explícitamente — devuelve `{ iniciada: false, validacionIdEnCurso }` en vez de tratarlo como un error genérico, para que el componente retome esa corrida en silencio en vez de bloquear al administrador; `consultarValidacion()` (`GET /api/validaciones-libros/:id`) nunca lanza. `ValidarLibrosComponent` en `/admin/validar-libros` (`RoleGuard('administrador')`): botón "Iniciar validación", barra de progreso, `muebleActualNombre`, contadores en vivo, y al terminar, lista de `portadasPendientes` (con link a la portada actual para inspección visual) y `erroresLibro`. Polling cada 3s (`setInterval`, limpiado en `ngOnDestroy`), con una consulta inmediata al arrancar/retomar en vez de esperar el primer tick. `ValidacionesLibrosService.ultimoValidacionId` (Signal en memoria, singleton `providedIn: 'root'`) permite retomar el polling si el administrador navega fuera de la ruta y vuelve dentro de la misma sesión de la SPA; tras un recargo real de página, el respaldo es el propio `409` del backend. **Bug real encontrado y corregido durante las pruebas del propio componente (no solo un test que fallaba por error del test):** si `consultarValidacion` dejaba de encontrar la corrida (red, corrida abandonada) a mitad del polling, el componente detenía el intervalo pero dejaba el último `resumen` "en_progreso" congelado en el Signal — el botón "Iniciar validación" nunca se reactivaba, dejando al administrador bloqueado sin poder reintentar aunque el polling ya no estuviera corriendo. Corregido limpiando `resumen` a `null` en ese caso, para que el estado derivado (`enProgreso()`) vuelva a `false`. Nueva tarjeta "Validar libros" en `AdminInicioComponent`/`/admin`. 17 tests nuevos (345 frontend en verde, con `vi.useFakeTimers()`/`advanceTimersByTimeAsync` para el polling — sin precedente previo en el proyecto). `npm run build`/`ng test` verificados; verificación visual interactiva con login real queda pendiente del usuario en `staging` (mismo criterio que el resto de features de administración — `CLAUDE.md` §1, no automatizable en sandbox sin credenciales reales).
- [x] **Selector manual de portada por ISBN (Catalogar/Editar) — COMPLETO (2026-08-19)** — ajuste ad-hoc nuevo, planeado con `Plan` mode (3 preguntas de producto resueltas con el usuario antes de escribir código: alcance solo por ISBN v1, sitios `info: true`, excluir del listado portadas ya conocidas como inválidas). Para placeholders genéricos con URLs de ID individual, sin palabra clave detectable por `portadaEsInvalida` (Tarea 2). Backend: `GET /api/metadatos/:isbn/portadas` (`handlerBuscarPortadas`, `server/api/handlers/metadatos.ts`) — `resolverPortadasCandidatas` reutiliza `scrapearSitio`/`portadaEsInvalida` tal cual existen, devuelve TODAS las portadas válidas de los sitios `info: true` (no solo la primera por prioridad, a diferencia de `resolverMetadatosCompletos`), ordenadas por `prioridad`. Nueva función Lambda `metadatosPortadas` + rol IAM `MetadatosPortadasLambdaRole`, verificado acción por acción contra `dynamodb.ts` (lección directa del incidente de IAM de "Validar libros", arriba, el mismo día). Frontend: `MetadatosService.buscarPortadas()` + `SelectorPortadaComponent` nuevo en `src/app/shared/selector-portada/` — **primer componente compartido del proyecto con estado/lógica real** (antes solo había `navegacion/`/`pipes/`, ligeros), con `input()`/`output()` signal-based de Angular 22 (sin precedente previo de `@Input`/`@Output` clásicos que romper). Diálogo con grid de portadas candidatas, selección en 2 pasos (elegir tarjeta, confirmar con "Cambiar", mismo patrón de overlay `fixed inset-0` que el diálogo de "Vender" en `libro-detalle.component.ts`). Wiring en `CatalogarLibroComponent`/`EditarLibroComponent`: botón de ícono (Heroicons `arrow-path`) junto al thumbnail, deshabilitado sin ISBN. `EditarLibroComponent` no tenía thumbnail de portada — se agregó igual que ya existía en Catalogar (pedido explícito del usuario). 9 tests backend + 21 frontend nuevos (343 backend / 363 frontend en total). **Hallazgo real de Angular durante las pruebas, no relacionado con el feature en sí — ver gotcha nuevo en §7:** `formControl.setValue(valor)` + `fixture.detectChanges()` NO actualiza un `@if` que lee `formControl.value` directamente en el template; hace falta un evento DOM `input` real. Reproducido en aislamiento contra código YA EXISTENTE antes de esta tarea (no es una regresión). `npm run build:api`/`build`/`serverless print`/`serverless package` verificados; smoke test `ng serve` + `curl /catalogar` (200, sin crash). Verificado en vivo por el usuario en `staging` con login real — exitoso.
- [x] **Adaptadores de scraping para Casa Tomada y Librería de la U — COMPLETO (2026-08-20, PR #100)** — el usuario agregó ambos sitios en `/admin/sitios` (en `production`) pero no traían portada/datos. Diagnosticado con `aws dynamodb scan` sobre `babel-sitios-scraping-production`: los dos dominios estaban bien capturados (`info`/`pvp` en `true`) — no fue un error de la interfaz de administración. Causa raíz: `babel-sitios-scraping` solo expresa **política** (ADR-010); el **mecanismo** de extracción vive en código (`ADAPTADORES_POR_DOMINIO`, `scraping.ts`) y ninguno de los dos dominios tenía adaptador todavía — `scrapearSitio` caía en la rama "dominio sin adaptador de código" y devolvía `{}` en silencio, sin ningún error visible para el administrador. Investigado en vivo contra ambos sitios reales antes de escribir código: **Librería de la U** corre la misma API pública VTEX que Lerner/Nacional, con los mismos nombres de campo que Lerner (`Autor`/`Editorial`) — reusa `consultarApiVtex` sin cambios. **Casa Tomada** corre exactamente la misma plataforma que Tornamesa (idéntica ruta `/busqueda/listaLibros.php?tipoBus=full&palabrasBusqueda=` e idéntico JSON-LD `Book` en la página de producto) — se generalizó `consultarTornamesa` a `consultarPlataformaListaLibros(urlBase, query)`, ahora compartida por ambos sitios. 4 tests nuevos con fixtures basados en respuestas reales (`libreriadelau-api-encontrado.json`, `casatomada-busqueda.html`, `casatomada-producto.html`) — 347 tests backend en verde (antes 343). Deliberadamente **fuera de alcance:** `handlerBuscar`/`buscarPvpPorTexto` (`metadatos.ts`, búsqueda por título/autor sin ISBN) no se tocaron — ya excluían a propósito los sitios sin datos completos en la misma respuesta (ver bullet de PR #48, arriba); si se quiere que estos 2 sitios también participen ahí, es una tarea aparte. Verificado por el usuario en `production` ("Todo funciona bien").
- [x] **Fix: buscador de "Catalogar > Editar" bloqueado con isbn undefined (2026-08-29, PR #103 fusionado)** — bug real reportado por el usuario en `production`: al escribir un título/autor en el buscador de la pestaña Editar, la lista se bloqueaba y no filtraba nada. Dos intentos previos NO lo resolvieron porque atacaban un síntoma distinto: PR #101 corrigió que `escanearTodo`/`escanearMayorQue` no seguían `LastEvaluatedKey` (`babel-libros` ya sobre el límite de ~1MB por página de un `Scan`, con 2.000+ libros), y PR #102 subió el timeout de esas Lambdas a 25s por la consecuencia de esa paginación (varios `Scan` secuenciales). Ninguno tocaba al frontend, y CloudWatch no mostraba ningún error — la pista fue el stack de DevTools (`TypeError: Cannot read properties of undefined (reading 'includes')` dentro del `computed` `librosFiltrados`). **Causa raíz real:** un libro sin ISBN se persiste con el atributo `isbn` AUSENTE en `babel-libros` (`omitirCamposNulos`, PR #89, el GSI disperso `isbn-index` tipa `isbn` estrictamente `S`), así que `GET /api/libros/inventario` devolvía esos libros con `isbn` `undefined`, no `null` — incumpliendo el contrato `Libro.isbn: string | null`. El guard `libro.isbn !== null` de `editar-libro.component.ts` dejaba pasar el `undefined`, y `undefined.includes(...)` reventaba el `computed`, tumbando el render de TODA la lista apenas se escribía algo (con el buscador vacío ese código nunca se ejecutaba, por eso el bug pasó cualquier prueba superficial). Misma clase de bug ya documentada en §7 el 2026-08-19 (`palabrasClaveInvalidas` ausente en filas viejas) — un atributo declarado no-opcional en el tipo TS puede seguir ausente en datos ya persistidos, y ningún test con fixtures "completos" lo detecta. Fix en dos capas: el filtro del componente lee `titulo`/`autor`/`isbn` con `?? ''` en vez de comparar contra `null` (mismo criterio que ya usaba `catalogo-publico.component.ts`, que por eso nunca falló); y nuevo helper `normalizarLibro()` en `server/api/handlers/libros.ts` restituye `isbn: null` en los 4 endpoints que devuelven libros crudos de DynamoDB (`GET /api/libros`, `GET /api/libros/:bookId`, `GET /api/libros/por-isbn/:isbn`, `GET /api/libros/inventario`) para que el JSON entregue lo que el tipo promete. 2 tests de regresión (el del componente reproduce el `TypeError` exacto sin el fix). 365 tests frontend / 354 backend en verde. Ver gotcha nuevo en §7.
- [x] **Diseño del lote "duplicados, apilamiento y reporte de repetidos" — PR #105 (docs-only) (2026-08-29)** — el usuario detectó que un mismo libro se catalogó **dos veces en la misma ubicación**, pese a la advertencia de duplicados ya existente (Tarea 2.3, PR #79). Trajo 3 funcionalidades: (1) dos flujos distintos de advertencia al catalogar según si el ISBN ya existe en la MISMA ubicación (bloqueante: campos deshabilitados salvo Cantidad, botón "Editar libro") o en OTRA (informativa, ignorable); (2) apilar ejemplares del mismo libro en el catálogo público, con un panel de ubicación y un botón VENDER por ejemplar en la ficha; (3) reporte XLSX de libros repetidos en `/admin/reportes`. Diseño completo en `docs/plan-duplicados-catalogacion.md`, sin código todavía. **4 ambigüedades reales resueltas con el usuario antes de escribir el plan:** semántica del campo Cantidad (precargar el total existente y enviar la **diferencia** a `fusionar-duplicado`, conservando el `ADD` atómico que corrigió la condición de carrera del PR #79 — la alternativa "enviar el total" habría reintroducido ese bug); criterio de apilamiento (**solo por ISBN**; se descartó "ISBN o título" porque encadena transitivamente y puede fusionar libros sin relación en la cara pública); cómo consultar Babel primero al buscar por título/autor (**índice ligero cacheado en el cliente** vía `escanearProyeccion` — `babel-libros` solo tiene el GSI `isbn-index`, así que un `Scan` por búsqueda caería sobre la ruta crítica de catalogación con 2.000+ libros y varias páginas); y el PVP en la ficha apilada (**por panel**, con rango en la cabecera si difieren, porque ejemplares catalogados por separado pueden tener precios distintos y el vendedor debe cobrar el del ejemplar exacto). 7 supuestos adicionales (§2 del plan) quedaron **confirmados por el usuario tal cual** el 2026-08-29, sin ajustes. **Hallazgo de la revisión del código que explica el incidente reportado:** `seleccionarDuplicado` (`catalogar-libro.component.ts`) sobrescribe silenciosamente el panel "Ubicación del libro" con la ubicación del duplicado — el vendedor elige la ubicación B, escanea un ISBN que ya existe en A, y el formulario salta a A sin avisar; sumado a que la advertencia no distingue "misma ubicación" de "otra ubicación" y su enlace "Ignorar y catalogar como nuevo" es fácil de pulsar. Se corrige en la Tarea 1. Buena parte de la infraestructura necesaria ya existe y se reutiliza tal cual (GSI `isbn-index`, `GET /api/libros/por-isbn/:isbn` con ubicación resuelta, `POST /api/libros/:bookId/fusionar-duplicado` atómico, `escanearProyeccion`, el patrón XLSX de `handlerExportarInventario`): solo la Tarea 3 agrega infraestructura nueva.
- [x] **Tarea 1 del lote — Duplicados en catalogación: los dos flujos de advertencia — COMPLETA (2026-08-29, PR #106 fusionado)** — implementa `docs/plan-duplicados-catalogacion.md` §4 tal como quedó diseñado, sin desviaciones. `CatalogarLibroComponent` gana un `computed` `duplicadoEnMismaUbicacion` (reactivo a `libroDuplicadoSeleccionado`/`panelUbicacionId`) y un `effect()` en el constructor que, cuando es `true`, deshabilita todo el formulario salvo `cantidadTotal` (que pasa a representar el TOTAL existente, precargado desde el duplicado) y cambia el botón a "Editar libro"; al salir de ese estado, re-habilita los campos y revierte `cantidadTotal` a `1` solo si él mismo la había puesto en el total (flag `cantidadReflejaTotalExistente`, para no pisar un valor que el vendedor haya escrito libremente en cualquier otro caso). `guardar()` calcula el DELTA (`cantidadNueva - total existente`) y solo lo envía a `fusionar-duplicado` si es positivo — un delta ≤ 0 remite a la pestaña Editar sin enviar nada. **La corrección real del incidente:** `seleccionarDuplicado` ya NO sobrescribe el panel "Ubicación del libro" — si el duplicado queda en otra ubicación (caso informativo, no bloqueante), el botón sigue diciendo "Catalogar libro" y crea un `bookId` nuevo e independiente (`POST /api/libros`, nunca una fusión con el libro de la otra ubicación) en la ubicación que el vendedor ya había elegido. También se invirtió `dispararBusquedaPorIsbn`: Babel se consulta primero (`GET /api/libros/por-isbn/:isbn`) y, si el ISBN ya está catalogado, la búsqueda de metadatos externos (`api.letiende.co` + scraping, varios segundos) ya no se llama en absoluto. Sin backend nuevo. El describe completo de "detección de libros ya catalogados" en `catalogar-libro.component.spec.ts` se reescribió para el nuevo contrato (casos MISMA/OTRA ubicación por separado, cálculo del delta, reclasificación reactiva al mover el panel) — 369 tests frontend en verde (antes 365). `npm run build` limpio; smoke test `ng serve` + `curl /catalogar` (200, sin crash) — verificación visual interactiva con login real queda pendiente del usuario en `staging`, mismo criterio que el resto de features de administración/catalogación (`CLAUDE.md` §1).
- [x] **Tarea 2 del lote — Reporte de libros repetidos — COMPLETA (2026-08-29, PR #107 fusionado)** — implementa `docs/plan-duplicados-catalogacion.md` §5 tal como quedó diseñado. Backend: `GET /api/libros/exportar-repetidos` (`handlerExportarRepetidos`, `libros.ts`, Lambda propia con rol IAM de solo lectura sobre `babel-libros`/`babel-ubicaciones`/`babel-muebles`/`babel-espacios`/`babel-usuarios`, calcado literal de `ExportarInventarioLambdaRole`). `agruparPorIsbnOTitulo` implementa componentes conexos (union-find) con buckets `Map` por ISBN y por título normalizado (`normalizarParaComparacion`, exportada) — evita comparar cada par (`O(n²)`, inviable con 3.000+ libros): cada bucket de 2+ índices se une en bloque. `motivoDelGrupo` calcula `ISBN`/`Título`/`ISBN y título` buscando un valor repetido DENTRO del propio grupo (no contra todo el catálogo), así que también acierta en un grupo de 3+ libros formado por encadenamiento (A~B por ISBN, B~C por título — los 3 quedan juntos, motivo "ISBN y título" si ambas coincidencias existen dentro del grupo). Solo se emiten grupos de 2+ libros. Frontend: `LibrosService.exportarRepetidos()` (mismo patrón de descarga de blob que `exportarInventario` — se parametrizó `descargarArchivo(archivo, nombreArchivo)`, antes hardcodeaba `'reporte-inventario.xlsx'`) y un tercer bloque "Reporte de libros repetidos" en `/admin/reportes` (`ReportesVentasComponent`), calcado del bloque de inventario. **Hallazgo de test durante la implementación:** el helper `botonExportarInventario` del spec de `ReportesVentasComponent` ubicaba el botón como "el último `button[type=\"button\"]`" — dejó de servir al agregar el tercer botón; corregido a localizar por POSICIÓN fija (índice 0/1 en el orden de la plantilla), no por texto (que cambia a "Exportando…" mientras carga) ni por "el último". 7 tests backend (`normalizarParaComparacion` + agrupación/encadenamiento/exclusión de grupos de 1, con un caso de 3 libros que ejercita el encadenamiento transitivo real) + 7 frontend nuevos (361 backend / 376 frontend en total). `npm run build`/`build:api`/`serverless print --stage dev` (sin `[!] Invalid configuration`, gotcha del límite de 256 caracteres) y `serverless package --stage dev` verificados — el zip de `exportarLibrosRepetidos` incluye `dist-server/api/handlers/libros.js`/`lib/verificar-token.js`/`services/dynamodb.js` (mismo cuidado del gotcha de `ImportModuleError`, §7). Smoke test `ng serve` + `curl /admin/reportes` (200, sin crash) — verificación visual interactiva con login real queda pendiente del usuario en `staging`.
- [x] **Tarea 3 del lote — Babel como primera fuente al buscar por título/autor — COMPLETA (2026-08-29, PR pendiente de abrir)** — implementa `docs/plan-duplicados-catalogacion.md` §6, única tarea del lote con infraestructura nueva. **Medición real contra `production` antes de dar la tarea por buena** (requisito explícito del plan): `aws dynamodb scan` con la misma `ProjectionExpression` de 8 campos que usaría el endpoint — 1.534 libros, ~360 B/libro sin comprimir, ~100 B/libro con gzip (API Gateway HTTP API lo aplica automáticamente); a 3.000 libros (volumen fundacional) el índice completo pesa ~1 MB sin comprimir, ~300 KB comprimido — aceptable para una sola carga por sesión. Decisión tomada con esos números: mantener `portadaUrl` pese a ser, con diferencia, el campo más pesado, para que el vendedor siga viendo la miniatura del candidato. Backend: `GET /api/libros/indice` (`handlerIndice`, `escanearProyeccion`, rol IAM `LibrosIndiceLambdaRole` calcado de `LibrosInventarioLambdaRole`). Frontend: `LibrosService.cargarIndice()` con un flag `indiceSolicitado` (no un Signal booleano) para pedirlo UNA sola vez por sesión, incluso si la primera llamada falló. `CatalogarLibroComponent.buscarCandidatos()` ahora filtra primero `LibrosService.indice` en memoria (`filtrarIndice`, `normalizarTexto` ya existente — si el vendedor escribió título Y autor, exige que ambos coincidan) y solo cae a `MetadatosService.buscarCandidatos` (externo) sin ninguna coincidencia — nunca en paralelo, para no mostrar resultados de terceros cuando el libro ya está en Babel. Los candidatos de Babel se distinguen con un nuevo campo discriminante `origen: 'babel' | 'externo'` (tipo `CandidatoConOrigen`) y se muestran con una etiqueta "Ya en el catálogo". **Reutilización limpia del trabajo de la Tarea 1:** al elegir un candidato de Babel, se resuelve la ficha completa por `bookId` (`LibrosService.obtenerDetalle`, ya existente — el índice ligero no trae `editorial`/`porcentajeDescuentoEditorial`/etc.) y se delega a `seleccionarDuplicado`, el mismo método de la Tarea 1 — decide solo, sin código nuevo, si es un caso bloqueante (misma ubicación) o informativo (otra ubicación). Único ajuste retroactivo a `seleccionarDuplicado`: ahora también precarga el campo `isbn` (antes no lo tocaba, porque en el flujo por ISBN de la Tarea 1 ese campo ya tenía el valor correcto — sin efecto ahí, pero necesario aquí, donde el vendedor pudo no haber tocado nunca el campo isbn). **Hallazgo de test durante la implementación:** agregar `librosService.cargarIndice()` a `ngOnInit()` rompió TODOS los tests preexistentes del componente (`GET /api/libros/indice` sin flushear en cada `afterEach`) — corregido mockeando `LibrosService` por completo en `configurarPrueba()` (mismo patrón que `MetadatosService`/`UbicacionFisicaService`, nunca golpea HTTP real), exponiendo `indiceSignalDePrueba` para que las pruebas nuevas puedan poblar el índice antes de buscar. También rompió `gestionar.component.spec.ts` (mock parcial de `LibrosService` sin `cargarIndice`, porque `GestionarComponent` renderiza `CatalogarLibroComponent` como hijo) — mismo fix. 15 tests nuevos (5 backend + 4 `LibrosService` + 6 `CatalogarLibroComponent`) — 366 backend / 386 frontend en total. `npm run build`/`build:api`/`serverless print --stage dev`/`serverless package --stage dev` verificados — el zip de `librosIndice` incluye `libros.js`/`verificar-token.js`/`dynamodb.js`. Smoke test `ng serve` + `curl /catalogar` (200, sin crash) — verificación visual interactiva con login real queda pendiente del usuario en `staging`.

---

## 3. ADRs (Architecture Decision Records)

### ADR-001 — Backend propio en AWS Lambda/DynamoDB; `api.letiende.co` solo para metadatos
- **Fecha:** 2026-07-16
- **Estado:** Aceptado
- **Decisión:** Babel construye su propio backend (Lambda + DynamoDB + Serverless Framework) para almacenamiento y lógica de negocio (libros, ventas, estantes, usuarios, descuentos). La API externa `https://api.letiende.co`, ya existente y compartida entre apps de Le Tiende, se consume únicamente para resolver metadatos de libro (título, autor, portada, editorial) a partir del ISBN vía Google Books.
- **Razón:** el enunciado del proyecto describe explícitamente Lambda + DynamoDB como parte de la infraestructura propia de Babel ("el objetivo de la infraestructura (Lambda, Dynamo, Firebase Authentication, etc.) es que sea de costo $0"), mientras que `api.letiende.co` se menciona como un servicio ya consumido para un paso específico (metadatos). Confirmado con el usuario en la sesión de bootstrap.
- **Consecuencias:** Babel es responsable de su propio modelo de datos y de la lógica de scraping/búsqueda de PVP; no depende de cambios en `api.letiende.co` salvo para el paso de metadatos.

### ADR-002 — Aplicación PWA responsive, no app nativa empaquetada
- **Fecha:** 2026-07-16
- **Estado:** Aceptado
- **Decisión:** Babel se implementa como una PWA responsive (acceso a cámara vía `getUserMedia` desde el navegador), no como una app nativa empaquetada con Capacitor/Cordova.
- **Razón:** el stack solicitado (Angular SSR en AWS Lambda) es infraestructura web; empaquetar nativo agregaría complejidad de build/publicación no solicitada explícitamente. Confirmado con el usuario.
- **Consecuencias:** el acceso a cámara depende de las restricciones de navegador (HTTPS obligatorio, permisos por gesto de usuario — ver `CLAUDE.md` §7). Si la experiencia de escaneo resulta insuficiente en campo, evaluar Capacitor como ítem de roadmap Baja prioridad (ver `PRD.md` §6).

### ADR-003 — Tabla `babel-ventas` separada del registro de libro
- **Fecha:** 2026-07-16
- **Estado:** Aceptado
- **Decisión:** cada venta se registra como un documento independiente en `babel-ventas`, y `babel-libros.cantidadDisponible` se decrementa por cada venta, en vez de sobrescribir un único campo "vendido" en el libro.
- **Razón:** los reportes de ventas (PRD §5.5) requieren filtrar/ordenar por fecha, PVP, utilidad, costo, editorial y forma de pago con granularidad por transacción; un libro catalogado puede tener múltiples ejemplares vendidos en momentos distintos.
- **Consecuencias:** el catálogo público debe consultar `cantidadDisponible > 0` para determinar si un libro sigue en venta, en vez de un simple booleano.

### ADR-004 — Scraping sin navegador headless
- **Fecha:** 2026-07-16
- **Estado:** Aceptado
- **Decisión:** la búsqueda de PVP en sitios de la lista blanca se implementa con peticiones HTTP simples (`fetch`/`undici`) + `cheerio` para parsear HTML, sin Puppeteer/Playwright.
- **Razón:** mantener el tiempo de ejecución y memoria de la Lambda bajos para permanecer dentro de la capa siempre gratuita de AWS (objetivo de costo $0 explícito del proyecto).
- **Consecuencias:** sitios que dependan fuertemente de JavaScript del lado del cliente para mostrar el precio no serán compatibles con este enfoque; deberán excluirse de la lista blanca o resolverse manualmente.

### ADR-005 — Documentación en español, código en español
- **Fecha:** 2026-07-16
- **Estado:** Aceptado
- **Decisión:** variables, funciones, clases, nombres de tablas, commits, comentarios y documentación técnica en español.
- **Razón:** decisión explícita del usuario en la sesión de bootstrap (a diferencia de Comandante, que usa código en inglés con comentarios/UI en español).
- **Consecuencias:** revisar que las librerías de terceros no impongan convenciones en inglés dentro del propio código de Babel (los nombres de paquetes npm y sus APIs siguen en inglés, eso es aceptado).

### ADR-006 — Dos descuentos distintos: editorial (margen/consignación) vs. venta (discrecional)
- **Fecha:** 2026-07-17
- **Estado:** Aceptado
- **Decisión:** el sistema modela dos porcentajes de descuento completamente independientes:
  1. **Descuento editorial** (`Libro.porcentajeDescuentoEditorial`): margen que Le Tiende retiene sobre el PVP en libros que la editorial deja en consignación (típico 35% en el contexto colombiano: PVP $100.000 → editorial cobra $65.000, Le Tiende retiene $35.000 de utilidad). El administrador puede configurar modelos distintos por editorial (`babel-editoriales-descuentos`). Cuando el libro es propiedad de Le Tiende y no está en consignación, este porcentaje es 100% (sin costo, toda la venta es utilidad). Se fija al catalogar y determina `Libro.costo`.
  2. **Descuento de venta** (`Venta.porcentajeDescuentoVenta`): descuento discrecional que el vendedor acuerda con el comprador al momento de vender (0% por defecto), usado para rotar catálogo o al negociar libros propios de Le Tiende. Determina `Venta.precioFinal`, no afecta el costo del libro.
- **Razón:** precisión de negocio aportada por el usuario tras la primera versión de la documentación — el modelo original no distinguía estos dos conceptos con claridad, lo que habría llevado a un cálculo incorrecto de costo/utilidad en los reportes.
- **Consecuencias:** `Venta` ahora incluye un snapshot (`costoLibro`, `utilidad`, `pvp`) tomado al momento de la venta, para que cambios posteriores en la configuración de descuentos de editorial no alteren el costo/utilidad de ventas ya registradas. `POST /api/ventas` debe calcular y persistir ese snapshot, no solo referenciar el libro. Ver `tech-specs.md` §4.3 y §5.1, y `PRD.md` §5.2/§5.4/§10.

### ADR-007 — Firebase Authentication compartido con Comandante; autorización independiente por app
- **Fecha:** 2026-07-17
- **Estado:** Aceptado
- **Decisión:** Babel no crea un proyecto Firebase propio. Reutiliza el mismo proyecto Firebase que usa Comandante para Google Sign-In (misma identidad de usuario en ambas apps), pero cada app resuelve la autorización (rol `administrador`/`vendedor`) de forma completamente independiente en su propia base de datos: `babel-usuarios` en DynamoDB para Babel, la colección `users` de Firestore para Comandante. Babel usa su propia cuenta de servicio (`FIREBASE_SERVICE_ACCOUNT_BABEL`) sobre ese proyecto compartido, distinta de la de Comandante.
- **Razón:** pedido explícito del usuario — los mismos usuarios de Google de Comandante deben poder autenticarse en Babel sin crear cuenta nueva, evaluando primero si esto introducía algún problema de seguridad.
- **Consecuencias:**
  - Estar autenticado en el proyecto compartido no otorga ningún permiso por sí solo en ninguna de las dos apps — el correo debe existir explícitamente en la tabla/colección de usuarios de esa app específica (ya era así, se refuerza en `CLAUDE.md` A01/A07).
  - Mayor "blast radius" si el proyecto Firebase o una cuenta de servicio se ven comprometidos (afecta potencialmente a ambas apps) — mitigado con cuentas de servicio separadas por app.
  - Revocación en dos pasos: quitar el rol en `babel-usuarios` revoca solo Babel; deshabilitar la cuenta en la consola de Firebase revoca ambas apps. El administrador debe saber cuál usar según el caso.
  - Cuota de usuarios activos mensuales (MAU) de Firebase se comparte entre ambas apps (sin impacto esperado dado el volumen).
  - `projectId` confirmado por el usuario el 2026-07-17: **`comandante-letiende`**. Babel debe configurar el SDK cliente y `firebase-admin` apuntando a ese `projectId` exacto (ver `tech-specs.md` §6 y §8.1).

### ADR-008 — Un endpoint de negocio = una función Lambda propia, no un router monolítico
- **Fecha:** 2026-07-18
- **Estado:** Aceptado
- **Decisión:** cada endpoint (o grupo pequeño de endpoints muy relacionados) se despliega como su propia función Lambda en `serverless.yml`, con su propio rol IAM de mínimo privilegio — no un único Lambda "api" con un router interno que reciba todo `/api/*`. Primer caso real: `usuariosMe` es una función separada de `api` (que hoy solo sirve `/api/health`), con un rol que únicamente tiene `dynamodb:GetItem` sobre `babel-usuarios`, no el CRUD completo de las 5 tablas que tiene `ApiLambdaRole`.
- **Razón:** `tech-specs.md` §1 documentaba "2 funciones Lambda... compartiendo dominio y evitando CORS", pero esa razón (compartir dominio/evitar CORS) se resuelve a nivel de API Gateway/HTTP API, no depende de que exista un único proceso Lambda — varias funciones pueden convivir bajo el mismo HTTP API sin problema, como ya hacían `api` y `ssr`. Consolidar toda la lógica de negocio en un único Lambda hubiera obligado a darle a esa función un rol IAM amplio (acceso a las 5 tablas) incluso para un endpoint que solo necesita leer una, violando CLAUDE.md A05 (mínimo privilegio).
- **Consecuencias:** el número de funciones Lambda crecerá con cada endpoint nuevo (aceptado); cada una necesita su propio bloque en `serverless.yml` con `package.patterns` explícito (ver gotcha de `node_modules/**` en §7). `tech-specs.md` §1 debería actualizarse en algún momento para reflejar esto (no se hizo en esta sesión, fuera de alcance de la tarea).

### ADR-009 — `POST /api/usuarios` responde `409` sobre un email existente; un administrador no puede degradarse ni eliminarse a sí mismo
- **Fecha:** 2026-07-19
- **Estado:** Aceptado
- **Decisión:** dos reglas explícitas para el CRUD de `/api/usuarios` (`server/api/handlers/usuarios.ts`):
  1. `POST /api/usuarios` sobre un `email` que ya tiene fila en `babel-usuarios` responde `409 Conflict` en vez de sobrescribir en silencio (comportamiento por defecto de `guardar`/`PutCommand`).
  2. Un `administrador` autenticado nunca puede, a través de este endpoint, cambiar su propio `rol` (`PUT` sobre su propio email con `rol !== 'administrador'`) ni eliminar su propia fila (`DELETE` sobre su propio email) — ambos casos responden `400` con un mensaje explicativo indicando que debe hacerlo otro administrador.
- **Razón:** `TODO.md` (tarea del CRUD de usuarios) marcaba ambos como decisiones de diseño explícitas, no implícitas. Para (1): sobrescribir sin avisar permitiría que un `POST` accidental (o mal usado) cambiara el rol de un usuario existente sin que quien lo hizo se diera cuenta — un error de "alta" no debería poder mutar una fila ya existente. Para (2): sin esta salvaguarda, un solo `PUT`/`DELETE` (accidental o malicioso desde una sesión comprometida) podría dejar `babel-usuarios` sin ningún `administrador`, bloqueando el panel admin por completo sin ninguna vía de recuperación dentro de la propia app (habría que volver a `sembrar-usuario-prueba` manualmente vía CI).
- **Consecuencias:** degradar o eliminar a un administrador requiere siempre la acción de **otro** administrador distinto — si en algún momento solo queda un administrador y necesita dejar de serlo, la única vía es la operación interna de `operaciones-staging.yml` (o edición directa de DynamoDB), igual que hoy. No se implementó un conteo de "administradores restantes" (alternativa descartada): habría requerido un `Scan` completo de la tabla en cada `PUT`/`DELETE` solo para ese caso límite, más costoso y con una ventana de carrera propia, para un beneficio marginal sobre la regla más simple "nunca te toques a ti mismo".

### ADR-010 — Lista única de sitios de scraping en DynamoDB, administrable, con banderas `info`/`pvp`
- **Fecha:** 2026-07-21
- **Estado:** Aceptado
- **Decisión:** la obtención automática de datos de librerías externas (fallback de metadatos y fuente de PVP) se gobierna con una **única** tabla DynamoDB `babel-sitios-scraping`, editable por el administrador desde `/admin/sitios` (mismo patrón CRUD que `estantes`/`editoriales-descuentos`). Cada fila: `{ dominio (PK), nombre, url, info: boolean, pvp: boolean, prioridad: number }`. Reemplaza el modelo previo de dos listas separadas (lista blanca de sitios autorizados + lista negra de sitios prohibidos): ahora hay una sola lista donde cada sitio declara, por separado, si sirve para `info` (título/autor/editorial/portada) y/o para `pvp` (precio). `{info:false,pvp:false}` = prohibido para todo.
- **Razón:** pedido explícito del usuario — (1) el administrador necesita añadir/quitar fuentes sin un deploy (la config estática obligaba a un cambio de código + despliegue por cada sitio); (2) al menos un sitio antes "en lista negra" resultó ser fuente confiable de datos bibliográficos aunque no de precio, lo que el modelo binario blanca/negra no podía expresar — dos banderas independientes sí. **Supersede** la decisión de `tech-specs.md` §6 ("lista mantenida como configuración estática en el repo, no editable desde la UI de administración en el alcance actual") y la consecuencia de ADR-004 al respecto.
- **Consecuencias:** hay una tabla y un CRUD de administración nuevos. La fila de DynamoDB guarda solo **política** (qué sitio, para qué, en qué orden); la **mecánica** de extracción (plantilla de URL de búsqueda por ISBN + selectores CSS) vive en código (`server/api/services/scraping.ts`), indexada por `dominio`. Un sitio en la lista sin adaptador de código no se puede scrapear (se registra y se omite) — el admin controla *si/para qué* se usa un sitio, el desarrollador aporta el *cómo*. Como la lista pasa a ser editable por datos, la garantía anti-SSRF no puede depender de ella (ver ADR-011).

### ADR-011 — Guardia SSRF fija e independiente de la lista administrable de sitios
- **Fecha:** 2026-07-21
- **Estado:** Aceptado
- **Decisión:** toda URL saliente del módulo de scraping pasa SIEMPRE, en tiempo de `fetch`, por una función `esUrlSegura()` estática (código del repo, no configurable por datos): exige esquema `https:`, resuelve el hostname y **rechaza** IP literal o resuelta en rangos privados/loopback/link-local (`10/8`, `172.16/12`, `192.168/16`, `127/8`, `::1`, `fc00::/7`, `fe80::/10`) y explícitamente `169.254.169.254` (metadata service de AWS), y no sigue redirecciones a hosts no revalidados (`fetch` con `redirect: 'manual'`, revalidando cada salto).
- **Razón:** ADR-010 movió el allowlist de sitios de config estática (código) a una tabla editable por el administrador. Si la única barrera anti-SSRF fuera "está en la lista", un administrador (o una sesión de administrador comprometida) podría añadir `http://169.254.169.254` o un host interno y convertir el scraper en un arma de SSRF. La tabla expresa *intención*; la guardia estática impone *seguridad*. Así se mantiene intacta la regla `CLAUDE.md` A10 aunque el allowlist sea data-driven.
- **Consecuencias:** un sitio válido en la lista igual es rechazado en tiempo de ejecución si su host resuelve a una IP no pública (defensa en profundidad). La guardia se prueba con tests unitarios que confirman el rechazo de IPs privadas/link-local/metadata y esquemas no-https. El scraping nunca lanza hacia afuera: un host rechazado se trata como "no encontrado", igual que cualquier otro fallo de fuente externa.

### ADR-012 — Proceso asíncrono "Validar libros": auto-invocación por lotes + tabla de progreso, sin Step Functions/SQS
- **Fecha:** 2026-08-19
- **Estado:** Aceptado
- **Decisión:** el primer patrón asíncrono del proyecto (revalidar en bloque PVP/portada de todo el inventario, Tarea 3 del lote 2026-08-18) se implementa con una Lambda `validarLibrosWorker` que se auto-invoca (`InvocationType: 'Event'`) en lotes fijos de 20 libros, coordinada por un único ítem de progreso en una tabla nueva `babel-validaciones-libros`, y el frontend hace polling por HTTP cada 3s. Sin AWS Step Functions ni SQS. Detalle completo (modelo de datos, contrato de las 3 Lambdas, IAM, batching "por mueble") en `plan-validar-libros-async.md`.
- **Razón:** mantener el patrón ya vigente "un endpoint de negocio = una función Lambda propia" (ADR-008) sin sumar un servicio administrado nuevo. Step Functions cobra por transición de estado (aunque bajo, es un costo nuevo distinto de cero, CLAUDE.md §7.3) y SQS —aunque gratis en su capa siempre gratuita— añade infraestructura nueva (cola, DLQ, permisos, otro punto de fallo) para un volumen (miles de libros, no millones) que no la justifica. Una Lambda auto-invocada + una tabla de progreso ya resuelve el problema real (evitar el timeout de 10s del proveedor sobre 3.000+ libros) con las mismas piezas (Lambda + DynamoDB) que el resto del proyecto.
- **Consecuencias:** la auto-invocación exige construir el ARN de la propia función **por convención** (`!Sub` con `${self:service}-${sls:stage}-<functionKey>`) en vez de `!GetAtt`, porque referenciar el `Arn` real crea una dependencia circular en CloudFormation (el rol necesita el ARN de la función, la función necesita el rol). El worker debe ser idempotente respecto al progreso (siempre relee `indiceActual` de DynamoDB antes de avanzar) porque `InvocationType: 'Event'` reintenta automáticamente ante una excepción no capturada — sin idempotencia, un reintento duplicaría trabajo. Una corrida puede quedar "colgada" (`estado: 'en_progreso'` para siempre) si se agotan los reintentos de Lambda en un fallo verdaderamente fatal; se mitiga con una regla de expiración por `actualizadoEn` (10 minutos) en el endpoint que inicia una corrida nueva, en vez de bloquear al administrador indefinidamente con un `409`.

---

## 4. Dependencias instaladas

**Ya instaladas** (scaffold Angular, Tarea 1 completada):
- Runtime: `@angular/{common,compiler,core,forms,platform-browser,platform-server,router,ssr}` 22.x, `express` 5.x (servidor SSR), `rxjs` 7.8, `tslib`
- Dev: `@angular/{build,cli,compiler-cli}` 22.x, `tailwindcss` 4.x + `@tailwindcss/postcss` 4.x, `postcss`, `prettier` 3.x, `typescript` 6.x, `vitest` (test runner que usa `ng test` en Angular 22), `@types/{express,node}`, `jsdom`

**Ya instaladas** (esqueleto Serverless, Tarea completada 2026-07-17): `serverless` 4.39.0 (devDependency, versión exacta), `@codegenie/serverless-express` (wrapper Lambda del SSR), `@types/aws-lambda` (dev).

**Ya instaladas** (autenticación con Google, Tarea completada 2026-07-17): `firebase` v12.x (SDK cliente, sin `@angular/fire` — ver §7 y §9).

**Ya instaladas** (modelos + cliente DynamoDB, Tarea completada 2026-07-18): `@aws-sdk/client-dynamodb`, `@aws-sdk/lib-dynamodb` (dependencias de runtime del backend, ver §9 — instaladas con `--ignore-scripts` por un problema de red del entorno, no de los paquetes en sí, ver §7).

**Ya instaladas** (verificación de ID Token, Tarea completada 2026-07-18): `firebase-admin` ^14.2.0 (backend). Se agregó además un `overrides` en `package.json` fijando `jose` a `^5.9.6` — sin él, `firebase-admin`/`jwks-rsa` resuelven `jose@6.x` (ESM puro) y la Lambda revienta en el cold start con `ERR_REQUIRE_ESM` (ver gotcha crítico en §7).

**Ya instaladas** (escaneo de ISBN, PR #30): `@zxing/browser` (+ `@zxing/library` transitivo).

**Pendientes** (previstas en `tech-specs.md` §2, aún no instaladas): `xlsx` (reportes XLSX), `cheerio` (llega con la Tarea 2 de la iniciativa de scraping — ADR-004; recordar el gotcha de tamaño de Lambda de §7: no excluirla del empaquetado y verificar < 250 MB).

---

## 5. Configuraciones vigentes

| Campo | Valor |
|---|---|
| Proyecto Firebase (Authentication) | `comandante-letiende` — compartido con Comandante, confirmado por el usuario (ver ADR-007) |
| Dominios autorizados de Firebase Auth | `localhost`, dominios propios de Comandante (`comandante-letiende.{firebaseapp,web}.app`, `comandante.letiende.co`, preview channels `pr*`), y **`oyzau0c910.execute-api.us-east-1.amazonaws.com`** (agregado el 2026-07-17 para que `signInWithPopup` funcione en `staging` de Babel — ver gotcha en §7). Si el stack de `staging` se destruye y se vuelve a desplegar, API Gateway genera un dominio nuevo y hay que repetir este paso (o mejor, resolverlo de raíz con el dominio personalizado `babel.letiende.co` del roadmap). |
| Cuenta AWS | `696912647258` — credenciales configuradas localmente vía AWS CLI, usadas para el deploy real a `staging` del 2026-07-17 |
| Stage `staging` — API Gateway | `https://oyzau0c910.execute-api.us-east-1.amazonaws.com` (HTTP API, region `us-east-1`) |
| Stage `staging` — Lambda `api` | `arn:aws:lambda:us-east-1:696912647258:function:babel-letiende-staging-api`, rol `babel-api-role-staging` (CRUD acotado a las 5 tablas + `/index/*`, sin más permisos) |
| Stage `staging` — Lambda `usuariosMe` | `arn:aws:lambda:us-east-1:696912647258:function:babel-letiende-staging-usuariosMe`, rol `babel-usuarios-me-role-staging` (solo `dynamodb:GetItem` sobre `babel-usuarios`, ver ADR-008). Ruta `GET /api/usuarios/me`. |
| Stage `staging` — Lambda `libros` | `arn:aws:lambda:us-east-1:696912647258:function:babel-letiende-staging-libros`, rol `babel-libros-role-staging` (solo `dynamodb:Scan` sobre `babel-libros`, ver ADR-008). Ruta pública `GET /api/libros`, sin autenticación. |
| Stage `staging` — Lambda `ssr` | `arn:aws:lambda:us-east-1:696912647258:function:babel-letiende-staging-ssr`, rol `babel-ssr-role-staging` (solo `AWSLambdaBasicExecutionRole`, sin acceso a DynamoDB) |
| Stage `staging` — Tablas DynamoDB (nombre real) | `babel-libros-staging`, `babel-ventas-staging`, `babel-estantes-staging`, `babel-editoriales-descuentos-staging`, `babel-usuarios-staging` — todas `PAY_PER_REQUEST` (on-demand), tabla y GSIs. Estuvieron en `PROVISIONED` 25/25 hasta el 01/08/2026; costó US$90 en julio, ver `docs/advertencia-urgente-costos-aws.md` |
| Nombres lógicos → variables de entorno de la Lambda `api` | `TABLA_LIBROS`, `TABLA_VENTAS`, `TABLA_ESTANTES`, `TABLA_EDITORIALES_DESCUENTOS`, `TABLA_USUARIOS` (resueltos en `serverless.yml`, nunca hardcodeados en el código de negocio) |

**GitHub Actions Secrets configurados por el usuario (2026-07-17):** `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `SERVERLESS_LICENSE_KEY` (ruta de licencia de Serverless Framework v4, sin depender del dashboard — confirmado con la doc oficial que la variable de entorno exacta es `SERVERLESS_LICENSE_KEY`, no `SERVERLESS_ACCESS_KEY`; el workflow se corrigió para usarla).

**Secret `FIREBASE_SERVICE_ACCOUNT_BABEL` — RESUELTO (2026-07-18).** El usuario creó la cuenta de servicio propia de Babel en el proyecto GCP compartido `comandante-letiende` (nunca reutilizar la de Comandante — ADR-007) y la guardó como secret de GitHub Actions. El job `desplegar-staging` la inyecta en el deploy de `staging` (y de producción). **Rol IAM real necesario, distinto del sugerido originalmente:** `Firebase Authentication Admin` (`roles/firebaseauth.admin`) en Google Cloud IAM del proyecto `comandante-letiende` — el rol "Viewer" no alcanza porque `firebase-admin/auth` (`getUserByEmail`/`createUser`, usados por el script de verificación de §9) necesita permisos de administración, no solo lectura; sin este rol, cualquier llamada a la Admin Auth API falla con `auth/insufficient-permission`. El usuario lo agregó manualmente en la consola de IAM (`console.cloud.google.com/iam-admin/iam?project=comandante-letiende` → editar la cuenta de servicio → agregar rol) — confirmado funcionando el 2026-07-18.

**Verificación agnóstica de plataforma (2026-07-18):** tres mecanismos en GitHub Actions para que cualquier sesión (CLI, Web, Mobile, Desktop) pueda verificar contra `staging` real sin credenciales AWS ni de Firebase locales:
1. **Smoke test post-deploy** en el job `desplegar-staging` (`deploy.yml`): tras cada deploy de PR, hace `GET /api/health` contra staging y falla el job si no responde `200`. Extender este paso cuando existan más endpoints (p. ej. `GET /api/libros`).
2. **Workflow manual `operaciones-staging.yml`** (`workflow_dispatch`): siembra/consulta/elimina usuarios de prueba en `babel-usuarios-staging` usando los secrets de AWS del repo (operaciones `sembrar-usuario-prueba`/`consultar-usuario-prueba`/`eliminar-usuario-prueba`). Se dispara desde la UI de GitHub (Actions → "Operaciones sobre staging" → Run workflow) o con `gh workflow run operaciones-staging.yml -f operacion=... -f email=...`.
3. **Operación `probar-usuarios-me`** (mismo workflow, agregada 2026-07-18): emite un ID Token real de Firebase (custom token de `firebase-admin` + canje vía la REST API de Identity Toolkit, sin login interactivo) y llama `GET /api/usuarios/me` en `staging`. Crea y borra un usuario desechable en el proyecto Firebase compartido en cada corrida — ver `server/scripts/probar-usuarios-me.mjs`. **Operación `ver-logs-usuarios-me`** (mismo workflow): lee CloudWatch Logs de la función `usuariosMe` de los últimos 15 minutos — se usó para diagnosticar el bug de `jose`/`ERR_REQUIRE_ESM` (ver §7) sin acceso directo a AWS desde el entorno de ejecución. Agregar operaciones nuevas al mismo workflow cuando otra tarea necesite sembrar/probar/diagnosticar otras tablas o funciones.

**Deploy automático a `staging` en cada PR (2026-07-17):** `.github/workflows/deploy.yml` tiene un job `desplegar-staging` que corre en todo evento `pull_request` (después de `build-y-test`), despliega con `npx serverless deploy --stage staging` usando los mismos secrets, y comenta la URL en el PR. Con esto, cualquier sesión de Claude Code (incluida la app de Android) puede validar cambios en `staging` solo con `commit → push → PR`, sin necesidad de credenciales de AWS locales ni de un deploy manual desde esta Mac. Motivo: hasta este punto, todos los deploys de `staging` se habían hecho a mano (`npm run deploy:staging`) con las credenciales AWS configuradas únicamente en esta máquina.

Estado de secretos (actualizado 2026-07-21): `API_LETIENDE_BASE_URL` ya está **configurado** como secret de GitHub Actions (`https://api.letiende.co`, agregado por el usuario durante el PR #33; confirmado con `gh secret list`) e inyectado en el deploy de `staging`; con él, `GET /api/metadatos/:isbn` resuelve metadatos reales. `FIREBASE_SERVICE_ACCOUNT_BABEL` está resuelto (ver arriba) — el usuario rotó la clave el 2026-07-21 tras una exposición accidental al depurar (ver §1/§7). `GOOGLE_CUSTOM_SEARCH_API_KEY`/`CX` quedan **descartados**: el diseño de PVP ya no usa Google Custom Search como fallback, sino el scraping de la lista administrable de sitios (ADR-010) — si esos secrets llegaron a crearse, pueden eliminarse. Sigue pendiente el dominio personalizado `babel.letiende.co` en Route53/API Gateway/ACM (fuera de alcance actual).

---

## 6. Patrones de código establecidos

- **Frontend:** Signals para estado reactivo (`AuthService`), Standalone components, guards como `CanActivateFn` — ver `tech-specs.md` §4.1 y `CLAUDE.md` §4.
- **Modelos de datos:** una interfaz TypeScript por archivo en `src/app/core/models/`, nombre de archivo en kebab-case + sufijo `.model.ts` (ej. `descuento-editorial.model.ts`), tipos de unión de string exportados aparte cuando se reutilizan (`FormaDePago`, `RolUsuario`). Comentario TSDoc solo en el nivel de la interfaz explicando el "por qué" de negocio (referencia a `tech-specs.md`/`MEMORY.md` ADR); comentario en línea por campo únicamente si su fórmula o su rango no es autoevidente (ej. `costo`, `porcentajeDescuentoEditorial`). Sin `any`.
- **Backend — acceso a datos:** un cliente `DynamoDBDocumentClient` único por proceso (`server/api/services/dynamodb.ts`) con funciones genéricas (`obtenerPorClave`, `guardar`, `eliminar`, `consultarPorIndice`) parametrizadas por nombre de tabla — el nombre real de tabla nunca se hardcodea dentro del servicio, lo resuelve el handler que llama desde `process.env.TABLA_*` (variables ya declaradas en `serverless.yml`). Los handlers de endpoint (`server/api/handlers/*.ts`) no hablan con el SDK de AWS directamente, siempre pasan por este servicio.
- **Backend — handlers:** un archivo por endpoint/grupo en `server/api/handlers/`, tipados con `APIGatewayProxyHandlerV2`/`APIGatewayProxyResultV2` de `aws-lambda`, TSDoc de una sola frase explicando el alcance (ver `health.ts`).
- **Backend — infraestructura por endpoint (ADR-008):** cada endpoint nuevo (o grupo muy relacionado) es su propia función Lambda en `serverless.yml`, con su propio rol IAM (`Resources.<Nombre>LambdaRole`, mínimo privilegio real, no reutilizar `ApiLambdaRole` "por comodidad") y su propio `package.patterns`. Si el handler usa paquetes npm reales en runtime (no solo tipos), incluir `'node_modules/**'` en el pattern en vez de intentar un allowlist manual de rutas (frágil, ver gotcha de `jose`/`jwks-rsa` en §7).
- **Verificación/depuración de infraestructura sin credenciales locales:** `.github/workflows/operaciones-staging.yml` es la herramienta estándar del proyecto para esto — cualquier tarea que necesite datos reales en `staging` (DynamoDB) o probar un endpoint con un ID Token real de Firebase, o revisar CloudWatch Logs, agrega una nueva `operacion` a ese mismo workflow en vez de intentar credenciales AWS/GCP locales (que no existen de forma confiable en todos los entornos de ejecución, ver §7).
- **Backend — tests unitarios:** `npm run test:api` (Vitest, config en `vitest.config.ts` en la raíz, `include: ['server/**/*.spec.ts']`) — separado por completo del test runner de Angular (`ng test`/`npm test`, que solo mira `src/**`). Se invocan ambos en CI (`build-y-test`). Un handler nuevo debe exportar su lógica de validación pura (ej. `validarDatosNuevoLibro` en `libros.ts`) para poder testearla sin mocks, y mockear `../lib/verificar-token`/`../services/dynamodb` (con `vi.hoisted`, `vi.mock` de rutas relativas SÍ funciona aquí — a diferencia del builder de Angular, ver gotcha en §7) para cubrir los casos 401/403/400/2xx del handler completo.

---

## 7. Gotchas conocidos

| Situación | Solución |
|---|---|
| **(Verificado 2026-07-21)** Acceso a cámara para código de barras no funciona | Verificar HTTPS y que la solicitud se dispare desde un gesto directo del usuario (tap), no automáticamente al cargar la página — ver `CLAUDE.md` §7. Confirmado en código real de Babel (PR #30, `@zxing/browser`): el usuario probó en vivo desde su celular contra `staging` y el escaneo funcionó de punta a punta. |
| Avatar de Google devuelve 429 | Agregar `referrerpolicy="no-referrer"` en el `<img>` — mismo hallazgo que en Comandante |
| Cold start alto en la Lambda SSR | Esperado en el nivel gratuito; no usar concurrencia aprovisionada salvo que el costo lo justifique |
| **(Verificado 2026-07-17)** Con `@angular/build:application` (builder de Angular 22 con SSR), Tailwind 4 vía `postcss.config.js` (`module.exports`) no siempre se detecta de forma confiable | Usar `.postcssrc.json` en JSON puro: `{ "plugins": { "@tailwindcss/postcss": {} } }`. Confirmado funcionando en el scaffold real (PR #2): las utilidades de Tailwind se generan correctamente en el CSS de salida. |
| **(Verificado 2026-07-17)** El `.gitignore` original del bootstrap (`b4a2bc4`) traía `/package-lock.json` ignorado — pasó inadvertido mientras no existía `package.json` | Corregido en PR #2: se quitó esa línea del `.gitignore`. Recordar para cualquier proyecto bootstrapeado con una plantilla genérica de `.gitignore` antes de tener `package.json`: revisar que no ignore el lockfile, ya que `CLAUDE.md` A08 exige `npm ci` reproducible en CI. |
| **(Verificado 2026-07-17)** `tsconfig.json` de Angular 22 usa TypeScript 6.x, que deprecó `baseUrl` — los path aliases (`paths`) deben usar valores con prefijo `./` explícito y no depender de `baseUrl` | Confirmado con un import de prueba (`@core/models/...`) que compiló correctamente sin `baseUrl`, solo con `paths` apuntando a `./src/...`. |

| **⚠️ (Root-caused y corregido 2026-07-19, CRÍTICO)** El SDK cliente `firebase` (`onAuthStateChanged`) persiste la sesión SOLO en almacenamiento del navegador (IndexedDB) — no existe ninguna cookie de sesión que el Lambda `ssr` pueda leer. Cualquier ruta con `RenderMode.Server` protegida por `AuthGuard`/`RoleGuard` se evalúa en el servidor en cada carga completa de página/navegación directa/refresh, donde `AuthService` nunca inicializa el SDK real (`isPlatformBrowser`, ver gotcha de arriba) — el guard siempre ve `usuario() === null` y redirige a `/login`, **sin importar que el usuario tenga una sesión real y válida en su navegador**. Pasó desapercibido durante toda la vida del proyecto porque ninguna verificación anterior de `AuthGuard`/`RoleGuard` usó un login interactivo real (todas usaban `curl`, que nunca porta una sesión de navegador, o tests unitarios con servicios mockeados) — recién se detectó cuando el usuario probó `/catalogar` con su cuenta real de Google (primera vez, ver §9). | Para cualquier ruta protegida por un guard que dependa de `AuthService`/Firebase, usar `RenderMode.Client` (CSR puro) en vez de `RenderMode.Server` en `app.routes.server.ts` — el guard evalúa así enteramente en el navegador, donde sí vive la sesión real. Además, `onAuthStateChanged` es asíncrono incluso al restaurar una sesión ya persistida: se agregó `AuthService.esperarListo()` (promesa que resuelve tras el primer callback, o de inmediato en servidor sin SDK) y `AuthGuard`/`NoAuthGuard`/`obtenerIdToken()` la esperan antes de leer el Signal, para no expulsar a un usuario real por leerlo demasiado pronto. No hay ningún cambio de seguridad real en esto — los guards siempre fueron solo UX (`CLAUDE.md` A01/A07), la autorización real la sigue verificando siempre la Lambda `api`. **Lección de proceso:** cualquier guard nuevo debe probarse con un login real de un humano en un navegador real antes de darlo por verificado — ni `curl` ni los tests unitarios pueden detectar esta clase de bug. |
| **(Confirmado 2026-07-19)** Un endpoint puede necesitar niveles de rol distintos para lectura vs. escritura, incluso compartiendo el mismo handler/Lambda (ADR-008 permite agrupar endpoints muy relacionados) — `GET /api/estantes` originalmente exigía `administrador` exclusivamente (decisión de diseño explícita de esa tarea, ver `MEMORY.md` histórico), pero `CatalogarLibroComponent` (tarea posterior) necesita que un `vendedor` pueda leer la lista para elegir estante, y nadie había revisado si esa restricción seguía siendo correcta al construir una feature nueva sobre un endpoint ya cerrado. Detectado por el usuario probando `/catalogar` en vivo: `403` en `GET /api/estantes` con el mensaje "No se pudieron cargar los estantes." | Al construir una feature nueva que consume un endpoint ya existente, releer sus restricciones de rol actuales en vez de asumir que siguen siendo correctas para el nuevo caso de uso. Patrón aplicado en `estantes.ts`: dos helpers de verificación separados (`exigirAdministrador` para `POST`/`PUT`/`DELETE`, `exigirVendedorOAdministrador` solo para `GET`), en vez de una única función de rol para todo el handler. |
| **(Verificado 2026-07-17)** Angular 22 con SSR protege contra SSRF exigiendo una lista explícita de hosts permitidos (`AngularNodeAppEngine`); sin ella, la Lambda `ssr` responde 400 a cualquier host, incluido el dominio real que genera API Gateway | Configurar `NG_ALLOWED_HOSTS` con el host real (`${HttpApi}.execute-api.${AWS::Region}.amazonaws.com` en `serverless.yml`) — no es necesario tener ya un dominio personalizado para que funcione. |
| **(Verificado 2026-07-17)** El build SSR de Angular (`dist/babel-letiende/server/server.mjs`) exporta una instancia de Express pensada para `app.listen()`, no un handler Lambda | Envolver esa misma instancia con `@codegenie/serverless-express` (`server/ssr/handler.mjs`) en vez de reimplementar el bootstrap SSR; `src/server.ts` solo necesita `export { app }` adicional. |
| **(Verificado 2026-07-17)** El SDK cliente `firebase` (`initializeApp`/`getAuth`) depende de almacenamiento del navegador (indexedDB/localStorage); inicializarlo sin guardas durante el renderizado SSR de la Lambda `ssr` rompería el prerenderizado | Inicializar Firebase solo si `isPlatformBrowser(inject(PLATFORM_ID))`; en servidor, el Signal de usuario permanece `null` sin error. Confirmado: el build SSR sigue prerenderizando correctamente con `AuthService` en el árbol de providers. |
| **(Verificado 2026-07-17)** `angular.json` con el builder `@angular/build:application` (Angular 22) sigue soportando `fileReplacements` por configuración, igual que en versiones anteriores de Angular | No cambió el mecanismo — se puede seguir usando `fileReplacements` en `architect.build.configurations.<config>` para alternar `environment.ts`/`environment.development.ts`. |
| **⚠️ (Verificado 2026-07-17, CRÍTICO)** `src/app/app.routes.server.ts` trae por defecto (`ng new --ssr`) `{ path: '**', renderMode: RenderMode.Prerender }` — esto genera HTML estático en **build time** y significa que los guards (`AuthGuard`/`NoAuthGuard`, y cualquier guard futuro) **nunca se ejecutan por petición** en el Lambda `ssr` desplegado. Detectado porque `/libros` servía `200` en vez de redirigir a `/login` en `staging`, mientras que en el dev server local (`ng serve`, que sí hace SSR real por petición) el mismo guard funcionaba bien — el dev server local puede ocultar este bug. | Usar `RenderMode.Server` (no `Prerender`) para cualquier ruta protegida por un guard. Solo usar `Prerender` en rutas genuinamente públicas y sin variación por usuario (p. ej. el futuro catálogo público, si se decide optimizar para SEO) — nunca en rutas con `canActivate`. Verificar SIEMPRE con un deploy real (no solo `ng serve`) antes de confiar en que un guard funciona en producción. |
| **(Verificado 2026-07-17)** La tipografía de marca **Angellya** (`CLAUDE.md` §4, `tech-specs.md` §4.4) no existe como archivo de fuente en ningún repo de Le Tiende — se buscó en `comandante/` (repo hermano) y solo aparece **vectorizada como paths dentro de los SVG de logo** (`logo_negro_sin_fondo.svg`/`logo_blanco_sin_fondo.svg`), nunca cargada como webfont; Comandante tampoco la usa en su propio código (su `login.component.ts` no aplica ningún `font-family` de Angellya al título) | Hasta que el usuario provea el archivo real (`.woff2`/`.otf`/`.ttf`), todo texto de marca usa Poppins como fallback (igual que hace Comandante en la práctica, aunque su `DESIGN.md` documente Angellya). No inventar ni descargar una fuente sustituta sin confirmar con el usuario. |
| **(Resuelto 2026-07-17)** El "favicon.ico" de Comandante solo tenía el monograma "tt" a 16×16/32×32 px | El usuario ubicó el set completo ya generado en `~/Documents/LeTiende/letiende.co/fuentes/logos_monos_png_ico/favicon_io/` (16/32/180/192/512 + `.ico` multi-resolución) y el SVG fuente en `logos_monos_svg/mono_naranja.svg` — copiado a `public/` (ver §9). El ícono de 512px tiene el trazo casi al borde (solo ~3% de margen horizontal, `magick -trim` confirma 481×343 dentro de un lienzo 512×512): **no se declaró `purpose: "maskable"` en el manifest**, solo `"any"`, porque no cumple el margen de seguridad recomendado (~10%) y un recorte circular del SO cortaría el trazo. |
| **(Cerrado 2026-07-18)** La fuente **Angellya** no existía en ningún repo de Le Tiende | El usuario la ubicó en `~/Documents/LeTiende/ImagenVisual/tipografías/angellya/Angellya.{otf,ttf}`, pero decidió que **NO se usará en el desarrollo de Babel**: es tipografía exclusiva del logo, y los logos ya existen generados (SVG en `public/`). No integrar `@font-face` ni commitear el archivo de fuente. Si en el futuro se necesitara, se retomará desde esa ruta local (solo existe en la Mac del usuario). Toda la tipografía de interfaz es Poppins. |
| **⚠️ (Verificado 2026-07-17, CRÍTICO)** `signInWithPopup` abre y cierra el popup inmediatamente, sin mostrar el selector de cuenta de Google, y `AuthService`/`LoginComponent` capturan un `auth/unauthorized-domain` genérico ("No se pudo iniciar sesión...") — Firebase Auth valida el dominio actual contra la lista de "Authorized domains" del proyecto **antes** de completar el flujo del popup; un dominio de API Gateway generado automáticamente (`*.execute-api.*.amazonaws.com`) nunca está ahí por defecto | Agregar el dominio exacto a la lista (Firebase Console → Authentication → Settings → Authorized domains, o vía la API v2 `identitytoolkit.googleapis.com/v2/projects/{project}/config` con `updateMask=authorizedDomains`, preservando los dominios existentes — **nunca sobrescribir la lista completa**, es un proyecto compartido con Comandante). Confirmado en esta sesión: se agregó `oyzau0c910.execute-api.us-east-1.amazonaws.com` (ver §5). Recordar para cualquier dominio nuevo (`production`, un dominio personalizado, u otro stage). |

| **(Verificado 2026-07-17, benigno — sin acción)** Tras el fix de dominio autorizado, Chrome muestra en consola `Cross-Origin-Opener-Policy policy would block the window.close call` durante `signInWithPopup`, aunque el login se completa correctamente | Warning conocido y confirmado como cosmético (ver issues `firebase-js-sdk` [#8541](https://github.com/firebase/firebase-js-sdk/issues/8541)/[#8295](https://github.com/firebase/firebase-js-sdk/issues/8295)): ocurre porque el dominio de Google/Firebase donde corre el popup tiene su propio COOP restrictivo, y el navegador bloquea la llamada a `window.close()` cross-origin — el SDK completa el login por otra vía igual. Solo pasa en Chrome. Decisión del usuario: no agregar el header `Cross-Origin-Opener-Policy: same-origin-allow-popups` en el servidor SSR por ahora, ya que no hay garantía de que elimine el warning (la política restrictiva vive del lado de Google) y no hay ningún problema funcional que arreglar. |

| **(Verificado 2026-07-18)** `npm install` completo falla en entornos sandbox sin salida a internet general (aunque el registro de npm sí sea alcanzable): el `postinstall` de la devDependency `serverless` intenta descargar un release y aborta todo el `npm install`, incluidos paquetes nuevos que no tienen nada que ver con `serverless` | Instalar con `--ignore-scripts` cuando el fallo sea específicamente el `postInstall.js` de `serverless` (confirmar el error exacto antes de usarlo, no como solución genérica). Verificado: los paquetes nuevos (`@aws-sdk/*`) quedaron correctamente en `package.json`/`package-lock.json` y `node_modules` tras reintentar así. |
| **(Verificado 2026-07-18)** Angular CLI 22 exige Node `^22.22.3 \|\| ^24.15.0 \|\| >=26.0.0`; algunos entornos traen preinstalado un patch inferior (`v22.22.2`) que falla `ng build`/`ng test` con un mensaje claro, pero no es evidente que sea solo un patch de diferencia | Si hay `nvm` disponible, `nvm install 22.22.3` (o la versión mayor/menor equivalente) resuelve sin tocar ninguna configuración del proyecto — no es un problema del repo, es del entorno de ejecución. |
| **(Confirmado 2026-07-18)** Las credenciales AWS (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`) presentes como variables de entorno en un entorno de ejecución en la nube (a diferencia de la Mac del usuario o del runner de GitHub Actions) no necesariamente son válidas contra la cuenta real `696912647258` — un intento de `guardar()`/`obtenerPorClave()` real contra `babel-estantes-staging` devolvió `UnrecognizedClientException` | No asumir que cualquier sesión tiene acceso real a AWS solo porque las variables de entorno existen. La verificación en vivo contra `staging` sigue dependiendo de GitHub Actions (que sí tiene los secrets reales) o de una sesión con credenciales confirmadas — y solo se ejerce genuinamente cuando existe al menos un handler que invoque el código en cuestión (ver `TODO.md`, aviso de prioridad). |
| **⚠️ (Verificado 2026-07-18, CRÍTICO)** `npm install firebase-admin` resuelve `jose@6.x` como dependencia transitiva (vía `jwks-rsa`), y `jose@6` es **ESM puro** (`"type": "module"`, sin entrypoint CJS). `server/tsconfig.json` compila a CommonJS a propósito (para no romper las rutas `dist-server/api/handlers/*.js` ya usadas en `serverless.yml`). El resultado: la Lambda compila sin errores localmente (`tsc` no detecta el problema, es puramente de resolución de módulos en runtime) y **truena en el cold start real** con `Error [ERR_REQUIRE_ESM]: require() of ES Module .../jose/dist/webapi/index.js ... not supported`, ANTES de ejecutar cualquier código propio — API Gateway lo reporta como un `500` genérico (`{"message":"Internal Server Error"}`) sin ninguna pista. Solo se pudo diagnosticar leyendo CloudWatch Logs directamente (ver operación `ver-logs-usuarios-me` en §5). | Fijar `jose` a una versión con entrypoint CJS real vía `"overrides"` en `package.json` (se usó `^5.9.6`). Verificar con `require('firebase-admin/auth')` (o el paquete que importe `jwks-rsa`/`jose`) en Node CJS puro ANTES de desplegar — no basta con que `tsc`/`npm run build:api` compilen sin errores. Cualquier dependencia nueva del backend que internamente use `jose` (o cualquier paquete que haya migrado a ESM-only) puede reproducir este mismo problema. |
| **(Verificado 2026-07-18, REINCIDENTE 2026-07-30, ACTUALIZADO 2026-08-19)** AWS Lambda rechaza el deploy si `functions.<nombre>.description` en `serverless.yml` supera 256 caracteres — CloudFormation falla ANTES de crear/actualizar cualquier recurso ("must NOT have more than 256 characters"). Las dos reincidencias anteriores (`usuariosMe` 2026-07-18, `librosPorIsbn`/`fusionarDuplicadoLibro` 2026-07-30) ocurrieron creyendo que `npx serverless print` no corría esa validación — **ya no es así**: con `serverless` 4.41.0 (resuelto por el rango `4.39.0` de `package.json`), `npx serverless print --stage dev` SÍ reporta `[!] Invalid configuration encountered ... must NOT have more than 256 characters` y termina con código de salida distinto de 0, verificado en vivo al agregar las funciones de "Validar libros" (2 de 3 excedían el límite, detectadas y corregidas antes de cualquier commit). No está confirmado si el comportamiento cambió entre versiones de `serverless` o si las corridas anteriores usaron el comando de forma distinta — de cualquier forma, la mitigación vigente hoy es correr `npx serverless print --stage dev` (barato, sin credenciales AWS) antes de cualquier PR que agregue/edite un `description:` de función, y tratar cualquier salida con `[!] Invalid configuration` como bloqueante. | Correr `npx serverless print --stage dev` tras agregar/editar funciones y revisar que no imprima `[!] Invalid configuration encountered` — ya detecta el límite de 256 caracteres en la versión actual. Como respaldo (por si una versión futura deja de validarlo, o por costumbre de doble-chequeo), sigue siendo válido contar caracteres explícitamente antes de un deploy real. |
| **⚠️ (Confirmado 2026-07-18)** Una cuenta de servicio de Firebase recién creada NO tiene automáticamente permiso para administrar usuarios de Firebase Authentication (`getUserByEmail`, `createUser`, etc. de `firebase-admin/auth`) — falla con `FirebaseAuthError: ... insufficient permission ..., code: 'auth/insufficient-permission'` aunque las credenciales sean válidas y `initializeApp()` no lance ningún error | Otorgar explícitamente el rol `Firebase Authentication Admin` (`roles/firebaseauth.admin`) a la cuenta de servicio en Google Cloud IAM (`console.cloud.google.com/iam-admin/iam?project=<projectId>`) — un rol de solo lectura ("Viewer") no alcanza para estas operaciones. Verificar/documentar el rol exacto que cada operación de `firebase-admin` necesita antes de asumir que "tener una cuenta de servicio" es suficiente. |
| **⚠️ (Confirmado 2026-07-19, CRÍTICO)** Cuando dos funciones Lambda comparten el mismo archivo de handler compilado (ADR-008 permite esto: `libros.js` exporta tanto `handler` (GET, función `libros`) como `handlerCrear` (POST, función `catalogarLibro`)), el `package.patterns` de **cada** función debe incluir TODOS los archivos que ese módulo importa a nivel superior — no solo los que usa el handler específico de esa función. `handlerCrear` importa `verificar-token.ts`; ese `import` está en la parte de arriba del archivo, así que se ejecuta al cargar el módulo sin importar qué export se invoque. El `package.patterns` de `libros` (GET) no incluía `verificar-token.js` (solo lo tenía `catalogarLibro`) — el resultado: `GET /api/libros`, que nunca toca `verificar-token`, igual reventaba con `Runtime.ImportModuleError: Cannot find module '../lib/verificar-token'` en el cold start, un `500` genérico sin pista alguna en la respuesta de API Gateway. Pasó desapercibido en el mismo PR que introdujo `handlerCrear`: el build/tests locales pasan (TypeScript no valida qué queda empaquetado), y la verificación en vivo de esa tarea probó `POST` pero no volvió a probar `GET` después del deploy. Se detectó recién al re-verificar manualmente TODOS los endpoints tras un deploy posterior, y se diagnosticó con la misma técnica que el gotcha de `jose` (leer CloudWatch Logs vía una operación nueva, `ver-logs-libros`, en `operaciones-staging.yml`). | Al agregar un handler nuevo a un archivo que YA es usado por otra función Lambda, revisar el `package.patterns` de **todas** las funciones que apuntan a ese archivo, no solo la nueva — un `import` de nivel superior nuevo afecta a todos los exports del módulo por igual. Como hábito de verificación: después de cualquier deploy, volver a probar los endpoints que YA estaban funcionando (no solo el nuevo) — el smoke test de `deploy.yml` solo cubre `/api/health`, no basta. |
| **(Confirmado 2026-07-19)** El AWS CLI (usado por los pasos `aws dynamodb ...` de `operaciones-staging.yml`) resuelve la región con `AWS_DEFAULT_REGION`, pero el SDK de JavaScript v3 (`@aws-sdk/client-dynamodb`, usado por scripts como `probar-catalogar-libro.mjs`) **no** cae de vuelta a esa variable — solo lee `AWS_REGION`. Un `new DynamoDBClient({})` sin región explícita falla con `Error: Region is missing`, y como esto ocurre en el bloque `finally` de limpieza (después de que la petición HTTP real ya se completó con éxito), el run entero se reporta como fallido en CI aunque el endpoint bajo prueba haya funcionado perfectamente — confirmado en un run real de `probar-catalogar-libro`: `POST /api/libros` respondió `201` correctamente, pero el script murió al intentar borrar el libro de prueba, dejándolo huérfano en `babel-libros-staging`. | Declarar **ambas** variables (`AWS_DEFAULT_REGION` y `AWS_REGION`, mismo valor) en el `env:` del job de cualquier workflow que mezcle llamadas del AWS CLI con scripts que usen el SDK de JS v3 — no asumir que una cubre a la otra. Cualquier script nuevo que instancie un cliente de `@aws-sdk/*` debe además pasar `region` explícitamente como respaldo (ver `probar-catalogar-libro.mjs`). Se agregó también una operación `eliminar-libro-prueba` a `operaciones-staging.yml` como red de seguridad manual para limpiar huérfanos si algo similar vuelve a fallar. |
| **⚠️ (Root-caused 2026-07-19)** `@angular/build:unit-test` trae `isolate: false` **por defecto** ("para alinearse con la experiencia de Karma/Jasmine", según su propio schema) — todos los archivos `*.spec.ts` corren en el mismo hilo/proceso, compartiendo el registro de módulos de Vitest. Cualquier archivo que importe (aunque sea solo como token de DI) algo que jale el SDK real de `firebase/auth`/`firebase/app` sin mockearlo puede pisar el `vi.mock('firebase/auth', ...)` de `auth.service.spec.ts`, rompiendo sus tests de forma **no determinística** (dependiente del orden/paralelismo real con el que Vitest agenda los archivos). El primer intento de solución (mockear `firebase/app`/`firebase/auth` también en cada archivo que toca `AuthService` — `usuarios.service.spec.ts`, `role.guard.spec.ts`, luego también `auth.guard.spec.ts`/`no-auth.guard.spec.ts`) **no fue suficiente**: el flake reapareció una tercera vez al agregar archivos de prueba nuevos que ni siquiera importan `AuthService` (`libros.service.spec.ts`, `catalogo-publico.component.spec.ts`, `absolute-url.interceptor.spec.ts`, tarea del catálogo público) — el mero cambio en la cantidad de archivos alteró el agendamiento y volvió a exponer la carrera. Confirma que el problema real no es "qué mockea cada archivo" sino la falta de aislamiento del builder. | **Fix de raíz:** agregar `"isolate": true` a las opciones del target `test` en `angular.json` (`architect.test.options.isolate`) — hace que Vitest corra cada archivo de prueba en su propio hilo/proceso, con su propio registro de módulos, eliminando la clase completa de bug (no solo el síntoma puntual con Firebase). Confirmado estable en 6 corridas consecutivas de `npm test -- --watch=false` tras aplicarlo (24 tests). Costo: el tiempo de `setup` subió de ~1.1s a ~3.3s (aceptable dado el tamaño actual de la suite). Los mocks de `firebase/app`/`firebase/auth` ya agregados en los 4 archivos que tocan `AuthService` se dejaron como está (son correctos y no dependen de real Firebase de todas formas) pero ya no son la defensa principal — con `isolate: true` cualquier archivo nuevo está protegido automáticamente, sin necesidad de recordar mockear Firebase manualmente. |

| **(Confirmado 2026-07-20, infraestructura de GitHub, no del repo)** Un job de `deploy.yml` (`build-y-test`, sin ninguna restricción de `concurrency` propia ni otros runs compitiendo en el repo) quedó ~20 minutos en estado `queued`, con el log mostrando únicamente `"Waiting for a runner to pick up this job..."` — confirmado por el usuario directamente desde la UI de GitHub, no un artefacto de polling obsoleto de las herramientas de este agente (a diferencia de los dos incidentes de "polling stale" documentados en sesiones anteriores, donde el run ya había terminado y solo la herramienta mostraba datos congelados). | Cancelar el run (`actions_run_trigger` método `cancel_workflow_run`) y volver a lanzarlo (`rerun_workflow_run`) — el segundo intento consiguió runner y corrió con éxito en segundos. Causa raíz no diagnosticable desde este entorno (posible degradación puntual de runners hosteados de GitHub); si se repite con frecuencia, vale la pena revisar límites de jobs concurrentes de la cuenta en Settings → Actions. |

| **(Confirmado 2026-07-20, RESUELTO 2026-07-20)** El GSI `vendidoEn-index` de `babel-ventas` (`serverless.yml`, declarado desde el esqueleto de Serverless Framework, 2026-07-17) solo tenía `vendidoEn` como clave de partición (`KeySchema` con un único `HASH`, sin `RANGE`) — a pesar de que `tech-specs.md` §5.1 lo describía como "GSI por `vendidoEn` (rango de fecha) para reportes". Un GSI con partición-únicamente en DynamoDB solo permite `Query` por igualdad exacta de esa partición, nunca por rango — es decir, solo servía para buscar "la venta del timestamp exacto con milisegundos", inservible. Se detectó al implementar `GET /api/ventas` (primer consumidor real de "reportes por rango de fecha"); antes de eso ningún handler lo había usado jamás. Peor aún: reservaba 25/25 RCU/WCU aprovisionados propios (aparte de los de la tabla) y duplicaba el almacenamiento de cada venta — capacidad pagada por no hacer nada, contra el objetivo de costo $0. | **Resuelto eliminando el GSI** (decisión del usuario, PR posterior al #26): se quitó de `TablaVentas` en `serverless.yml` (junto con la `AttributeDefinition` de `vendidoEn`, que CloudFormation rechaza si queda declarada sin uso en ningún `KeySchema`) y la referencia `${TablaVentas.Arn}/index/*` del `ApiLambdaRole`. NO se rediseñó como GSI "bien hecho" (partición fija + `vendidoEn` como rango) a propósito: a este volumen (miles de ventas) el `Scan` + filtrado en memoria de `GET /api/ventas` es el mismo criterio ya aceptado para tablas pequeñas, y un GSI de partición fija única sería a su vez un anti-patrón de partición caliente — YAGNI. Si el volumen algún día lo exige, diseñar el GSI real en ese momento. `tech-specs.md` §5.1 corregido para reflejar la realidad. Lección general: revisar que cada GSI declarado tenga la forma de clave que sus consultas previstas necesitan ANTES de que llegue el primer consumidor — este estuvo mal declarado 3 días y nadie lo notó porque nada lo usaba. |

| **⚠️ (Root-caused y corregido 2026-07-21, CRÍTICO)** Las 9 funciones Lambda de backend que verifican tokens (`usuariosMe`, `libros`, `catalogarLibro`, `cambiarEstanteLibro`, `estantes`, `usuarios`, `editorialesDescuentos`, `registrarVenta`, `listarVentas`) empaquetan `node_modules/**` completo a propósito (ver patrón en §6 — evita el allowlist frágil que ya causó el incidente de `jose`/`jwks-rsa` de más abajo). Pero `node_modules` había crecido a ~570MB porque ese mismo glob también arrastra paquetes que **ningún** handler de backend usa: `@angular/**` (51MB), el SDK cliente `firebase` — no `firebase-admin`, son paquetes npm distintos — (45MB), `rxjs` (11MB), y herramientas de desarrollo completas (`typescript`, `vite`, `vitest`, `tailwindcss`, `serverless` mismo, `@babel/*`, `@esbuild/*`, `zod`/`@algolia`/`algoliasearch` vía `@angular/cli`). El deploy a `staging` falló recién al agregar `@zxing/browser` (30MB, PR #30): `EstantesLambdaFunction - Unzipped size must be smaller than 262144000 bytes`. El error señaló solo esa función porque CloudFormation solo reevalúa el tamaño de una función Lambda cuando su código realmente cambia de hash — cualquier cambio a `package-lock.json` toca el hash de las 9 funciones por igual (todas empaquetan el mismo `node_modules`), así que probablemente **ya estaban todas sobre el límite** desde hace tiempo y ninguna había vuelto a subir código desde entonces para disparar el chequeo. | Se agregaron exclusiones a `package.patterns` en las 9 funciones, verificadas una por una con `npm ls <paquete>` contra el árbol real de dependencias (nunca adivinadas) — cada función bajó de ~574MB a ~103MB descomprimidos. El caso delicado fue `@firebase/**` (112MB): no se puede excluir entero porque `firebase-admin` sí depende de 9 subpaquetes suyos (`@firebase/database-compat`, `database`, `database-types`, `logger`, `util`, `component`, `app-check-interop-types`, `auth-interop-types`, `app-types` — arrastrados por su soporte de Realtime Database, aunque Babel no lo use en absoluto, `CLAUDE.md` ya aclara que Babel usa Firebase únicamente para autenticación) — se excluye casi todo el árbol y se re-incluyen exactamente esos 9 con patrones positivos después del negativo (mismo truco `'!**'` + includes explícitos que ya usa cada función en la raíz de `patterns`). Verificado con `npx serverless package --stage staging` (mide el tamaño real de cada zip) y con `require()`/`import()` directo de `firebase-admin`, `@aws-sdk/*` y el handler compilado desde el árbol ya podado, para no repetir el incidente de `Runtime.ImportModuleError` de más abajo (un `tsc`/build limpio NO garantiza que el paquete final cargue en runtime). Serverless Framework exige que cada patrón sea un string plano — un YAML anchor referenciado como un solo elemento de la lista queda anidado (`must be string`) y no compila, así que la lista de exclusiones se repite literal en las 9 funciones en vez de compartirse. **Cualquier dependencia nueva agregada a `package.json` (`dependencies` o `devDependencies`) debe revisarse con `npm ls` y, si es pesada y no la usa el backend, agregarse a la lista de exclusión de las 9 funciones — o repetir este incidente.** |

| **(Confirmado 2026-07-21)** La API externa `api.letiende.co/libros?barcode=<isbn>` (proxy de Google Books) responde `500`/`503` de forma **intermitente incluso con ISBNs válidos** — probablemente cuota/rate-limit de Google Books aguas arriba. Un usuario reportó "sin resultado" en `staging` con un ISBN real; el DevTools mostró que el backend respondió `200` con campos `null`, y la misma petición manual segundos después devolvió `200` con datos correctos. | El cliente `server/api/services/api-letiende.ts` **nunca lanza** y **reintenta una vez** (800ms) ante un fallo de transporte/HTTP no-200 antes de degradar a "no encontrado"; un `200` con `{}` (no encontrado legítimo) no reintenta. Se añadió un `console.error` cuando ambos intentos fallan (para diagnosticar vía CloudWatch). La cadena de fallbacks de scraping (iniciativa en curso, `plan-obtencion-info-libros.md`) es la mitigación de fondo para esta inestabilidad. |
| **⚠️ (Incidente 2026-07-21)** Al depurar el PR #33 se corrió `aws lambda get-function-configuration` sobre `babel-letiende-staging-metadatos`, cuya salida incluye TODAS las variables de entorno — entre ellas `FIREBASE_SERVICE_ACCOUNT_BABEL`, imprimiendo la **clave privada** del service account en el transcript. | Nunca volcar la configuración completa de una Lambda que tenga secretos en `environment`. Para inspeccionar una Lambda, filtrar los campos (`--query`) para excluir `Environment`, o usar CloudWatch Logs. El usuario rotó la clave del service account tras el incidente. Regla general: antes de imprimir salida de AWS/infra, verificar que no arrastre secretos. |
| **⚠️ (Root-caused y corregido 2026-07-22, CRÍTICO — riesgo real de pérdida de datos)** El stack de CloudFormation `babel-letiende-staging` es **uno solo, compartido por todos los PR abiertos** (`deploy.yml` corre `serverless deploy --stage staging` en cada evento `pull_request`, siempre contra el mismo stage). Cada deploy reconcilia el stack COMPLETO contra el `serverless.yml` de esa rama específica — si una rama no incluye un recurso que otra rama (aún sin fusionar) sí agregó, CloudFormation lo interpreta como "recurso eliminado" y lo borra. Pasó en la práctica: el PR #35 (`feature/gestion-sitios-scraping`) desplegó la tabla `babel-sitios-scraping`, su función Lambda y su rol IAM; minutos después, el PR #36 (`fix/google-signin-selector-cuenta`) — creado desde `main` ANTES de que el PR #35 se fusionara, así que su `serverless.yml` no tenía esos recursos — desplegó y **CloudFormation eliminó la tabla, la función y el rol completos**. Detectado porque el usuario no podía sembrar los sitios semilla: `aws dynamodb describe-table` devolvía `ResourceNotFoundException` pese a que el deploy del PR #35 había reportado éxito. Con datos reales en la tabla (no una siembra vacía), este mismo patrón habría borrado esos datos sin ningún aviso — ninguno de los dos deploys reportó error. | **Mitigación aplicada:** se volvió a ejecutar (`gh run rerun --job`) el job "Deploy a staging" del PR #35 para restaurar la tabla/función/rol (confirmado con `aws dynamodb describe-table` y `aws lambda get-function`), y se sembraron los 4 sitios directamente con `aws dynamodb put-item` (mismo resultado final que hacerlo desde la UI, sin depender de una sesión de administrador). **Regla de proceso hacia adelante:** cuando dos o más PRs abiertos tocan `serverless.yml` (agregan tablas/funciones/roles nuevos), el orden de fusión importa — fusionar el PR que agrega infraestructura nueva cuanto antes (para que los PRs subsiguientes partan de una rama que ya la incluye), o evitar re-desplegar/re-pushear una rama vieja mientras haya infraestructura nueva sin fusionar en otro PR abierto. Si se sospecha este problema, verificar con `aws cloudformation describe-stack-resources` que los recursos esperados sigan existiendo antes de asumir que un deploy anterior "sigue en pie". No hay (todavía) un mecanismo de stacks aislados por PR (`stage` por rama) — evaluarlo si este patrón se repite con tablas que sí tengan datos de producción.

**Se repitió una segunda vez el mismo día, de forma irónica:** al abrir el PR #37 (que documentaba este mismo incidente) desde una rama creada antes de fusionar el PR #35, su propio deploy a `staging` volvió a borrar la tabla/función/rol. Y al fusionar los PR #35/#36/#37 a `main` en menos de 1 minuto, cada merge disparó su propio deploy a **producción** en paralelo (ese job no tenía `concurrency`, a diferencia de `desplegar-staging`) — 2 de los 3 deploys a producción fallaron con `Stack ... is in UPDATE_IN_PROGRESS state and can not be updated`. Por suerte, el deploy que sí completó (el del merge de PR #36) ya incluía el estado acumulado de los tres merges (el checkout de un evento `push` a `main` toma el commit ya fusionado, no la rama original del PR), así que producción terminó `UPDATE_COMPLETE` con todo — confirmado con `aws cloudformation describe-stack-resources` (tabla, función y rol de `sitios-scraping` presentes). Fue suerte de timing, no una garantía: **se agregó `concurrency` (grupo `desplegar-produccion`, `cancel-in-progress: false`) al job de producción en `deploy.yml`**, mismo patrón que ya protegía a `staging`, para que fusiones rápidas sucesivas se encolen en vez de chocar. Para restaurar `staging` (que sí quedó vacío y sin ningún PR abierto para redesplegarlo, ya que fusionar a `main` NUNCA dispara un deploy a `staging` — solo `pull_request` lo hace), se abrió un PR nuevo desde el `main` ya completo, solo para forzar el redeploy. |

| **(Confirmado 2026-07-25)** Un `<select>` nativo de HTML sin `ngModel`/`formControlName` (property binding directo `[value]="signal()"` en el propio `<select>`) no selecciona la opción correcta si sus `<option>` (generadas por `@for`) todavía no existen en el DOM en el momento exacto en que Angular aplica ese binding — Angular procesa las instrucciones del `<select>` padre antes de instanciar sus hijos, así que `.value` se asigna contra un `<select>` vacío. El navegador no reintenta la asignación después, aunque la `<option>` coincidente se agregue en la misma pasada de change detection. Detectado en `CatalogoPublicoComponent` al escribir un test de pre-selección desde query params (`?espacio=...`): el `<select>` quedaba en `''` en vez del valor esperado. | Usar `[selected]="condición"` en cada `<option>` individual en vez de `[value]` en el `<select>` — se evalúa en el momento correcto porque se aplica cuando esa `<option>` puntual ya existe. Aplica a cualquier `<select>` fuera de un `FormGroup` (sin `ngModel`/`formControlName`, que sí tienen su propio mecanismo de reconciliación vía `SelectControlValueAccessor`). |

| **⚠️ (Root-caused y corregido 2026-08-19, CRÍTICO)** Agregar un campo nuevo NO opcional a un tipo respaldado por una tabla DynamoDB (ej. `SitioScraping.palabrasClaveInvalidas: string[]`), con default aplicado solo en el path de escritura (`POST`/`PUT`), NO alcanza a las filas que ya existían en la tabla antes del cambio — `escanearTodo`/`Scan` devuelve el ítem exactamente como quedó guardado en su momento, sin aplicar ningún default del tipo TypeScript actual; el atributo simplemente está ausente para esas filas, así que el valor real en runtime es `undefined`, no `[]`, pese a que el tipo lo declara no-opcional. Cualquier consumidor que confíe en el tipo (`sitio.palabrasClaveInvalidas.length`, `.join(', ')`) lanza `TypeError` en runtime con datos reales, aunque `tsc`/todos los tests con fixtures "completos" pasen limpios. En Babel rompió dos rutas reales a la vez: `GestionSitiosScrapingComponent` (`/admin/sitios` se veía incompleta, sin botones "Eliminar") y, más grave, `GET /api/metadatos/:isbn` (ruta crítica de catalogación) con `500` en cuanto un sitio guardado antes de la migración lograba scrapear una portada. | Al agregar un campo nuevo a un tipo que ya tiene filas reales en DynamoDB (o cualquier almacenamiento sin schema), normalizar explícitamente (`?? valorPorDefecto`) en el/los punto(s) donde se lee esa tabla (`Scan`/`Query`/`GetItem`), no solo donde se valida la escritura — el tipo de TypeScript no protege contra datos históricos que lo violan. Escribir al menos un test con un fixture que deliberadamente OMITE el campo nuevo (no solo fixtures que ya lo incluyen en `[]`), para que la suite hubiera detectado este bug antes de llegar a `staging`. Aplica a cualquier campo nuevo agregado de aquí en adelante a `Libro`, `Venta`, `Usuario`, o cualquier otro tipo con datos reales ya persistidos. |
| **(Confirmado 2026-08-19)** En un `FormGroup` reactivo, llamar `formulario.controls.X.setValue(valor)` de forma programática (no vía un evento DOM real) y luego `fixture.detectChanges()` NO actualiza en el DOM un `@if`/binding del template que lee `formulario.controls.X.value` directamente — el valor SÍ cambia en el control (confirmado leyéndolo justo después), pero el template sigue mostrando la rama vieja incluso llamando `detectChanges()` dos veces seguidas. Confirmado con un repro aislado (`TestBed.createComponent` mínimo, sin ningún mock adicional) y reproducido también contra código YA EXISTENTE antes de la tarea que lo encontró (`catalogar-libro.component.html`, el `@if` de la portada) — no es una regresión de ese cambio, es un comportamiento de fondo del proyecto que nunca había sido ejercitado por un test. Sí funciona disparando el evento real: `campo.value = valor; campo.dispatchEvent(new Event('input'))` — el mismo patrón que ya usaban TODOS los tests de estos archivos que verifican contenido del DOM (ninguno usaba `setValue` + verificación de DOM antes). Causa raíz exacta no diagnosticada (candidatos: alguna interacción entre el nuevo control-flow `@if`/`@for` de Angular 22 y el mecanismo de `ReactiveFormsModule`/zone.js para propagar cambios fuera de un evento DOM) — no bloqueó la tarea, se documenta el síntoma y la mitigación confirmada. | Al escribir un test que verifica contenido del DOM condicionado por el valor de un `FormControl`, simular SIEMPRE un evento DOM real (`dispatchEvent(new Event('input'))` sobre el `<input>` con `formControlName`) en vez de `formulario.controls.X.setValue(...)` — el segundo es válido para verificar el VALOR del control en sí (`componente.formulario.value.X`), pero no es confiable para verificar que el TEMPLATE reaccionó a ese cambio. Si un test futuro necesita mutar un control sin un elemento DOM real detrás (ej. un control fuera de cualquier `<input>` visible), investigar más a fondo antes de asumir que `detectChanges()` basta. |
| **⚠️ (Root-caused y corregido 2026-08-19, CRÍTICO)** El rol IAM `IniciarValidacionLambdaRole` (`iniciarValidacionLibros`, Tarea "Validar libros") le daba a `babel-usuarios` únicamente `dynamodb:Scan`, pero el código resuelve el rol del administrador con `obtenerPorClave` (`GetItem`), no con un `Scan` — un desajuste de acción IAM vs. acción real invisible a `tsc` y a los tests unitarios, porque **todos** mockean `obtenerPorClave` por completo (`vi.mock('../services/dynamodb', ...)`) y nunca ejercitan el permiso real contra AWS. El resultado en `staging` (desplegado automáticamente por el PR abierto, `deploy.yml` corre en cada evento `pull_request`): la primera llamada a DynamoDB del handler (resolver el rol) fallaba con `AccessDeniedException`, capturado por el `catch` genérico del handler y devuelto como `500 {"error":"Error interno del servidor."}` — sin ninguna pista para el usuario, reportado como "el botón Iniciar validación no funciona". Detectado por el usuario probando la UI real, no por ningún test ni por `npx serverless print`/`serverless package` (ninguno de los dos valida que las acciones IAM declaradas coincidan con las que el código realmente ejecuta). | Al escribir el rol IAM de una función nueva, verificar acción por acción contra el código real qué operación de DynamoDB hace cada llamada (`obtenerPorClave`/`consultarPorIndice` → `GetItem`/`Query`; `escanearTodo`/`escanearProyeccion`/`escanearMayorQue` → `Scan`; `guardar` → `PutItem`; `eliminar` → `DeleteItem`) — nunca asumir que "leer una tabla" es siempre `Scan` solo porque otras tablas de la misma función sí lo son. Ningún test unitario que mockee la capa de DynamoDB puede detectar este tipo de bug (por diseño, evita depender de AWS real) — la única defensa real es la revisión manual línea por línea del rol contra las llamadas del handler antes de dar por buena una tarea con infraestructura nueva, más una prueba en vivo temprana del flujo completo (no solo `build`/`test`) contra `staging` apenas esté desplegado. |

| **⚠️ (Root-caused y corregido 2026-08-29, REINCIDENCIA del gotcha de `palabrasClaveInvalidas` de arriba, 2026-08-19)** Un campo tipado `string \| null` (no solo un campo NO opcional) también puede llegar `undefined` en vez de `null` cuando el `null` se omite deliberadamente al escribir. `Libro.isbn` se persiste con el atributo AUSENTE cuando es `null` (`omitirCamposNulos`, PR #89 — necesario porque el GSI disperso `isbn-index` tipa `isbn` estrictamente `S` y rechaza un `null`), así que cualquier lectura cruda de `babel-libros` (`Scan`/`GetItem`/`Query`) devuelve `isbn: undefined`, no `isbn: null`, aunque el tipo TypeScript prometa lo segundo. `GET /api/libros/inventario` reenviaba ese `undefined` tal cual al cliente; el guard `libro.isbn !== null` en `editar-libro.component.ts` lo dejaba pasar, y `undefined.includes(...)` reventaba el `computed` `librosFiltrados`, tumbando el render de TODA la lista apenas se escribía algo en el buscador de "Catalogar > Editar" — reportado por el usuario en `production`, sin ningún error en CloudWatch (el fallo nunca tocó el backend). Dos intentos previos de diagnóstico (PR #101 paginación de `Scan`, PR #102 timeout de Lambda) no lo resolvieron por atacar síntomas de una hipótesis distinta — la pista real estaba en el stack de DevTools, no en los logs del servidor. | Cuando un campo `T \| null` se persiste omitiendo el atributo en vez de guardar `null` explícito (patrón ya establecido por `omitirCamposNulos` para cualquier atributo indexado por un GSI disperso con tipo estricto), **todo** endpoint que devuelva ese registro crudo debe normalizar `campo ?? null` antes de responder — no basta con que el tipo TS lo declare `\| null`, ni con que el código de escritura sea correcto. Y en el cliente, cualquier filtro/`computed` sobre un campo así debe usar `campo ?? valorPorDefecto` en vez de comparar contra `null` explícitamente, como defensa adicional (mismo criterio que ya usaba `catalogo-publico.component.ts`, que por eso nunca tuvo este bug). Ante un error de UI sin ningún rastro en los logs del backend, revisar primero el stack de DevTools — puede indicar que el fallo nunca llegó al servidor. |

Los primeros tres hallazgos de esta tabla siguen siendo anticipados por analogía con Comandante (no verificados en código real de Babel); los marcados "(Verificado 2026-07-17)", "(Verificado/Confirmado 2026-07-18)" o "(Confirmado 2026-07-18)" ya se confirmaron en código/infraestructura real de Babel (o en el entorno de ejecución).

---

## 8. Documentos de referencia

| Documento | Contenido |
|---|---|
| `CLAUDE.md` | Descripción, stack, comandos, convenciones, seguridad OWASP, git flow, gotchas |
| `PRD.md` | Visión de producto, flujos, roadmap funcional, casos de uso, requisitos no funcionales |
| `tech-specs.md` | Arquitectura, endpoints, modelos de datos, infraestructura, entornos, secretos |
| `MEMORY.md` | Este documento — estado, ADRs, gotchas, contexto de sesión |
| `TODO.md` | Motor JIT — siempre exactamente 2 tareas atómicas activas |

---

## 9. Contexto de la sesión actual

**Qué se hizo el 2026-07-16:** bootstrap completo de documentación del proyecto Babel usando la skill `project-docs-bootstrap` (repo `ocastelblanco/ia-orchestration-skills`). Se inspeccionó el repositorio hermano `comandante-letiende` (stack, `CLAUDE.md`, `DESIGN.md`, `tech-specs.md`, workflow de GitHub Actions) para mantener consistencia de filosofía visual y de CI/CD. Se resolvieron con el usuario las ambigüedades de arquitectura: `api.letiende.co` como servicio externo ya existente (solo metadatos), app como PWA responsive (no nativa), y código/documentación en español.

**Qué se hizo el 2026-07-17:** el usuario precisó que existen dos descuentos distintos e independientes — descuento editorial (margen de consignación, 100% si el libro es propio de Le Tiende) y descuento de venta (discrecional del vendedor). Se ajustaron `PRD.md` (§5.2, §5.4, §5.6, §7, §10), `tech-specs.md` (§4.3 modelos de datos con snapshot de costo/utilidad en `Venta`, §5 endpoints, §5.1 decisión de diseño) y este documento (ADR-006) para reflejarlo con precisión.

Adicionalmente, el usuario pidió compartir el mismo proyecto Firebase Authentication de Comandante (misma cuenta de Google en ambas apps), manteniendo roles independientes por app en su propia base de datos. Se evaluaron los riesgos de seguridad (blast radius compartido, revocación en dos pasos, necesidad de cuenta de servicio propia por app) y se documentaron en `tech-specs.md` §8.1 (nueva), `CLAUDE.md` (OWASP A01/A07 y tabla de prohibiciones), `PRD.md` (§5.1, §9) y este documento (ADR-007). Todos los cambios se enviaron a la misma rama/PR (`claude/babel-project-bootstrap-k7bhww`, PR #1) para aprobación conjunta.

**Qué se hizo también el 2026-07-17 (continuación):** el usuario confirmó el `projectId` de Firebase a reutilizar: `comandante-letiende`. Se resolvió el pendiente de ADR-007 y se actualizaron `tech-specs.md` §6/§8.1 y este documento (§1, §5, ADR-007) con el valor exacto.

**Qué se hizo el 2026-07-17 (Tarea 1 de `TODO.md` — scaffold Angular):** se generó el proyecto Angular 22 SSR (standalone components) en la raíz del repo con `ng new`, configurado Tailwind CSS 4 vía `@tailwindcss/postcss` (`.postcssrc.json` en JSON puro — ver gotcha verificado en §7), creada la estructura de carpetas exacta de `tech-specs.md` §3 (`src/app/{core,features,shared}`, `src/theme/`), configurados los path aliases (`@core`, `@shared`, `@features`, `@theme`) en `tsconfig.json`, y Prettier con la config generada por Angular CLI. Se corrigió además `.gitignore` para dejar de ignorar `package-lock.json` (requerido por `CLAUDE.md` A08). Build (`npm run build`) y servidor de desarrollo (`npm run start`, HTTP 200) verificados de forma independiente antes de commitear. Cambios enviados en la rama `feature/scaffold-angular-ssr` (PR #2, fusionado a `main`), en dos commits separados: uno de documentación pendiente (README completo + symlinks de skills) y otro del scaffold en sí.

**Qué se hizo el 2026-07-17 (esqueleto de Serverless Framework + DynamoDB):** se creó `serverless.yml` con dos funciones Lambda separadas por rol IAM de mínimo privilegio (`api` con CRUD acotado a las 5 tablas, `ssr` sin ningún permiso de DynamoDB), las 5 tablas DynamoDB de `tech-specs.md` §5.1 con capacidad aprovisionada 25/25, el handler placeholder `GET /api/health`, el wrapper `server/ssr/handler.mjs` (`@codegenie/serverless-express`) sobre el build SSR de Angular, y `.github/workflows/deploy.yml`. El usuario autorizó explícitamente un **deploy real** a AWS stage `staging` (cuenta `696912647258`) — se ejecutó y se verificó de forma independiente (recursos reales confirmados por AWS CLI, ver §5). Cambios en la rama `feature/serverless-skeleton-dynamodb` (PR #3). El primer run de CI falló (`serverless package` sin autenticar); el usuario configuró los GitHub Secrets (`AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `SERVERLESS_LICENSE_KEY`) y se corrigió el workflow para usar `SERVERLESS_LICENSE_KEY` (no `SERVERLESS_ACCESS_KEY` — son dos rutas de auth distintas de Serverless Framework v4, confirmado con la documentación oficial, ver §5). CI en verde tras el fix; **PR #3 fusionado a `main`** y rama remota/local eliminadas.

**Qué se hizo el 2026-07-17 (autenticación con Google):** se implementó `AuthService` (Signals, SDK `firebase` puro sin `@angular/fire`) y `AuthGuard` (`CanActivateFn`). La config pública de Firebase (proyecto compartido `comandante-letiende`) se copió del repo hermano Comandante (`/Users/ocastelblanco/Documents/LeTiende/letiende.co/comandante/src/environments/environment.ts`), confirmando que es la misma para ambos ambientes de Babel por ahora (no hay proyecto Firebase de producción distinto todavía). Firebase se inicializa solo en el navegador (`isPlatformBrowser`) para no romper el SSR. Cubierto con 6 tests unitarios (Vitest, mocks del SDK) — sin verificación manual de UI porque el alcance excluye explícitamente `LoginComponent`. Cambios en la rama `feature/auth-google-firebase` (PR #5, fusionado a `main`).

**Qué se hizo el 2026-07-17 (`LoginComponent` + prueba real en AWS):** se implementó `NoAuthGuard`, `LoginComponent` (Tailwind, paleta de marca) y las rutas `/login`/`/libros` (esta última con un placeholder `ListaLibrosCatalogadosComponent`, nombre real del roadmap). Se agregó un redirect de `""`/`"**"` hacia `/libros` porque un login exitoso navegaba a `/` sin ningún componente asociado. El usuario pidió expresamente probar el mínimo posible en AWS real para validar la infraestructura/CI-CD sin esperar la tarea de modelos/DynamoDB (confirmado que son independientes) y sin disparar el job de `production` vía GitHub Actions — se optó por un **redeploy manual a `staging`** (`npm run deploy:staging`) en vez de fusionar a `main`. Al probar contra el deploy real se encontró el bug crítico de `RenderMode.Prerender` (ver §7) que dejaba `AuthGuard` sin efecto en producción; corregido a `RenderMode.Server` y re-verificado con un segundo redeploy real: `/libros` sin sesión → `302 → /login` confirmado con `curl` contra `https://oyzau0c910.execute-api.us-east-1.amazonaws.com`. Cambios en la rama `feature/login-component` (PR #6, fusionado a `main`).

**Qué se hizo el 2026-07-17 (identidad visual de Comandante en `LoginComponent`):** el usuario pidió que Babel siga la misma filosofía UX/UI de Comandante. Se inspeccionó `comandante/` (repo hermano): usa Ionic + Tailwind, paleta de 4 colores de `DESIGN.md`, Poppins vía Google Fonts, y un logo wordmark SVG ("le tiende", con la tipografía Angellya ya vectorizada como paths, no como fuente utilizable). Se copiaron `favicon.ico`, `logo_negro_sin_fondo.svg` y `logo_blanco_sin_fondo.svg` a `public/`, se agregaron tokens de color/tipografía a `styles.css` y se rediseñó `LoginComponent` (Tailwind puro, sin Ionic) replicando la estructura visual de Comandante (tarjeta centrada, logo, botón con ícono). Se corrigió un problema real de contraste: el `button-primary` de `DESIGN.md` usa texto `on-secondary` (`#230C00`, oscuro) sobre fondo `secondary` (`#E8630A`) — el primer borrador de Babel usaba texto blanco, que el propio `DESIGN.md` de Comandante marca como reprobado en WCAG AA (~2.6:1). Verificado con una captura real (Playwright + Chrome headless, instalado en este entorno para la prueba) del dev server y con un redeploy manual a `staging`. Cambios en la misma rama `feature/login-component` (PR #6, fusionado a `main`).

**Pendiente de assets del usuario (ver §7):** (1) archivo real de la fuente **Angellya** (`.woff2`/`.otf`/`.ttf`) — no existe en ningún repo de Le Tiende, ni siquiera en Comandante, que documenta su uso en `DESIGN.md` pero nunca la implementó; (2) versión de alta resolución (SVG o PNG ≥512px) del monograma cuadrado "tt" — hoy solo existe embebido en `favicon.ico` a 16×16/32×32, insuficiente para generar el set completo de íconos PWA (192/512/maskable/apple-touch-icon) sin verse borroso.

**Qué se hizo el 2026-07-17 (íconos PWA + `manifest.webmanifest`):** el usuario ubicó `mono_naranja.svg` y reveló un directorio de assets fuente compartido de Le Tiende (`~/Documents/LeTiende/letiende.co/fuentes/` e `~/Documents/LeTiende/ImagenVisual/`) con un set de íconos PWA ya generado profesionalmente (`favicon_io/`) y la fuente Angellya real (`.otf`/`.ttf`). Se usó el set ya generado en vez de regenerar desde el SVG (mejor calidad, confirmado con el usuario) — copiados `favicon-{16,32}.png`, `apple-touch-icon.png` (180), `icon-{192,512}.png`, `favicon.ico` a `public/`. Se creó `public/manifest.webmanifest` (nombre, colores de marca, `purpose: "any"` únicamente — el ícono de 512px no tiene margen de seguridad suficiente para `maskable`, ver §7) y se enlazó todo en `index.html` (`theme-color`, favicons, `apple-touch-icon`, `manifest`). Verificado sirviendo localmente: manifest válido, todos los assets responden 200. Angellya queda pendiente de integrar como `@font-face` — no se hizo en esta misma tarea (alcance separado).

**Qué se hizo el 2026-07-17 (fix de `signInWithPopup` en `staging` — bug #1 post-merge del PR #6):** el usuario reportó que el popup de Google se abría y cerraba de inmediato, mostrando el mensaje genérico de error. Se confirmó la causa raíz consultando la config real de Identity Platform (`identitytoolkit.googleapis.com/v2/projects/comandante-letiende/config`, usando un access token obtenido a partir del refresh token ya almacenado por `firebase-tools` en esta máquina — sin nueva autorización interactiva): `oyzau0c910.execute-api.us-east-1.amazonaws.com` no estaba en `authorizedDomains`. Con permiso explícito del usuario, se hizo `PATCH` a esa misma API agregando el dominio a la lista existente (14 → 15, sin quitar ninguno de Comandante — ver §5 y §7). No fue necesario ningún cambio de código; el fix es de configuración del proyecto Firebase compartido, no del repo. **Confirmado por el usuario: el login ya funciona en `staging`.** Reportó además el warning benigno de COOP (ver §7, sin acción tomada por decisión del usuario).

**Segundo problema reportado por el usuario — RESUELTO:** el segundo bug era que `LoginComponent` se veía muy distinto al de Comandante, porque Comandante usa Ionic y Babel no. Se investigó, se descartó instalar Ionic (Comandante no usa SSR en absoluto y Ionic + Angular standalone + SSR tiene fricción conocida — riesgo real de romper el SSR ya desplegado en Babel) y en su lugar se replicó el resultado visual exacto con Tailwind puro, midiendo por píxel la app real (`https://comandante.letiende.co`) — ver "identidad visual de Comandante" arriba y el fix de colores reales del botón (PR #9, fusionado a `main`). Confirmado por el usuario como resuelto en todos lados.

**Qué se hizo el 2026-07-17 (CI: deploy automático a `staging` en cada PR):** para que el usuario pueda continuar el desarrollo desde Claude Code en Android sin depender de esta Mac ni de credenciales de AWS locales, se agregó el job `desplegar-staging` en `.github/workflows/deploy.yml` (dispara en todo `pull_request`, usa los secrets ya existentes, comenta la URL de `staging` en el PR). Verificado real en los PR #8, #9 y #10 — funciona de punta a punta. PR #8 fusionado a `main`.

**Qué se hizo el 2026-07-17 (íconos PWA + `manifest.webmanifest`, PR #10 fusionado):** ver detalle arriba — usó el set ya generado en `favicon_io/` en vez de regenerar desde el SVG. Verificado con auto-deploy real a `staging`.

**Qué se hizo el 2026-07-18 (Tarea 1 de `TODO.md` — modelos de datos + cliente DynamoDB):** se crearon las 5 interfaces de `src/app/core/models/` (`Libro`, `Venta`, `Estante`, `DescuentoEditorial`, `Usuario`, exactamente como `tech-specs.md` §4.3), se instalaron `@aws-sdk/client-dynamodb`/`@aws-sdk/lib-dynamodb` (con `--ignore-scripts`, ver gotcha en §7) y se implementó `server/api/services/dynamodb.ts` con un `DynamoDBDocumentClient` único y 4 funciones genéricas (`obtenerPorClave`, `guardar`, `eliminar`, `consultarPorIndice`) parametrizadas por nombre de tabla, sin lógica de negocio. `npm run build`, `npm run build:api` y `npm test` (8 tests) pasan limpios en este entorno (tras instalar Node 22.22.3 vía `nvm`, ver §7). **No se pudo completar** el último ítem del Definition of Done: la verificación en vivo contra una tabla real de `staging` — un script puntual (`guardar`/`obtenerPorClave`/`eliminar` de prueba contra `babel-estantes-staging`, no commiteado) falló con `UnrecognizedClientException` porque las credenciales AWS de este entorno no son válidas contra la cuenta real (ver §7). No quedó ningún dato de prueba escrito (el fallo ocurrió en el primer `guardar()`). Se documentó el riesgo abierto en `TODO.md` y se decidió cerrarlo naturalmente con la Tarea 2 (primer endpoint real que invoca este servicio), en vez de forzar una verificación artificial fuera de alcance. Cambios en la rama `feature/modelos-datos-dynamodb-client`, PR #12.

**Qué se hizo el 2026-07-18 (independencia de plataforma — verificación agnóstica en CI):** tras fusionar el PR #12, el usuario preguntó qué faltaba para poder desarrollar desde cualquier plataforma de Claude Code (CLI, Web, Mobile, Desktop) sin depender de esta Mac. Diagnóstico y acciones:
1. **Secrets de negocio en el deploy de `staging`:** el job `desplegar-staging` de `deploy.yml` solo pasaba las credenciales AWS/Serverless; ahora inyecta también `FIREBASE_SERVICE_ACCOUNT_BABEL`, `GOOGLE_CUSTOM_SEARCH_*` y `API_LETIENDE_BASE_URL` (con `''` como default en `serverless.yml`, su ausencia no rompe nada). El secret `FIREBASE_SERVICE_ACCOUNT_BABEL` en sí queda pendiente de que el usuario cree la cuenta de servicio (instrucciones entregadas — ver §5); es bloqueante para la Tarea 1 del `TODO.md`.
2. **Smoke test post-deploy** en el mismo job: `GET /api/health` contra staging debe responder `200` o el check del PR falla (ver §5).
3. **Workflow manual `operaciones-staging.yml`** para sembrar/consultar/eliminar usuarios de prueba en `babel-usuarios-staging` desde GitHub Actions, sin credenciales locales (ver §5) — cubre el Definition of Done de la Tarea 1.
4. **Decisión del usuario sobre Angellya:** la fuente NO se usará en el desarrollo (es solo del logo, y los logos ya existen); se cierra el pendiente de `@font-face` (ver §7 y §2). Con esto desaparece la última dependencia de assets locales de la Mac para el desarrollo previsto.
5. Se corrigió además el §1 de este documento, que había quedado desactualizado tras el merge del PR #12 (decía "pendiente de push/PR").

**Qué se hizo el 2026-07-18 (verificación real del ID Token de Firebase + `GET /api/usuarios/me`):** implementados `server/api/lib/verificar-token.ts` (valida el header `Authorization` con `firebase-admin` contra `comandante-letiende`, cuenta de servicio propia de Babel) y `server/api/handlers/usuarios-me.ts` (`401`/`403`/`200` según corresponda). Nueva función Lambda `usuariosMe` en `serverless.yml` con rol IAM propio de solo lectura (ADR-008). Se corrigió también un bug real en `dynamodb.ts` (cota genérica `Record<string, unknown>` → `object`, las interfaces de dominio no tenían índice de firma).

Build/tests locales en verde, y el caso `401` se probó localmente invocando el handler compilado sin red (Tarea 1 previa, PR #14). Al desplegar a `staging` el endpoint respondía `500` genérico — se agregó la operación de diagnóstico `ver-logs-usuarios-me` a `operaciones-staging.yml` (nueva, lee CloudWatch Logs vía CI) y se encontró la causa raíz real: `jose@6` (ESM puro) resuelto como dependencia transitiva de `firebase-admin`/`jwks-rsa`, incompatible con el `require()` de CommonJS de la Lambda compilada — corregido fijando `jose` a `^5.9.6` vía `overrides` en `package.json` (ver gotcha crítico en §7). Confirmado el `401` en vivo con `curl` real tras el fix.

Para los casos `403`/`200` (que exigen un ID Token real, no solo datos de DynamoDB) se preguntó al usuario cómo proceder, dado que automatizarlo implica crear/borrar un usuario desechable en el proyecto Firebase **compartido** con Comandante — el usuario aprobó automatizarlo. Se agregó la operación `probar-usuarios-me` a `operaciones-staging.yml` (emite un custom token con `firebase-admin`, lo canjea por un ID Token real vía la REST API de Identity Toolkit, prueba el endpoint, y siempre borra el usuario de Firebase Auth al final). El primer intento falló con `auth/insufficient-permission`: la cuenta de servicio no tenía el rol de Google Cloud IAM necesario para administrar usuarios de Firebase Authentication. Se le dieron al usuario (desde su celular) las instrucciones exactas para otorgar el rol `Firebase Authentication Admin` (`roles/firebaseauth.admin`) a esa cuenta de servicio en la consola de IAM. Tras confirmarlo, se re-disparó la verificación: **`403` confirmado** (correo sin fila en `babel-usuarios-staging`) y, tras sembrar un registro de prueba con `sembrar-usuario-prueba`, **`200` confirmado con el rol `vendedor` correcto** en la respuesta — el registro de DynamoDB y el usuario de Firebase Auth de prueba quedaron limpios al terminar. Cambios en la rama `feature/verificacion-id-token-backend`, PR pendiente de crear/fusionar.

**Qué se hizo el 2026-07-19 (endpoint público `GET /api/libros`):** implementada `escanearMayorQue` en `server/api/services/dynamodb.ts` (nueva función genérica de `Scan` + `FilterExpression`, parametrizada por tabla/atributo) y `server/api/handlers/libros.ts` (`GET /api/libros`, sin autenticación, filtra `cantidadDisponible > 0`). Nueva función Lambda `libros` en `serverless.yml` con rol IAM propio de solo lectura (`dynamodb:Scan` únicamente sobre `babel-libros`, siguiendo ADR-008); `package.patterns` con `node_modules/**` completo a propósito (misma precaución que `usuariosMe` tras el incidente de `jose`/`jwks-rsa`, en vez de un allowlist manual de las dependencias de `@aws-sdk/*`).

Build/tests locales en verde. Verificado en vivo contra `staging` con `curl` real: `GET /api/libros` → `200` con `[]` (tabla vacía, sin datos catalogados todavía), sin ningún header de autenticación. Se confirmó además que no hubo regresión en `/api/health` (`200`) ni en `/api/usuarios/me` sin token (`401`) — el ruteo de API Gateway prioriza correctamente la ruta específica `/api/libros` sobre el catch-all `/api/{proxy+}` de la función `api`. Cambios en la rama `feature/endpoint-libros-publico`, PR pendiente de fusión.

**Qué se hizo el 2026-07-19 (`RoleGuard` en el frontend):** implementados `UsuariosService` (`src/app/core/api/usuarios.service.ts`, cliente de `GET /api/usuarios/me` con `Authorization: Bearer <idToken>`, expone el `Usuario` resuelto como Signal de solo lectura, nunca lanza — resuelve `null` ante sin sesión/401/403/error de red) y `RoleGuard` (`src/app/core/auth/role.guard.ts`, `CanActivateFn` parametrizado por rol, redirige a `/libros` si no coincide). Se agregó `obtenerIdToken()` a `AuthService` y `provideHttpClient(withFetch())` a `app.config.ts` (Fetch API, compatible con SSR). No se agregó ninguna ruta real de administración todavía, a propósito (ver `TODO.md`).

Se encontraron y corrigieron tres bugs reales durante el desarrollo de los tests unitarios (`usuarios.service.spec.ts`, `role.guard.spec.ts`):
1. Un helper `configurarPrueba()` configuraba el mock de `obtenerIdToken` en un orden que dejaba `obtenerIdTokenMock`/`httpMock` en `undefined` al momento de usarlos — corregido pasando el valor resuelto como parámetro del helper (mismo patrón que ya usaba `role.guard.spec.ts`).
2. Una condición de carrera de microtask: el test llamaba `httpMock.expectOne(...)` sincrónicamente justo después de invocar `obtenerUsuarioActual()`, sin darle tiempo al `await` interno sobre el mock de `obtenerIdToken()` (una promesa ya resuelta, pero que igual necesita un tick de microtask) a completarse antes de que la petición HTTP quedara registrada — corregido con un `await Promise.resolve();` entre ambas líneas.
3. El gotcha de Vitest/Firebase documentado en §7 (`vi.mock` no aislado entre archivos) — corregido mockeando `firebase/app`/`firebase/auth` también en `usuarios.service.spec.ts` y `role.guard.spec.ts`.

`npm run build` y `npm test -- --watch=false` (14 tests) en verde, confirmado estable en 3 corridas consecutivas. Para el ítem del DoD sobre verificación en vivo contra `staging`, se le preguntó al usuario cómo proceder dado que `RoleGuard` no está cableado a ninguna ruta real (instrucción explícita de la tarea) y un login real de Google no se puede automatizar en este entorno sandbox — el usuario eligió combinar las evidencias ya existentes: el 401/403/200 de `GET /api/usuarios/me` ya verificado en vivo en la tarea anterior, más los 3 tests unitarios del guard (que usan exactamente esas formas de `Usuario`), en vez de desplegar una ruta de prueba desechable. Cambios en la rama `feature/role-guard-frontend`, PR pendiente de crear.

**Qué se hizo el 2026-07-19 (`CatalogoPublicoComponent`, SSR):** implementados `LibrosService` (`src/app/core/api/libros.service.ts`, cliente de `GET /api/libros` sin autenticación, expone `libros`/`cargando`/`error` como Signals, nunca lanza), `CatalogoPublicoComponent` (grid de libros con Tailwind y paleta de marca, estados de carga/error/vacío) y `PvpPipe` (`src/app/shared/pipes/pvp.pipe.ts`, formatea `45000` → `$45.000` con `Intl.NumberFormat('es-CO')` en vez del `CurrencyPipe`/`DecimalPipe` de Angular, para no depender de registrar los datos del locale `es-CO`). La ruta `/` ahora sirve este componente sin ningún guard, reemplazando el redirect placeholder a `/libros`; el fallback `**` también redirige a `/` (antes a `/libros`, ver `tech-specs.md` §4.2).

Al probar el componente contra el dev server real (`ng serve`, SSR por petición) se encontró un bug real: `LibrosService.cargarCatalogo()` se llama en `ngOnInit`, y en SSR eso dispara una petición `HttpClient` con URL relativa (`/api/libros`) — el `fetch` de Node no tiene noción de "origen actual" como el navegador, y la petición nunca resuelve (el log mostraba `Application did not stabilize` / `PendingTasks keeping application unstable` sin ningún error explícito, silencioso). Corregido con `absoluteUrlInterceptor` (`src/app/core/api/absolute-url.interceptor.ts`): un `HttpInterceptorFn` que, solo cuando el token `REQUEST` de Angular (disponible durante SSR real vía `AngularNodeAppEngine`, no en el navegador) existe, antepone su origen a cualquier URL que empiece con `/`. Cubierto con 2 tests unitarios (con/sin `REQUEST`). La reproducción end-to-end completa en este entorno sandbox no fue posible (sin un backend `/api/libros` real corriendo localmente, la petición se auto-referencia contra el propio dev server y no es representativa) — la verificación real queda para el deploy a `staging` (ver DoD de la tarea).

De paso reapareció, por tercera vez, el flake de Vitest documentado en la tarea de `RoleGuard` (`MEMORY.md` §7) — esta vez rompiendo `auth.guard.spec.ts`/`no-auth.guard.spec.ts` a pesar de que ya tenían el mock de Firebase aplicado, y de que los archivos nuevos de esta tarea ni siquiera importan `AuthService`. Se investigó el schema del builder `@angular/build:unit-test` y se encontró la causa raíz real: `isolate` es `false` por defecto (todos los archivos de prueba comparten un mismo hilo/proceso y su registro de módulos, "para alinearse con Karma/Jasmine"). Corregido activando `"isolate": true` en `angular.json` (`architect.test.options`) — fix de raíz, no un parche por archivo; confirmado estable en 6 corridas consecutivas de `npm test -- --watch=false` (24 tests). Ver detalle completo en §7.

`npm run build` y `npm test -- --watch=false` en verde. Cambios en la rama `feature/catalogo-publico-ssr` (PR #17, fusionado — verificado en vivo con `curl` real: `GET /` → `200` con SSR real mostrando "Catálogo" y el estado vacío, sin regresión en `/api/health`/`/api/libros`/`/api/usuarios/me`).

**Qué se hizo el 2026-07-19 (`POST /api/libros`, backend protegido):** implementado `handlerCrear` en `server/api/handlers/libros.ts` — verifica el ID Token, exige rol `vendedor` **o** `administrador` en `babel-usuarios` (a diferencia de `RoleGuard`, que exige un rol exacto), valida el body con `validarDatosNuevoLibro` (función pura exportada: `pvp` positivo con techo de sanidad de $5.000.000, `porcentajeDescuentoEditorial` 0-100, `cantidadTotal` entera positiva, campos de texto requeridos) y genera `bookId`/`creadoPor`/`creadoEn`/`actualizadoEn`/`costo`/`utilidadCatalogo` siempre en el backend — nunca confía en nada de eso desde el cliente (`CLAUDE.md` A01/A08). Nueva función Lambda `catalogarLibro` (ADR-008) con rol IAM propio (`PutItem` sobre `babel-libros`, `GetItem` sobre `babel-usuarios`).

**Nuevo en esta tarea: tests unitarios reales del backend.** Hasta ahora los endpoints de backend (`usuariosMe`, `libros` GET) se habían verificado solo manualmente/en vivo, sin automatización — `ng test` únicamente mira `src/**`. Se agregó `vitest.config.ts` (raíz, `include: ['server/**/*.spec.ts']`) y el script `npm run test:api`, y se cubrió `handlerCrear` con 11 tests (`server/api/handlers/libros.spec.ts`): validación pura del body (6 casos) + el handler completo mockeando `verificar-token`/`dynamodb` (401/403/400/201×2 — vendedor y administrador). A diferencia del builder de Angular, `vi.mock` de rutas relativas SÍ funciona con Vitest plano — no aplica la restricción de `@angular/build:unit-test`. Se excluyó `*.spec.ts` de `server/tsconfig.json` (si no, `npm run build:api` los compilaba también, sin los tipos de Vitest). Se agregó `npm run test:api` al job `build-y-test` de CI.

**Verificación en vivo — encontrado y corregido un bug real de infraestructura de pruebas.** Se agregó la operación `probar-catalogar-libro` a `operaciones-staging.yml` (mismo mecanismo que `probar-usuarios-me`: ID Token real sin login interactivo) para probar `POST /api/libros` end-to-end. El primer intento confirmó el `201` correctamente pero el script se cayó al limpiar el libro de prueba: `Error: Region is missing` — el SDK de JS v3 (`@aws-sdk/client-dynamodb`) no cae de vuelta a `AWS_DEFAULT_REGION` (convención del AWS CLI, ya usada por el resto del workflow), solo lee `AWS_REGION` (ver gotcha nuevo en §7). Corregido agregando `AWS_REGION` al `env:` del workflow y una región explícita como respaldo en el script; se agregó también `eliminar-libro-prueba` (nueva operación) como red de seguridad manual. Se limpió el libro huérfano que quedó del primer intento fallido y se re-verificó el flujo completo: `401` (sin token, `curl` directo), `403` (correo sin fila en `babel-usuarios-staging`) y `201` (usuario `vendedor` sembrado, libro creado con los cálculos correctos y limpiado automáticamente al terminar) — los tres confirmados en vivo contra `staging`, con todos los datos de prueba (usuario de Firebase Auth, libro, fila de `babel-usuarios-staging`) limpios al cerrar. Cambios en la rama `feature/endpoint-catalogar-libro`, PR #18 pendiente de fusión.

**Regresión post-verificación — encontrada y corregida antes de cerrar la tarea.** Después de la verificación en vivo de arriba, una comprobación de rutina de los endpoints ya existentes (no solo el nuevo) reveló que `GET /api/libros` respondía `500` (antes `200` con `[]`). Causa raíz vía CloudWatch Logs (nueva operación `ver-logs-libros`): `Runtime.ImportModuleError: Cannot find module '../lib/verificar-token'` — el `package.patterns` de la función `libros` (GET) nunca incluyó ese archivo, aunque el módulo compartido `libros.js` ahora lo importa a nivel superior para `handlerCrear` (ver gotcha crítico nuevo en §7). Corregido agregándolo al `package.patterns` de `libros` en `serverless.yml` (commit `0c5d771`) y reverificado en vivo: `GET /api/libros` → `200 []`, `GET /` → `200` (SSR real), `GET /api/health` → `200`, `POST /api/libros`/`GET /api/usuarios/me` sin token → `401` — todo verde de nuevo.

**Qué se hizo el 2026-07-19 (datos de demostración para el catálogo público):** a pedido del usuario, se sembraron 3 libros reales y presentables en `babel-libros-staging` (*Cien años de soledad*, *Rayuela*, *El Aleph*) para poder probar `CatalogoPublicoComponent` desde el navegador con datos que se vieran bien, en vez de la lista vacía `[]`. Nueva operación `sembrar-libros-demo` en `operaciones-staging.yml` + `server/scripts/sembrar-libros-demo.mjs`: siembra directo por DynamoDB (sin pasar por `POST /api/libros`/Firebase, ya que no se está probando el endpoint sino generando datos de exhibición), con `bookId` fijos (`demo-1`/`demo-2`/`demo-3`) para que volver a correrla actualice los mismos ítems en vez de duplicar. Confirmado en vivo: `GET /api/libros` devuelve los 3 libros con PVP/costo/utilidad calculados correctamente.

**Qué se hizo el 2026-07-19 (CRUD `/api/estantes`, administrador):** implementado `server/api/handlers/estantes.ts` — un solo handler para los 4 verbos (`GET`/`POST`/`PUT`/`DELETE`, distinguidos por `event.requestContext.http.method`), exige rol `administrador` exclusivamente (a diferencia de `POST /api/libros`, aquí `vendedor` NO basta — función `exigirAdministrador` dedicada). `PUT`/`DELETE` verifican primero que el `estanteId` del path exista (`obtenerPorClave`) antes de actuar, respondiendo `404` si no. Se agregó `escanearTodo<T>` a `dynamodb.ts` (Scan sin filtro, para el listado). Nueva función Lambda `estantes` (ADR-008) con rol IAM propio: CRUD completo sobre `babel-estantes` + solo lectura sobre `babel-usuarios`. **Aplicada de entrada la lección del incidente de `POST /api/libros`** (ver §7): `verificar-token.js` se incluyó en el `package.patterns` desde el primer commit, no como parche posterior. 13 tests unitarios nuevos (`estantes.spec.ts`).

Se agregaron las operaciones `probar-estantes` (mismo mecanismo de ID Token real que `probar-catalogar-libro`: `GET /api/estantes` primero — si no es `200`, el script se detiene ahí, cubriendo 401/403 sin efectos secundarios; si es `200`, corre el ciclo `POST`→`PUT`→`GET`→`DELETE` y limpia su propio estante de prueba) y `ver-logs-estantes` a `operaciones-staging.yml`. Verificado en vivo contra `staging` de punta a punta: `401` sin token (`curl` directo), `403` con un usuario `vendedor` sembrado (el script se detuvo solo, como se esperaba), y con ese mismo usuario promovido a `administrador`: `GET`→`200 []`, `POST`→`201`, `PUT`→`200` (actualiza `ubicacion`), `GET` posterior→`200` (el estante actualizado aparece), `DELETE`→`204` — el estante y el usuario de Firebase Auth de prueba quedaron limpios automáticamente; la fila de `babel-usuarios-staging` se limpió aparte con `eliminar-usuario-prueba`. Sin regresión en los demás endpoints (`GET /api/libros`, `GET /`, `GET /api/health`, `GET /api/usuarios/me`).

**Nota sobre el cierre de esta tarea:** el usuario fusionó el PR #19 (rama `feature/endpoint-catalogar-libro`... perdón, `feature/crud-estantes`) mientras la verificación en vivo del ciclo completo con `administrador` todavía estaba en curso — se terminó de verificar igual (con la rama ya fusionada, staging seguía sirviendo el mismo código desplegado) y se limpiaron los datos de prueba con normalidad. Como la rama ya no admite más pushes, esta actualización de `TODO.md`/`MEMORY.md` se hizo en una rama nueva y independiente (`docs/cierra-tarea-estantes`), solo documentación, sin cambios de código — precedente a seguir si se repite la situación (el usuario puede fusionar en cualquier momento; si ya no queda rama abierta para los docs, se abre una rama/PR solo de docs en vez de intentar reabrir o forzar la rama fusionada).

**Qué se hizo el 2026-07-19 (`POST /api/ventas`, backend protegido):** implementado `server/api/handlers/ventas.ts` — verifica el ID Token, exige rol `vendedor` **o** `administrador` (mismo patrón que `POST /api/libros`), recibe únicamente `{ bookId, formaDePago, porcentajeDescuentoVenta }` del cliente y lee el `Libro` real de `babel-libros` (`obtenerPorClave`) para `pvp`/`costo` — nunca confía en esos valores desde el body (`CLAUDE.md` A08). Antes de guardar la venta, decrementa `Libro.cantidadDisponible` de forma **condicional** con la nueva función `decrementarSiPositivo` (`server/api/services/dynamodb.ts`, `UpdateCommand` + `ConditionExpression: '#atributo > :cero'`, captura `ConditionalCheckFailedException` y devuelve `false` en vez de lanzar) para evitar sobrevender si dos ventas del mismo libro llegan casi al mismo tiempo — `400` si ya no queda disponibilidad, `404` si el `bookId` no existe (en ninguno de los dos casos se llega a crear la venta). Calcula y persiste el snapshot completo (`pvp`, `costoLibro`, `precioFinal = pvp * (1 - porcentajeDescuentoVenta / 100)`, `utilidad`, ADR-006/ADR-003) — `ventaId` (UUID), `vendidoPor` (email del token, nunca del body) y `vendidoEn` siempre generados en el backend. `validarDatosNuevaVenta` exportada como función pura (mismo patrón que `validarDatosNuevoLibro`) para validar `formaDePago` (uno de los 5 valores de `FormaDePago`) y `porcentajeDescuentoVenta` (0-100) antes de procesar. Nueva función Lambda `registrarVenta` (ADR-008) con rol IAM propio: `PutItem` sobre `babel-ventas`, `GetItem`+`UpdateItem` sobre `babel-libros`, `GetItem` sobre `babel-usuarios`. **Aplicada de entrada la lección del incidente de `POST /api/libros`** (ver §7): `verificar-token.js` incluido en `package.patterns` desde el primer commit. 11 tests unitarios nuevos (`server/api/handlers/ventas.spec.ts`; el mensaje del commit y el cuerpo del PR #21 dicen por error "18 tests" — el conteo real confirmado por `npm run test:api` es 11: 4 de `validarDatosNuevaVenta` + 7 del handler completo, incluida la nota aquí para no repetir la imprecisión).

Se agregaron las operaciones `probar-registrar-venta` (mismo mecanismo de ID Token real que `probar-estantes`: cataloga un libro de prueba real con `POST /api/libros` primero — si no es `201`, el script se detiene ahí, cubriendo el caso `403` sin efectos secundarios porque ni siquiera llega a intentar la venta; si es `201`, vende ese mismo libro con `POST /api/ventas`, verifica que `cantidadDisponible` bajó, y limpia el libro y la venta de prueba al terminar) y `ver-logs-registrar-venta` a `operaciones-staging.yml`. Verificado en vivo contra `staging` de punta a punta: regresión descartada primero (`GET /api/libros`/`GET /`/`GET /api/health` → `200`, `GET /api/estantes` sin token → `401`); `POST /api/ventas` sin token (`curl` directo) → `401`; con un correo sin fila en `babel-usuarios-staging` → `POST /api/libros` (paso previo del script) respondió `403` y el script se detuvo ahí, sin datos de prueba creados; con ese mismo correo sembrado como `vendedor` (`sembrar-usuario-prueba`) → `POST /api/libros` → `201`, `POST /api/ventas` → `201` con el snapshot exacto esperado (`pvp` 50000, `porcentajeDescuentoVenta` 10, `precioFinal` 45000, `costoLibro` 32500, `utilidad` 12500), y el libro dejó de aparecer en `GET /api/libros` tras la venta (confirmando `cantidadDisponible` en 0, ya que `cantidadTotal` era 1) — el libro y la venta de prueba se limpiaron solos, y la fila de `babel-usuarios-staging` se limpió aparte con `eliminar-usuario-prueba`. Cambios en la rama `feature/endpoint-registrar-venta`, PR #21 (abierto al momento de esta actualización de documentación, enviada a la misma rama).

**Qué se hizo el 2026-07-19 (CRUD `/api/usuarios`, administrador):** implementado `server/api/handlers/usuarios.ts` — un solo handler para los 4 verbos (`GET`/`POST`/`PUT`/`DELETE`, distinguidos por `event.requestContext.http.method`, mismo patrón que `estantes.ts`), exige rol `administrador` exclusivamente. `validarDatosNuevoUsuario`/`validarActualizacionUsuario` exportadas como funciones puras (mismo patrón que el resto de handlers) — email validado con un regex simple, `nombre` no vacío, `rol` uno de `'vendedor' | 'administrador'`. Dos decisiones de diseño explícitas del punto 2 y 3 de `TODO.md`, documentadas como **ADR-009** (ver §3): `POST` sobre un email existente responde `409` en vez de sobrescribir; un administrador nunca puede degradarse ni eliminarse a sí mismo (`400` explicativo en `PUT`/`DELETE` sobre su propio email — comparando el email del path contra el email resuelto del token verificado). Nueva función Lambda `usuarios` (ADR-008) con rol IAM propio: `GetItem`/`PutItem`/`DeleteItem`/`Scan` únicamente sobre `babel-usuarios` (no hace falta acceso a ninguna otra tabla, ya es la misma que usa para resolver el rol del token). **Aplicada de entrada la lección del incidente de `POST /api/libros`** (ver §7): `verificar-token.js` incluido en `package.patterns` desde el primer commit. 22 tests unitarios nuevos (`server/api/handlers/usuarios.spec.ts`: 5 de `validarDatosNuevoUsuario` + 3 de `validarActualizacionUsuario` + 14 del handler completo, incluidos los dos casos de la salvaguarda de auto-degradación/auto-eliminación).

Se agregó la operación `probar-usuarios-crud` a `operaciones-staging.yml` (mismo mecanismo de ID Token real que `probar-estantes`: `GET /api/usuarios` primero — si no es `200`, el script se detiene ahí, cubriendo `401`/`403` sin efectos secundarios; si es `200`, primero confirma la salvaguarda de ADR-009 intentando `PUT`/`DELETE` sobre el propio email del administrador de prueba —ambos deben fallar con `400`— y luego corre el ciclo `POST`→`PUT`→`GET`→`DELETE` sobre un usuario objetivo de prueba fijo y distinto (`objetivo-prueba-usuarios-crud@letiende.co`), con una limpieza defensiva previa por si un run anterior dejó ese objetivo huérfano). Verificado en vivo contra `staging` de punta a punta: regresión descartada primero (`GET /api/libros`/`GET /`/`GET /api/health` → `200`, `GET /api/estantes` sin token → `401`); `GET /api/usuarios` sin token → `401`; con un correo sin fila en `babel-usuarios-staging` → `403` y el script se detuvo ahí sin datos de prueba creados; con ese mismo correo sembrado como `administrador` (`sembrar-usuario-prueba`) → `GET /api/usuarios` → `200`, `PUT`/`DELETE` sobre el propio email → `400` en ambos casos (mensajes exactos: "No puedes degradar tu propio rol de administrador..." / "No puedes eliminarte a ti mismo..."), `POST` del usuario objetivo → `201`, `PUT` → `200`, `GET` posterior confirmó que aparece (`true`), `DELETE` → `204` con limpieza automática — la fila de `babel-usuarios-staging` del administrador de prueba se limpió aparte con `eliminar-usuario-prueba`. Cambios en la rama `feature/crud-usuarios`, PR #22 (abierto al momento de esta actualización de documentación, enviada a la misma rama).

**Qué se hizo el 2026-07-19 (catalogación manual en el frontend):** implementados `EstantesService` (`src/app/core/api/estantes.service.ts`, cliente autenticado de `GET /api/estantes`, mismo patrón que `UsuariosService`) y `CatalogarLibroComponent` (`src/app/features/catalogar/`, standalone, ruta nueva `/catalogar`): formulario reactivo con todos los campos de `POST /api/libros` (`isbn`, `titulo`, `autor`, `editorial`, `portadaUrl`, `pvp`, `porcentajeDescuentoEditorial`, `cantidadTotal`, `estanteId` como `<select>` poblado por `EstantesService`), envío con el ID Token real, mensaje de éxito/error, limpieza del formulario tras guardar (conservando `porcentajeDescuentoEditorial`) para agilizar la catalogación en serie. `RoleGuard` se extendió para aceptar un rol único **o** una lista de roles válidos (`RolUsuario | RolUsuario[]`), ya que `POST /api/libros` acepta `vendedor` **o** `administrador` y el guard original solo soportaba un rol exacto. Se agregó también un header mínimo en `App` (enlace "Ingresar"/"Cerrar sesión" según `authService.usuario()`) — antes no existía ninguna forma de llegar a `/login` desde `/` sin escribir la URL a mano. 10 tests unitarios nuevos (3 `EstantesService`, 2 `RoleGuard` multi-rol, 5 `CatalogarLibroComponent`).

**Verificación en vivo — primera vez con un login real de Google, y dos bugs reales encontrados.** A diferencia de toda tarea anterior con rutas protegidas (siempre verificadas con `curl` o tests unitarios, nunca con un login interactivo real — decisión explícita en la tarea de `RoleGuard`, ver arriba), esta vez el usuario **decidió probar personalmente desde su propio navegador** ("Verificación manual tuya"), tras sembrar su email real (`ocastelblanco@gmail.com`) como `vendedor` en `babel-usuarios-staging` a su pedido explícito. El resultado destapó dos bugs reales que ninguna verificación anterior podía haber detectado:

1. **Bucle de redirección a `/login` con sesión real activa (bug crítico de arquitectura).** El usuario reportó: autenticarse con Google y navegar a `/libros` (o cualquier ruta protegida) siempre redirigía de vuelta a `/login`, con o sin sesión real. Causa raíz: el SDK cliente de Firebase persiste la sesión solo en el navegador (IndexedDB), sin ninguna cookie que el Lambda `ssr` pueda leer; con `RenderMode.Server`, `AuthGuard`/`RoleGuard` se evalúan en el servidor en cada carga completa de página, donde `AuthService` nunca inicializa el SDK real — el guard siempre veía `usuario() === null` y redirigía, autenticado o no. Diagnosticado por completo desde el código (sin poder reproducirlo interactivamente en este entorno sandbox): se confirmó por `grep` que no existe ningún mecanismo de cookie de sesión, y se razonó desde cómo `RenderMode.Server` ejecuta guards en cada petición. Con permiso explícito del usuario ("Sí, adelante, implementa el fix."), se corrigió cambiando `/libros` y `/catalogar` a `RenderMode.Client` (CSR puro, el guard evalúa en el navegador con la sesión real) en `app.routes.server.ts`, y se agregó `AuthService.esperarListo()` (promesa que resuelve tras el primer `onAuthStateChanged`, o de inmediato en servidor) que `AuthGuard`/`NoAuthGuard`/`obtenerIdToken()` esperan antes de leer el Signal — evita una condición de carrera al restaurar una sesión persistida. Ningún cambio de seguridad real: los guards siempre fueron solo UX, la autorización real la sigue verificando siempre la Lambda `api` (`CLAUDE.md` A01/A07). Ver gotcha crítico completo en §7.
2. **`GET /api/estantes` bloqueaba a `vendedor` (bug de diseño en una tarea ya cerrada).** Tras resolver el bug anterior, el usuario reportó: pudo entrar a `/catalogar`, pero el `<select>` de estante siempre aparecía vacío con "No se pudieron cargar los estantes." Causa raíz: `GET /api/estantes` exigía `administrador` exclusivamente (decisión de diseño explícita de la tarea del CRUD de estantes, correcta en su momento pero nunca revisada al construir esta feature nueva sobre ella). Corregido separando la verificación de rol dentro del mismo handler: `exigirVendedorOAdministrador` (nueva) solo para `GET`, `exigirAdministrador` (sin cambios) para `POST`/`PUT`/`DELETE`. 2 tests netos nuevos en `estantes.spec.ts` (59 tests totales en `test:api`, antes 57). Ver gotcha completo en §7.

Ambos fixes se hicieron dentro de la misma tarea/PR antes de cerrarla (mismo precedente que el incidente de `package.patterns` de `POST /api/libros` y la regresión 500 de `GET /api/libros`), no diferidos a una tarea nueva.

**Datos de demostración para la catalogación manual.** Tras desplegar el segundo fix, se detectó que `babel-estantes-staging` seguía vacía — nunca se había sembrado ningún estante no desechable (a diferencia de `sembrar-libros-demo`, que sí siembra libros reales); un `vendedor` real seguiría viendo el `<select>` vacío (sin error esta vez, pero sin opciones). Se agregó la operación `sembrar-estantes-demo` a `operaciones-staging.yml` + `server/scripts/sembrar-estantes-demo.mjs` (mismo patrón que `sembrar-libros-demo`: `estanteId` fijos, reejecutar actualiza en vez de duplicar) — se usaron a propósito los IDs `estante-1`/`estante-2`, los mismos que ya referenciaban los 3 libros de `sembrar-libros-demo` desde su creación sin que existiera ningún estante real con esos IDs (bug de datos menor, corregido de facto con esta siembra).

**Verificación final: el usuario confirmó "Funciona bien, el libro queda correctamente catalogado."** — flujo completo login real → `/catalogar` → seleccionar estante real → guardar, confirmado de punta a punta en su propio navegador contra `staging`. Cambios en la rama `feature/catalogar-libro-frontend`, PR #23 (abierto al momento de esta actualización de documentación, enviada a la misma rama).

**Qué se hizo el 2026-07-20 (CRUD `/api/editoriales-descuentos`, administrador):** implementado `server/api/handlers/editoriales-descuentos.ts` — un solo handler para los 4 verbos (`GET`/`POST`/`PUT`/`DELETE`, mismo patrón que `estantes.ts`/`usuarios.ts`), exige rol `administrador` exclusivamente. `editorial` es la clave primaria de `babel-editoriales-descuentos`, no se genera. `validarDatosDescuentoEditorial`/`validarDatosNuevoDescuentoEditorial` exportadas como funciones puras (`porcentajePorDefecto` y cada valor de `porcentajesDisponibles` entre 0-100). `POST` sobre una editorial que ya existe responde `409` en vez de sobrescribir en silencio — misma decisión que ADR-009 (CRUD de usuarios), aplicada aquí por consistencia sin abrir un ADR nuevo (el precedente ya cubre el criterio general). Nueva función Lambda `editorialesDescuentos` (ADR-008) con rol IAM propio: CRUD completo sobre `babel-editoriales-descuentos` + solo lectura sobre `babel-usuarios`. Aplicada de entrada la lección del incidente de `POST /api/libros` (ver §7): `verificar-token.js` en `package.patterns` desde el primer commit. 19 tests unitarios nuevos (`editoriales-descuentos.spec.ts`: 5 de `validarDatosDescuentoEditorial` + 3 de `validarDatosNuevoDescuentoEditorial` + 11 del handler completo — 78 tests totales en `test:api`, antes 59).

Se agregó la operación `probar-editoriales-descuentos-crud` a `operaciones-staging.yml` (mismo mecanismo de ID Token real que `probar-usuarios-crud`) + `ver-logs-editoriales-descuentos`. Verificado en vivo contra `staging` en dos pasadas: primero con un correo (`admin-prueba-editoriales@letiende.co`) sin fila en `babel-usuarios-staging` → `GET /api/editoriales-descuentos` respondió `403` ("Este correo no está autorizado para administrar descuentos editoriales en Babel.") y el script se detuvo ahí, confirmando el caso de correo no autorizado; luego, sembrado ese mismo correo como `administrador` (`sembrar-usuario-prueba`), se corrió el ciclo completo: `GET` inicial → `200 []`, `POST` → `201` (editorial "Editorial De Prueba CRUD" creada con `porcentajePorDefecto: 35`), `PUT` → `200` (actualizado a `40`), `GET` posterior → `200` confirmando el cambio persistido, `DELETE` → `204` — la editorial de prueba y el usuario de Firebase Auth se limpiaron solos, y la fila de `babel-usuarios-staging` se limpió aparte con `eliminar-usuario-prueba`. Sin regresión en el resto de endpoints (cubierta indirectamente por los 78 tests de `test:api`, todos en verde antes de desplegar).

**Incidente de infraestructura durante la verificación (no relacionado con el código de esta tarea):** el job `build-y-test` del PR #24 quedó ~20 minutos en `queued` sin que GitHub le asignara runner — el usuario confirmó directamente desde la UI de GitHub el mensaje `"Waiting for a runner to pick up this job..."`, descartando que fuera (por tercera vez en el proyecto) un caso de polling con datos obsoletos de las herramientas de este agente. Se canceló el run y se relanzó (`cancel_workflow_run` + `rerun_workflow_run`); el segundo intento consiguió runner y corrió con éxito en segundos. Causa raíz fuera del control del repo (ver gotcha nuevo en §7).

**Qué se hizo el 2026-07-20 (`PATCH /api/libros/:bookId/estante`, backend):** implementado `handlerCambiarEstante` en `server/api/handlers/libros.ts` (mismo archivo que ya exporta `handler`/`handlerCrear`) — exige rol `vendedor` o `administrador` (mismo criterio que `POST /api/libros`, mover un libro de estante es parte de la operación diaria de venta), valida que el `bookId` del path exista en `babel-libros` (`404` si no) y que el `estanteId` del body exista en `babel-estantes` (`400` si no) antes de actualizar solo `estanteId`/`actualizadoEn` del libro. Se corrigió de paso el path desactualizado que traía `tech-specs.md` (`PATCH /api/libros/:isbn/estante` → `:bookId/estante`, en la tabla de endpoints §5 y en la ruta del frontend §4.2) — la clave primaria real de `babel-libros` es `bookId` (`isbn` puede ser `null`), mismo criterio que ya usan `PUT`/`DELETE /api/estantes/:estanteId` y `PUT`/`DELETE /api/usuarios/:email`. Nueva función Lambda `cambiarEstanteLibro` (ADR-008) con rol IAM propio: `GetItem`+`PutItem` sobre `babel-libros`, solo `GetItem` sobre `babel-estantes` y `babel-usuarios`. Aplicada de entrada la lección del incidente de `POST /api/libros` (ver §7): `verificar-token.js` en `package.patterns` desde el primer commit, y se revisó que el `package.patterns` de `libros`/`catalogarLibro` (las otras 2 funciones que ya comparten `libros.ts`) siguiera íntegro tras el `import` nuevo. 6 tests unitarios nuevos (`libros.spec.ts`: 401/403/404 bookId/400 sin estanteId/400 estanteId inexistente/200 — 84 tests totales en `test:api`, antes 78).

Se agregó la operación `probar-cambiar-estante` a `operaciones-staging.yml` (mismo mecanismo de ID Token real que el resto): a diferencia de `probar-catalogar-libro`/`probar-estantes` (que dependían de estantes ya sembrados o creaban uno solo), este script siembra **dos** estantes desechables por DynamoDB directo (origen/destino, con UUID en el `estanteId` para evitar colisiones entre corridas) para no depender de que `estante-1`/`estante-2` (los demo, ver tarea de catalogación manual) ya existan en el entorno. Verificado en vivo contra `staging` en dos pasadas: primero con un correo (`admin-prueba-cambiar-estante@letiende.co`) sin fila en `babel-usuarios-staging` → `POST /api/libros` (paso previo del script) respondió `403` y el script se detuvo ahí — pero limpió igual los dos estantes desechables que ya había sembrado, confirmando que la limpieza del script funciona aunque el ciclo se corte temprano (mismo patrón de robustez ya usado en `probar-registrar-venta`); luego, sembrado ese mismo correo como `vendedor`, se corrió el ciclo completo: `POST /api/libros` → `201` (libro creado con `estanteId` = origen), `PATCH /api/libros/:bookId/estante` → `200` (`estanteId` actualizado al destino), `GET /api/libros` posterior → `200` confirmando que el libro en el catálogo ya muestra el `estanteId` del destino — el libro y ambos estantes de prueba se limpiaron solos, y la fila de `babel-usuarios-staging` se limpió aparte con `eliminar-usuario-prueba`. Sin incidentes de infraestructura esta vez (el deploy del PR corrió normalmente, sin el atasco de runner de la tarea anterior). Cambios en la rama `feature/cambiar-estante-libro`, PR #25 (abierto al momento de esta actualización de documentación, enviada a la misma rama).

**Qué se hizo el 2026-07-20 (`GET /api/ventas`, backend):** implementado `handlerListar` en `server/api/handlers/ventas.ts` (mismo archivo que ya exporta `handler` de `POST /api/ventas`) — exige rol `administrador` exclusivamente (a diferencia de `POST`, que acepta `vendedor`: un reporte con costos/utilidades es información sensible de negocio). Filtros opcionales por query params: `desde`/`hasta` (comparación directa de strings ISO contra `Venta.vendidoEn`, que siempre se genera con `new Date().toISOString()`), `formaDePago` (uno de los 5 valores válidos), `editorial` (la `Venta` no guarda `editorial` directamente — se resuelve contra `Libro.editorial` haciendo `GetItem` puntual por cada `bookId` único entre las ventas que ya pasaron los demás filtros, evitando un `Scan` completo de `babel-libros`). `validarFiltrosVentas` exportada como función pura (mismo patrón que el resto de handlers).

**Hallazgo real de infraestructura durante la implementación:** al construir el primer filtro por rango de fecha del proyecto, se descubrió que el GSI `vendidoEn-index` de `babel-ventas` (declarado desde el esqueleto de Serverless Framework, nunca antes usado por ningún handler) solo tiene `vendidoEn` como partición, sin clave de ordenamiento — no admite `Query` por rango en absoluto, pese a que `tech-specs.md` lo describía para eso. Se optó por `Scan` + filtrado en memoria (mismo criterio ya aceptado para tablas pequeñas que `estantes.ts`/`editoriales-descuentos.ts`) en vez de rediseñar el GSI, documentando la deuda técnica en §7 en vez de expandir el alcance de esta tarea.

Nueva función Lambda `listarVentas` (ADR-008) con rol IAM propio de solo lectura: `Scan` sobre `babel-ventas`, `GetItem` puntual sobre `babel-libros` y `babel-usuarios`, sin ningún permiso de escritura. Aplicada de entrada la lección del incidente de `POST /api/libros` (ver §7): `verificar-token.js` en `package.patterns` desde el primer commit. 13 tests unitarios nuevos (`ventas.spec.ts`: 6 de `validarFiltrosVentas` + 7 del handler completo — 97 tests totales en `test:api`, antes 84).

Se agregó la operación `probar-listar-ventas` a `operaciones-staging.yml` (mismo mecanismo de ID Token real que el resto): `GET /api/ventas` primero — si el correo no es administrador, se detiene ahí sin crear datos de prueba. Verificado en vivo contra `staging` en dos pasadas: primero con un correo (`admin-prueba-listar-ventas@letiende.co`) sin fila en `babel-usuarios-staging` → `403` ("Este correo no está autorizado para ver reportes de ventas en Babel.") y el script se detuvo ahí; luego, sembrado ese mismo correo como `administrador`, se corrió el ciclo completo: `GET` inicial → `200 []`, `POST /api/libros` × 2 (editoriales distintas) → `201` cada uno, `POST /api/ventas` × 2 (formas de pago distintas: efectivo/tarjeta) → `201` cada uno, `GET /api/ventas` sin filtro → `200` con ambas ventas presentes, `GET /api/ventas?formaDePago=tarjeta` → `200` con solo la venta de tarjeta, `GET /api/ventas?editorial=...` → `200` con solo la venta de esa editorial (la otra correctamente excluida) — los 2 libros, las 2 ventas y el usuario de Firebase Auth de prueba se limpiaron solos, y la fila de `babel-usuarios-staging` se limpió aparte con `eliminar-usuario-prueba`. Deploy del PR sin incidentes de runner esta vez. Cambios en la rama `feature/listar-ventas`, PR #26 (abierto al momento de esta actualización de documentación, enviada a la misma rama).

**Qué se hizo el 2026-07-20 (eliminación del GSI `vendidoEn-index`, fix fuera del motor JIT):** tras fusionar el PR #26, el usuario pidió resolver la deuda técnica del GSI en vez de dejarla anotada. Diagnóstico: el GSI no solo era inútil (partición-únicamente, sin `Query` por rango posible) sino activamente costoso — 25/25 RCU/WCU aprovisionados propios + duplicación de almacenamiento, contra el objetivo de $0. Se decidió **eliminarlo** en vez de rediseñarlo (YAGNI: a este volumen el `Scan` ya verificado en vivo es suficiente, y un GSI de partición fija sería un anti-patrón de partición caliente). Cambios: `serverless.yml` (GSI + `AttributeDefinition` de `vendidoEn` + referencia `index/*` en `ApiLambdaRole`), `tech-specs.md` §5.1 (refleja la realidad), TSDoc de `handlerListar` (ya no habla de un GSI existente), gotcha de §7 marcado RESUELTO. Sin cambios de lógica: `GET /api/ventas` ya usaba `Scan`, ningún test cambia. Verificado en vivo tras el deploy re-ejecutando `probar-listar-ventas` (mismo ciclo completo del PR #26). Rama `fix/gsi-vendido-en-inutil`.

**Qué se hizo el 2026-07-20 (`CambiarEstanteComponent`, frontend):** primer consumo desde el frontend de `PATCH /api/libros/:bookId/estante` (backend ya cerrado y verificado en vivo en el PR #25). Se revisó primero el estado real de `ListaLibrosCatalogadosComponent`: seguía siendo el placeholder trivial del scaffold inicial, así que se extendió mínimamente (reutilizando `LibrosService` ya existente, sin tocar el backend) para listar el catálogo real con un enlace "Cambiar estante" por libro — explícitamente no se construyó una pantalla completa de gestión de libros, eso queda como alcance de roadmap futuro. Se implementó `CambiarEstanteComponent` (`src/app/features/libros/cambiar-estante.component.ts`, ruta nueva `/libros/:bookId/estante` en `app.routes.ts`, guardada solo con `AuthGuard` — no `RoleGuard`, ya que tanto `vendedor` como `administrador` pueden mover un libro de estante, igual que `/libros`): el `bookId` se toma del route param, y el libro actual se resuelve con un `computed()` sobre `LibrosService.libros()` (el catálogo público que la app ya carga) en vez de agregar un endpoint de detalle nuevo — decisión deliberada, ya que `GET /api/libros` solo expone el listado completo y no hacía falta un `GET /api/libros/:bookId` solo para esta pantalla (limitación conocida y documentada en el código: un libro con `cantidadDisponible` 0 no aparece en el catálogo y por lo tanto tampoco en esta pantalla, pero es autoconsistente porque el mismo catálogo es la única puerta de entrada para llegar aquí). El `<select>` de estante reutiliza `EstantesService` (ya existente); el envío del `PATCH` autenticado se hizo inline en el componente con el ID Token real, siguiendo el mismo patrón ya usado en `CatalogarLibroComponent` en vez de crear un método nuevo en `LibrosService` (que hoy es deliberadamente el cliente del catálogo público sin autenticación). Se agregó la ruta nueva a `app.routes.server.ts` con `RenderMode.Client` (mismo motivo ya documentado en el archivo para `/libros`/`/catalogar`, ver §7: la sesión de Firebase solo existe en el navegador). 10 tests unitarios nuevos (`cambiar-estante.component.spec.ts`: 6 — carga estantes/catálogo al iniciar, preselecciona el estante actual tras cargar el catálogo, "no se encontró el libro" con un `bookId` desconocido, `PATCH` real con ID Token y mensaje de éxito, mensaje de error si el `PATCH` falla, no envía nada si el formulario no se renderiza; `lista-libros-catalogados.component.spec.ts`: 4 — carga el catálogo al iniciar, muestra libros con el enlace correcto, mensaje de catálogo vacío, mensaje de error).

Para la verificación, dado que la ruta requiere un login real de Google (no automatizable en este entorno sandbox), se preguntó al usuario cómo proceder — **eligió explícitamente "Combinar evidencia existente (recomendado)"**: el backend `PATCH /api/libros/:bookId/estante` ya se había verificado en vivo de punta a punta contra `staging` en el PR #25 (ciclo completo `POST`→`PATCH`→`GET` con un `vendedor` real), y ahora se suma la cobertura de los 10 tests unitarios nuevos del frontend, en vez de requerir una verificación manual puntual del usuario para esta tarea. `npm run build` y `npm test -- --watch=false` en verde (45 tests totales de frontend). Cambios en la rama `feature/cambiar-estante-component`, PR #28.

**Qué se hizo el 2026-07-20 (sección `/admin`, frontend):** primer punto de entrada real y protegido a la administración. `AdminInicioComponent` (`src/app/features/admin/admin-inicio.component.ts`, ruta nueva `/admin` en `app.routes.ts`, guardada con `RoleGuard('administrador')` ya existente) muestra un índice con las 4 secciones futuras (Estantes, Usuarios, Editoriales, Reportes) como tarjetas sin enlace real — deliberadamente, ninguna pantalla existe todavía, evitando tanto una página vacía sin sentido como sobrealcanzar la tarea implementando cada CRUD. Se agregó `RenderMode.Client` para la ruta nueva en `app.routes.server.ts`, mismo motivo ya documentado ahí para `/libros`/`/catalogar` (la sesión de Firebase solo vive en el navegador).

El header (`App`) resuelve el rol reutilizando `UsuariosService` (ya existente, el mismo que usa `RoleGuard`) en vez de duplicar lógica: se agregó un `computed()` `esAdministrador` que depende de `authService.usuario() !== null` **y** de `usuariosService.usuarioActual()?.rol === 'administrador'` — depender también de `authService.usuario()` (no solo del rol cacheado) fue una decisión deliberada para que el enlace desaparezca de inmediato al cerrar sesión, ya que `UsuariosService.usuarioActual` no se limpia solo y `cerrarSesion()` no lo resetea. Un `effect()` nuevo en el constructor de `App` dispara `usuariosService.obtenerUsuarioActual()` cada vez que `authService.usuario()` deja de ser `null`, para que el rol esté resuelto tan pronto el usuario inicia sesión (sin depender de que navegue primero a una ruta guardada por `RoleGuard`, que es lo único que llamaba a ese método hasta ahora). 7 tests unitarios nuevos (`app.spec.ts` ampliado de 2 a 5 — visibilidad del enlace sin sesión/como vendedor/como administrador, y que se resuelve el usuario al iniciar sesión —, más 2 de `AdminInicioComponent`).

**De paso, a pedido explícito del usuario, se sembró la primera cuenta `administrador` real y persistente del proyecto:** `letiende.co@gmail.com`, vía la operación ya existente `sembrar-usuario-prueba` de `operaciones-staging.yml` (mismo mecanismo de escritura directa a DynamoDB que toda cuenta de prueba anterior), pero **sin ejecutar `eliminar-usuario-prueba`** al final — a diferencia de cualquier uso previo de esa operación, este registro es intencionalmente permanente, no desechable. Confirmado con `consultar-usuario-prueba`: `rol: administrador`, `nombre: "Le Tiende"`, fila real en `babel-usuarios-staging`.

`npm run build` y `npm test -- --watch=false` en verde (51 tests, antes 45); `npm run build:api`/`npm run test:api` sin cambios (97 tests, backend no tocado). Para la verificación en vivo, dado que ya existía una cuenta `administrador` real utilizable, se le preguntó al usuario cómo proceder — **a diferencia de la tarea anterior (`CambiarEstanteComponent`, evidencia combinada), esta vez eligió explícitamente "Verificación manual tuya"**: confirmó personalmente en `staging` que el enlace "Administración" y la ruta `/admin` funcionan con esa cuenta, y que un `vendedor` no ve el enlace ni puede entrar. Cambios en la rama `feature/admin-inicio-placeholder`, PR #29.

**Nota (2026-07-25):** entre el PR #29 (sección `/admin`, arriba) y el PR #60 (área "Gestionar", ver §2), el proyecto avanzó a través de varias sesiones distintas (posiblemente en paralelo, incluido al menos un dispositivo móvil — ver `Hitos desarrollo - detail.csv`, columna `device`) sin que esta cadena de entradas de §9 se mantuviera al día: el escaneo de código de barras (`@zxing/browser`), la búsqueda por título/autor, el módulo completo de obtención de metadatos/PVP por scraping (`ajustes-finales.md`/`plan-obtencion-info-libros.md`), los 3 CRUD de administración restantes (usuarios, editoriales, sitios de scraping), el reporte de ventas XLSX, la ficha pública de libro, el modelo jerárquico Espacio→Mueble→Ubicación (reemplaza a `Estante`), la venta desde la ficha y el área "Gestionar" se implementaron y fusionaron (PRs #30–#60) sin narrativa correspondiente aquí — el detalle de cada uno vive en su propio PR/commits de GitHub y, para las Tareas A–E del backlog de `ajustes-finales.md`, en el checklist de §2 de este mismo documento. No se reconstruye retroactivamente por no haber sido presenciado de primera mano por ninguna sesión.

**Qué se hizo el 2026-07-25 (resincronización tras trabajo duplicado con PR #60):** esta sesión retomó el trabajo con `TODO.md`/`MEMORY.md` desactualizados en su contexto (todavía mostraban la sección `/admin` como Tarea 1 activa) y comenzó a reimplementar el área "Gestionar" desde cero — sin saber que otra sesión ya la había completado y fusionado como PR #60. A mitad de la implementación del backend (`PUT`/`DELETE /api/libros/:bookId`, sin commitear), el usuario avisó que había aceptado y fusionado el PR #60, y pidió actualizar todo, limpiar el repositorio local y revalidar el motor JIT. Se descartó el borrador duplicado (`git checkout -- server/api/handlers/libros.ts`, nunca llegó a un commit), se sincronizó `main` con `origin/main` (9 commits nuevos) y se eliminaron 10 ramas locales ya fusionadas y obsoletas (`claude/babel-project-bootstrap-k7bhww`, y las de `/admin`, `CambiarEstanteComponent`, cambio de estante, catalogación manual, CRUD de editoriales, listar ventas, modelos de datos, fix del GSI, y el propio borrador de "Gestionar"). Se releyó `ajustes-finales.md` §"Backlog ordenado de implementación" y el cuerpo real del PR #60 (vía la API de GitHub, no adivinado) para corregir con precisión el `TODO.md`: la Tarea E se marcó `[x]` en `ajustes-finales.md`, subió a Tarea 1 la Tarea F (filtrado público por ubicación, sin cambios de contenido) y se agregó como Tarea 2 la Tarea G (Reporte de Inventario XLSX), redactada reutilizando el patrón ya existente de `GET /api/ventas/exportar`. **Lección de proceso, ver también la nota de la Tarea E en §2:** con varias sesiones activas sobre el mismo repositorio, empezar cualquier tarea nueva sin antes hacer `git fetch`/`pull` de `main` y releer `TODO.md` desde disco (no solo confiar en el estado cargado al inicio de la conversación) puede llevar a trabajo duplicado — esta vez se detectó a tiempo (antes de commitear) gracias al aviso directo del usuario, pero el patrón general aplica igual para sesiones futuras.

**Qué se hizo el 2026-07-25 (filtrado público por ubicación):** `CatalogoPublicoComponent` (`src/app/features/catalogo-publico/`) suma 2 `<select>` (Espacio, y Mueble filtrado por el Espacio elegido vía un `computed()` `mueblesFiltrados`) que se combinan con el `<input type="search">` de título/autor/ISBN ya existente — los tres criterios son acumulativos (`librosFiltrados` los aplica todos en un único `computed()`). Para saber a qué Espacio/Mueble pertenece cada libro, se construyeron 2 `Map` en memoria a partir de `UbicacionFisicaService.ubicaciones()`/`.muebles()` (`ubicacionId → muebleId`, `muebleId → espacioId`) — reutiliza los mismos endpoints públicos que ya usa `/admin/ubicaciones`, sin agregar ninguna llamada nueva por libro ni tocar `GET /api/libros`. Se inyectaron `ActivatedRoute`/`Router` para soportar `?espacio=<id>&mueble=<id>`: al iniciar, `route.snapshot.queryParamMap` precarga los signals de selección; al cambiar cualquier `<select>`, `router.navigate([], { queryParams: {...}, queryParamsHandling: 'merge', replaceUrl: true })` refleja la selección en la URL sin ensuciar el historial en cada cambio (habilita el caso de uso QR de `ajustes-finales.md`: solo la URL filtrable, no la generación de la imagen). Cambiar de Espacio limpia el Mueble seleccionado (puede no pertenecer al nuevo Espacio), mismo criterio de cascada ya usado en `/admin/ubicaciones`.

**Bug real de Angular encontrado al escribir el test de pre-selección por query params:** el primer borrador del template usaba `[value]="espacioSeleccionado()"` en el `<select>` (property binding directo, sin `ngModel`/`formControlName`). El test que precargaba `?espacio=espacio-2` fallaba: el `<select>` quedaba en `''` en vez de `'espacio-2'`, a pesar de que el signal sí tenía el valor correcto. Causa raíz: Angular aplica los bindings del propio elemento `<select>` ANTES de crear sus `<option>` hijas (las que vienen de un `@for`) — al asignar `.value = 'espacio-2'` a un `<select>` que todavía no tiene ninguna `<option value="espacio-2">` en el DOM, el navegador simplemente no selecciona nada, y **no se autocorrige después** aunque la opción se agregue milisegundos más tarde en la misma pasada de change detection (comportamiento nativo del elemento `<select>`, no un bug de Angular). Corregido moviendo la selección a cada `<option>` individual (`[selected]="espacioSeleccionado() === espacio.espacioId"`), que sí se evalúa en el momento correcto porque se aplica cuando esa `<option>` específica ya existe. Este es el patrón correcto para cualquier `<select>` sin `ngModel`/`formControlName` en Angular — vale la pena recordarlo para futuros selects "simples" fuera de un `FormGroup` (ver gotcha nuevo en §7).

8 tests unitarios nuevos (`catalogo-publico.component.spec.ts`: filtra por Espacio, filtra por Mueble, acumulativo con la búsqueda de texto, el `<select>` de Mueble se limita al Espacio elegido y se resetea al cambiarlo, pre-selección desde query params, actualización de la URL al cambiar un `<select>` — 295 tests totales de frontend, antes 288). `npm run build`/`npm test -- --watch=false` en verde; `npm run build:api`/`npm run test:api` sin cambios (262 tests, backend no tocado). Antes de poder correr build/tests hubo que `npm ci --ignore-scripts` (gotcha ya documentado en §7: el `postinstall` de `serverless` falla sin salida a internet general) porque el `node_modules` local no tenía las dependencias que otras sesiones ya habían agregado a `package.json` entre PRs (`@zxing/browser`, `@zxing/library`, `@angular/cdk`, `xlsx`, etc.).

**Incidente de proceso: conflicto de fusión real, resuelto sin rebase.** Al abrir el PR, GitHub marcó un conflicto real (`mergeable_state: "dirty"`) contra `main`. Causa: esta rama se creó desde el commit del PR #60, y mientras estaba en curso, el PR #61 (revalidación del motor JIT, de esta misma sesión) se fusionó a `main` — ambos PRs agregaron una fila distinta al final de `Hitos desarrollo - detail.csv` (CLAUDE.md §8), y git no pudo reconciliar automáticamente dos inserciones adyacentes en el mismo punto del archivo. Resuelto con `git merge origin/main` (nunca `git rebase`/`push --force`, prohibidos por `CLAUDE.md` para cualquier rama) — el único archivo con conflicto real fue el CSV (los demás, `TODO.md`/`MEMORY.md`/`ajustes-finales.md`, se fusionaron solos porque esta rama nunca los había tocado); se conservaron ambas filas en orden cronológico dentro del archivo. Se re-corrieron `npm run build`/`npm test -- --watch=false` tras el merge (295 tests, sin regresión) antes de volver a hacer push. **Lección:** cuando varias ramas activas tocan el mismo archivo de registro append-only (como este CSV), un conflicto de fusión es prácticamente inevitable si sus PRs se fusionan fuera de orden — no es un error de ningún PR en particular, solo hay que resolverlo con un merge normal conservando ambas entradas, nunca eligiendo una sobre la otra.

Para la verificación, dado que es una ruta pública (sin login), se le preguntó al usuario cómo prefería cerrarla — **eligió "Verificación manual tuya"** en vez de combinar evidencia (a pesar de no requerir autenticación, señal de que prefiere confirmar visualmente los cambios de URL/`<select>` él mismo). Confirmó que "funciona bien" tras probar en `staging`. Cambios en la rama `feature/filtro-catalogo-por-ubicacion`, PR #62 (fusionado).

**Qué se hizo el 2026-07-25 (Reporte de Inventario XLSX, cierra `ajustes-finales.md`):** implementado `handlerExportarInventario` en `server/api/handlers/libros.ts` (`GET /api/libros/exportar`), siguiendo el mismo patrón exacto que `handlerExportar` de `ventas.ts` (librería `xlsx`, `XLSX.utils.json_to_sheet` + `XLSX.write(..., { type: 'base64', bookType: 'xlsx' })`, respuesta con `isBase64Encoded: true` y `Content-Disposition: attachment`). Exige rol `administrador` exclusivamente — igual criterio que el reporte de ventas, es información de negocio sensible (costo/volumen de inventario). A diferencia de `resolverUbicacion` (que hace 3 `GetItem` puntuales por libro, usado por la ficha pública que solo resuelve un libro a la vez), este handler carga las 3 tablas de ubicación completas con `Promise.all([escanearTodo(...), ...])` una sola vez y arma 2 `Map` en memoria — decisión explícita documentada en el propio código: a este volumen (miles de libros, pero solo decenas de ubicaciones distintas) es más barato escanear 3 tablas pequeñas una vez que repetir una cadena de 3 `GetItem` por cada fila del reporte. `Cantidad` en el XLSX reporta `cantidadDisponible` (no `cantidadTotal`): la decisión (no explícita en `ajustes-finales.md`, que solo decía "Cantidad") fue interpretar el reporte como una herramienta de conciliación física — `cantidadDisponible` es lo que debería estar físicamente en el estante ahora mismo, mientras que `cantidadTotal` incluye ejemplares ya vendidos.

Nueva función Lambda `exportarInventario` en `serverless.yml` (ADR-008) con rol IAM de solo lectura (`Scan` sobre `babel-libros`/`babel-ubicaciones`/`babel-muebles`/`babel-espacios`, `GetItem` sobre `babel-usuarios`, sin ningún permiso de escritura). Frontend: `LibrosService.exportarInventario()` (nuevo método, mismo patrón de descarga de blob — `URL.createObjectURL`/`<a download>`/`URL.revokeObjectURL` — que `VentasService.exportarVentas`) y una segunda tarjeta "Reporte de inventario" agregada a `ReportesVentasComponent` (`/admin/reportes`, ya existía para el reporte de ventas), con su propio `signal` de carga/error/éxito totalmente independiente del formulario de ventas — un fallo o una carga en un reporte no afecta al otro. 3 tests unitarios nuevos de backend (401/403/200 con verificación de columnas y resolución de nombres, incluido un caso con datos inconsistentes — `ubicacionId` que ya no existe — que debe degradar a `'—'` sin romper el reporte, CLAUDE.md A08) y 7 de frontend (3 en `libros.service.spec.ts`, 4 en `reportes-ventas.component.spec.ts`) — 265 tests de backend (antes 262), 302 de frontend (antes 295).

Antes de poder correr build/tests hubo que reinstalar dependencias (`npm ci --ignore-scripts`, gotcha ya documentado en §7 sobre el `postinstall` de `serverless`): el `node_modules` de esta sesión no tenía las librerías que otras sesiones ya habían agregado a `package.json` entre tareas anteriores. Verificado en vivo por el usuario contra `staging`: confirmó la descarga real del `.xlsx` con datos reales. Cambios en la rama `feature/reporte-inventario-xlsx`, PR #63 (fusionado). **Esto cierra por completo el backlog de `ajustes-finales.md` (Tareas A–G) — todo el roadmap Alta y Media del PRD queda cubierto.**

**Pausa deliberada del motor JIT, a pedido explícito del usuario:** tras confirmar la verificación en vivo y fusionar el PR #63, el usuario pidió limpiar el repositorio local, revalidar la documentación (esta actualización) y abrir el PR de documentación correspondiente — pero **no** continuar de inmediato con las 2 últimas piezas del roadmap (modo offline/cola de sincronización, primer despliegue a producción). Antes de detallarlas, el usuario va a correr una ronda de pruebas manuales de flujos completos en `staging` por su cuenta, y avisará si esas pruebas revelan algún ajuste pendiente antes de continuar — o si se puede seguir directo con esas 2 tareas. `TODO.md` queda documentado explícitamente sin tareas activas (a diferencia de cualquier cierre de tarea anterior en este proyecto, que siempre calculaba y dejaba lista la siguiente Tarea 1/Tarea 2) para reflejar con precisión este estado de espera, en vez de fabricar contenido especulativo de "Qué hacer" para 2 tareas que hoy no tienen ningún detalle de producto más allá de una mención de una línea en `PRD.md`.

**Próxima tarea sugerida:** ninguna todavía — ver `TODO.md`. Esperar el resultado de la ronda de pruebas manuales del usuario. Si no aparece ningún ajuste, las 2 tareas a detallar y arrancar son modo offline/cola de sincronización y primer despliegue a producción (`PRD.md` §6, ambas "Baja" — desbloqueadas ahora que `ajustes-finales.md` está cerrado por completo).

**Nota de proceso:** entre esa pausa (2026-07-25) y la siguiente entrada (2026-08-19, abajo) hay más de 15 PRs fusionados (#64–#89: cierre de `ajustes-2026-07-27.md`, cancelación de modo offline, dominio personalizado y primer despliegue a producción, generador de QR para muebles, orden alfabético y mejoras a Catalogar, reforma del header superior, fix de isbn nulo) sin narrativa propia en esta sección — ver `TODO.md` y los números de PR en el historial de GitHub para el detalle de cada uno; §2 sí tiene una entrada por cada uno. No se reconstruye retroactivamente aquí por no ser parte de la tarea que motivó esta entrada.

**Qué se hizo el 2026-08-19 (Tarea 2 del lote de catalogación — palabras clave que invalidan una portada):** sesión retomada con `git fetch`/`pull` de `main` y relectura de `docs/TODO.md` desde disco (ya movido de la raíz a `docs/` por otra sesión, junto con `MEMORY.md` y el CSV de tracking, renombrado a `tracking-detail.csv`) — confirmó que el roadmap había avanzado mucho más allá del último estado conocido en el contexto de la conversación (PRs #67–#89 ya fusionados), y que la Tarea 2 (de un lote de 3 ajustes de catalogación traído el 2026-08-18) estaba lista para arrancar tras el cierre de la Tarea 1 (isbn nulo, PR #89). Se implementó siguiendo el detalle ya descrito en `TODO.md` (ver §2 arriba para el resumen técnico completo): campo `palabrasClaveInvalidas` en las 3 copias de `SitioScraping`, `portadaEsInvalida()` en `scraping.ts`, wiring en `resolverMetadatosCompletos`, control nuevo en `GestionSitiosScrapingComponent`. Abierto como PR #90 en `feature/palabras-clave-invalidas-portada` (306 tests backend / 328 frontend, ambos builds limpios) — el usuario reportó, tras probarlo en `staging`, que `/admin/sitios` aparecía incompleta (sin botones "Eliminar", solo el primer sitio con info visible). Diagnosticado y corregido en el mismo PR (ver la entrada de §2 y el gotcha nuevo de §7 para el detalle técnico completo de la causa raíz — normalización de campos nuevos ausentes en filas de DynamoDB ya existentes) con un segundo commit; el usuario confirmó "Funciona bien" tras el fix. **Lección de proceso reforzada (ya documentada el 2026-07-25, se repite aquí porque volvió a aplicar):** el estado real del repositorio puede avanzar mucho entre turnos de una misma conversación cuando hay trabajo en paralelo o pausas largas — releer `TODO.md`/`MEMORY.md` desde disco al retomar, en vez de confiar en el resumen de contexto, siguió siendo la forma correcta de detectar esto.

Con la Tarea 2 cerrada, se promovió la Tarea 3 (proceso asíncrono "Validar libros") como siguiente pieza del roadmap en `TODO.md` — sin detallar sus pasos atómicos todavía, ya que ese detalle solo existió en la conversación de la sesión que trajo el lote original y nunca quedó persistido en ningún documento (a diferencia de las Tareas 1 y 2, que sí traían un resumen técnico suficiente en `TODO.md` desde el principio).

**Próxima tarea sugerida:** Tarea 3 — proceso asíncrono "Validar libros" (PVP + portada, por mueble) — ver `TODO.md` para las decisiones de diseño ya resueltas con el usuario y la nota de que su detalle atómico completo debe redactarse desde cero al arrancarla.
